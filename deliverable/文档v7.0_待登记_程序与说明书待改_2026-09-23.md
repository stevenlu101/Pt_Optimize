# 待登记：程序内说明书页与界面文字待改（本轮不改 .cs）

从 v7.0 草案 F 的改动清单移出，另加四份审查新登记的几条。合稿后移入 HANDOVER。路径相对仓库根。

## 程序内说明书页 `Pt_Optimize/UI/ManualPage.cs`

- :80 WebView2 缺失提示写「内容一致」，与 :1387、`UI/Flow.cs:362`「只留程序里讲不了的事」矛盾。
- :1001 与 `UI/LineDesignPage.cs:2748`「这几项本页没有控件」已过时：舌根圆角、环宽、压接段、逐片舌保温、渐变环现在都有框，只有角焊缝没有（`LineDesignPage.cs:1057-1067,4855-4867`）。
- :1059 写重量对账「±1 % 内」，:1344 写「差 ≤ 2 %」，两处不一致；`Program.cs:7371` 命令行是 2 %。界面导出路径的门槛没有代码出处。
- :1083、:1364-1365 仍写「厚度到头，该改形状」。
- :1098 仍写「①②③④⑤ 状态条」，与判据代号同形，未解释。
- :1112 搜形状耗时写「几小时」，`UI/Flow.cs:316-317` 的 Cost 是「几十分钟」。
- :1236-1265 设计记录判据表没有工况列。
- :1292 升温行印的是 72 h（升温到位用时这条参考量的限值），不是升温这条硬判据的限值（管许用 J）。
- :1325 仍写「剩余误差估计」。
- :1334 仍写「③ 页多出两个页签」。
- :1361 常见问题只提未收敛，不提判不了。

## 界面文字 `Pt_Optimize/UI/Flow.cs` 等

- `Flow.cs:270-271` geom.analyze 的 Tip 写「报各几何量对判据的斜率（只测不调）」。实际做的是读入口片 .3dm、反推几何，只在 .3dm 模式可用（`LineDesignPage.cs:1313-1317,1578`）。
- `Flow.cs:331-333` export.page3dm 的 Tip 写「三段管 + 四片法兰，几何与刚才求解的完全一致」。
- `LineDesignPage.cs:5591-5594` 界面导出调 `Geometry3dm.WriteFinal3dm(dExp, file, baseIn: _base)`，没传 segCount、segLenMm、tubeIdMm，取默认 3 段、300 mm、50 mm（`Core/Geometry3dm.cs:141-143,238,247`）。段数或段长不同于默认时，出图的管与求解不一致；回显也写死「三段管 + 四片法兰」。需核。
- `Flow.cs:442-446` 参考工具页横幅写「设计从「① 整线核算」页开始」「判据与交付数一律以「① 整线核算」页为准」。没有这个页名：① 是「① 输入」，整线核算在「② 法兰优化」。
- `Flow.cs:553,1024,1035,1042,1044`：界面显示「回「整线核算」…」，应为「② 法兰优化」。
- `Flow.cs:229`（ParamScope 的 OverriddenBy 写「整线核算」页）、`Core/DesignInputs.cs:152`（写「③ 整线核算」页）：页名过时。
- `Core/ParamCat.cs:43`：第 8 组标题写「由「② 法兰优化」页上的控件决定」，而这些控件现在摆在「① 输入」页（`UI/MainForm.cs:183-190`）。
- `UI/MainForm.cs:795-796`「为各段选最省牌号」候选名单写死，含 FKS16/Pt、FKS16/PtRh-9010 两个灰显牌号，绕过了下拉的齐全度检查（`Core/LineSolver.cs` BestGrade 不查）。
- `UI/LineDesignPage.cs:5529` 舌保温可行窗口页签的结论标签直接印 `InsulWindow.Verdict`，而 Verdict 里带 markdown「**」（`Core/InsulWindow.cs:152,156`），Label 不认，会把星号原样印在界面上。

## 过时的代码注释（只登记，不改）

- `Core/Solver.cs:2660-2667`（QuantInsulMm 注释）仍写「二分先收到 0.005 mm 再对齐」。现行 RaiseUntil 二分直接在图纸格点上走（`Solver.cs:1362-1368`；HANDOVER.md:338），`TolOf`（`Solver.cs:2524`）没有调用方。
- `Core/Solver.cs:349,441,1719,1739` 仍写「法兰变重 ⇒ 电流略升」。设计电流只取管子自己的准静态电流，法兰几何不进这条式子（`Core/DesignCurrent.cs:14-21,103-113`）。
