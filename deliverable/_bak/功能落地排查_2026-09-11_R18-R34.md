# 功能落地排查 R18–R34（只读审查，不改代码）

范围：`HANDOVER.md` 要求登记表 R18–R34。四问：①界面入口 ②链路 ③结果可见 ④门（测试/走查节号）是否属实。
方法：直接读源码 + `grep`/`Read` 取证 + `dotnet build Pt_Optimize.sln`（0 错，确认所引用的测试/走查文件确实能编译，不是死代码）。

判定：齐＝四问都有证据；半＝链路在但入口或结果可见缺一样；缺＝只在 CLI/测试里、代码找不到。

| 行 | 判定 | 界面入口（文件:行） | 链路（文件:行） | 结果可见 | 门 | 缺什么／怎么补 |
|---|---|---|---|---|---|---|
| R18 法兰 J 判据 | 齐 | 无需单独入口：随「核算整线」自动跑（`LineDesignPage.cs:757` `_btnRun`） | `Core/DesignCurrent.cs`（升温电流）、`Core/Solver.cs:1274 ApplySectionFloor`（J=10 定下角）、`Core/LineRunner.cs:1923-1944`（判据「法兰截面 J」） | ③页判据表：`LineDesignPage.cs:4524-4548 FillChecks` 通用遍历 `r.Checks`，新判据自动上屏，无需改 UI | `BranchMarksAreReachedTests`、`SectionSizingTests`、`CriteriaTableTests`、`BestShapeFineMeshTests` 均存在且随解编译 | 无 |
| R19 .3dm 进搜形状 | 齐 | ①页「分析几何变数」`LineDesignPage.cs:781`／「◈ 图纸几何 → 参数」`:783` | `Flow.cs:535,601,628,658`（3dm 模式下交接到解析路，不管上次收不收敛） | 交接后盘径/舌宽控件解禁，输出框说明；一键流水线接着走 | `FlowDeadEndTests.cs`、`UniformPlateSizingTests.cs`、`OneClickPipelineTests.cs` 均存在；`tests/UiWiring/Program.cs:93` 有 `--follow3dm` 走查 | 无 |
| R20 判据①改闭式 | 齐 | 同 R18，自动跑 | `Core/DesignCurrent.cs:37-39,91-106`（`RampTubeJPeakAPerMm2`/`TubeJAllowAPerMm2`）、`LineRunner.cs:1936-1944`（硬判据行「① 升温」） | 同 R18，判据表通用渲染 | `DesignCurrentTests.cs`、`RequiredChecksTests`、`CriteriaTableTests` 均存在 | 无 |
| R21 三步流程 | 齐 | `MainForm.cs:182,194,201-204` 三个 TabPage：「① 输入」「② 法兰优化」「③ 结果与出图」 | `Flow.cs:381+ Stages`（`StageId` 枚举 + 门） | 页签本身 + 状态轨；一键流水线从②走到③ | `UiWiring` 阶段轨断言、`FlowDeadEndTests`、`--follow3dm` 走查 | 无 |
| R22 J 工程师设定 | 齐 | ①页「设计电流密度 J」`LineDesignPage.cs:39 _jDesign`（3–30，预设 10） | `DesignSpec.JDesignAPerMm2` → `SizeTongues`/`ApplySectionFloor`/`SectionSizing.JCheckOf`（限值=J+1）；`Solver.MeltFloor` 只判不抬 | 判据表「法兰截面 J」随 J 变；`Describe()` 印「J x（工程师设定，极限 x+1）」 | `DesignJTests.cs`（4 条方法名核对属实）、`MeltFloorTests.cs:51`、`BranchMarksAreReachedTests.cs:139`；`UiWiring:735` §16″ 实测节存在 | 无 |
| R23 舌片重定厚/场定孔/交界切口 | 齐 | 无独立控件：由既有孔径/槽张角旋钮触发（同 R7 `_holeR` 等） | `Solver.SetKnob`（每次探针重定舌片厚）、`Solver.FieldPlacement`（孔心写回/冻结）、`SectionSizing.DiscCutChordMm`（交界切口扣弦） | 「舌孔孔心 x mm」只读行 `LineDesignPage.cs:4267`；舌片厚/叉臂随之变的重量与判据表 | `TongueResizeTests.cs`（3 条方法名核对属实）、`SectionSizingTests.cs:167,184`、`FieldPlacementTests.cs:134,158` 均存在 | 无 |
| R24 搜形状结果可选 | 齐 | ②页下拉「搜形状结果 ▾」`LineDesignPage.cs:654,838` | `PickShape()` → `AdoptSolvedDesign`（写回）/ 只提示（不可行） | 下拉条目含铂重/可行性文字；选中后状态置「已选形状，待精算」 | `UiWiring:765` §16‴ 存在；`--searchshape` 走查 | 无 |
| R25 五步状态条 | 齐 | 无独立入口，随核算/自动定厚/搜形状/加密复算自动更新 | `Core/SolveStages.cs`（识别 ①②③④⑤ 前缀） | `UI/SolveStageStrip.cs`，挂在②页输出框顶 `LineDesignPage.cs:282,1052` | `SolveStagesTests.cs`；`UiWiring:797` §16⁗ 存在；截图 `deliverable/界面截图_R22_2026-09-09/02_②法兰优化.png` | 无 |
| R26 细网格重解防死循环 | 齐 | 手动分步条按钮「◆ 细网格重解（加密不过后）」`LineDesignPage.cs:297,771,848` | `Flow.cs:304,418-421,566`（`core.fineResolve` 命令 + 指路规则）、`LineDesignPage.FineResolveAsync()` | 判据表刷新、状态条到⑤；`AdoptSolvedDesign` 换快照 | `FineResolveAfterVerifyTests.cs`；`OneClickPipelineTests`/`SizerDeadLoopTests` 含新命令名；`UiWiring` §28′ | 无 |
| R27 段数输入框 | 齐 | ①页「铂金管加热段数」`NumericUpDown` `LineDesignPage.cs:278,988,991` | `SegCountChanged` ↔ 段表双向同步（`_suppressAuto` 互斥） | 段表行数随框变，输出框写「删掉了…」说明 | `UiWiring:2876` §35″ 实测 13 项、断言内容与索赔一致（非占位） | 无 |
| R28 使用说明+提示 | 齐 | 「使用说明」页新节 `ManualPage.cs:1004-1005`；按钮 `ToolTipText`：`LineDesignPage.cs:761-763`（核算整线）、`:1168-1172`（搜形状，含 .3dm 模式禁用原因） | 纯信息型，无需链路 | 说明书正文 + 悬停提示 + 输出框首行提示 | `UiWiring:2940` §35‴ 实测（说明书渲染文本、按钮 tooltip 非空、.3dm 下真禁用） | 无 |
| R29 舌根加厚带（叉臂） | **半** | 无独立入口：随孔/槽切口自动派生（设计如此，非旋钮） | `DesignSpec.SizeTongue`（带范围+带厚闭式）、`FlangePlate.TabArmX0Mm/X1Mm/TabArmThicknessMm/InTabArm/ThicknessAt` 全部确认 | **只有杆厚有专属只读框**（`LineDesignPage.cs:4201,2866-2867`「舌片厚 mm」）；叉臂厚全代码库搜索只出现在 `Describe()` 拼字符串（`DesignSpec.cs:961`）里，靠 `sb.AppendLine(fd.Describe())` 进输出框文本，**没有像槽心角/挖料形状/孔心/孔形状那样的逐片只读行**（那四个在 `LineDesignPage.cs:4265-4268`） | `TabArmBandTests.cs`、`Export3dmMatchesSolvedTests.cs` 均存在 | 叉臂厚补一个逐片只读标签（HANDOVER 原文已自认「低欠账」，本次审查核实这条欠账确实还在，一句代码级证据：`TabArmThicknessMm` 在 UI 层零引用） |
| R30 管内径进几何 | 齐 | 参数表 PropertyGrid「内径 ID [mm]」`DesignInputs.cs:125-126`，类别「管几何」 | `DesignSpec.TubeIdMm` → `DesignSpec.cs:872 BuildCase` 写 `LineCase.TubeIdMm` → 求解器/几何全线用它（`LineRunner.cs`多处、`Geometry3dm.cs:745-746`） | `Describe()` 非默认时印「内径」；出图管孔尺寸随之变 | `TubeIdIntoGeometryTests.cs`、`DesignSpecStoreTests` 样本、`TongueResizeReachableTests`「大管径」案例均存在 | 小欠账（作者自认）：载入设计记录后 PropertyGrid 那格不自动刷新，只影响显示同步，不影响求解 |
| R31 锥形舌片+孔朝向 | 齐 | ①页「舌片边」勾选框 `LineDesignPage.cs:64,948` | `DesignSpec.TabTaper`、`TangentXMm()`、`FlangePlate.TabParallel` | `_holeShape[i].Text` 派生框印朝向 `LineDesignPage.cs:189-190`；`UiWiring:831` §16⁗′ 实测切点随勾选变 | `TaperedTabTests.cs`、`UiWiring` §16⁗′ | 本行 HANDOVER 原文结尾自写「抓图待」——按项目铁律「改 UI 必须自己截图」（`ui-layout-must-screenshot.md`），这条新增勾选框还没有专门截图存档，需补 |
| R32 解法两族独立 | 齐 | ①页「解法」下拉三选一 `LineDesignPage.cs:949,951 _family` | `FamilyAllowsCuts`/`FamilyBoth` → `SolverOptions.AllowTabCuts`（`:3024,3280,3542-3556`） | 输出框并列两行对比；`Provenance` 记「解法：…」；不挖族写回页面，挖族进搜形状下拉 | `SolverFamilyTests.cs`；`UiWiring:846` §16⁗″ 实测（真断言默认值/切换后属性联动，非占位） | 无 |
| R33 .3dm 单一输入 | 齐 | ①页只画一行「法兰图纸 .3dm」`LineDesignPage.cs:4036-4043` | `PickFile()`：`:1463` 选一次填满所有片下标 | 单行文本框显示路径；逐片数组仍照旧驱动求解/出图 | `UiWiring:863` §16⁗‴ 实测 | 无 |
| R34 界面审排版9条+文案 | 齐 | 参数表枚举/布尔/铜排热导转换器 `DesignInputs.cs:10 DescribedEnumConverter`、`:32 ChineseBoolConverter`、`:42 AutoOrValueConverter` | 直接作用于 PropertyGrid 显示，无需额外链路 | 参数树中文取值；分类名不重复显示 | `ParamDisplayTests.cs` 存在；截图 `deliverable/界面截图_R34_2026-09-10/`（6 张 + 索引，已核实存在） | 无 |

## 清单

**缺**：无（R18–R34 本次审查未发现「代码找不到、只在 CLI/测试里」的行）。

**半**：
- R29 舌根加厚带（叉臂）：链路与算法都在，但叉臂厚（`TabArmThicknessMm`）没有像同批派生量（槽心角/挖料形状/孔心/孔形状）那样的逐片只读框，只能从输出框的自由文本（`Describe()`）里找到——工程师容易漏看。补法：仿照 `LineDesignPage.cs:4265-4268` 加一行「{名} 叉臂厚 mm（算出来的）」只读框。

## 附带发现（不计入判定，供参考）
- R31 完工时自己写明「抓图待」，未按项目铁律截图存档新增的锥形舌片勾选框布局，建议补拍。
- R30 完工时自认 PropertyGrid 载入记录后「内径 ID」格不自动刷新，属显示滞后，不影响求解结果，建议顺手修。

`dotnet build Pt_Optimize.sln`：0 错误，42 条无关警告，确认本表引用的全部测试文件与 UiWiring 走查节点均为真实可编译代码。
