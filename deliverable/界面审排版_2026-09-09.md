# 界面审排版 2026-09-09

| 页面 | 位置（附近文字） | 问题 | 建议 | 严重度（高=看错数或点不到／中=难读／低=美观） |
|---|---|---|---|---|
| 01 输入 | 左侧参数树「铜排总热导 [W/K]」 | 值显示为 `-1`，疑似"未设置/自动"的内部哨兵值直接漏到界面，无任何说明 | 显示为空白或"自动"，加悬浮说明 | 高 |
| 01 输入 | 左侧参数树「8 X 改了没用——由「整线核算」页上的控件决定」「9 X 改了没用——由程序自己算出来」 | 分类标题里的"X"含义不明，像遗留的内部变量名 | 改为具体中文标题，去掉"X" | 中 |
| 01 输入 | 右侧「法兰几何来源」下 `HC1\|HC2`、`HC2\|HC3` 输入框标签 | 内部代号，未注明含义（猜测是法兰分段位置） | 加中文说明，如"法兰1-法兰2 间" | 中 |
| 01 输入 | 左侧参数树「共用片抽热两侧均分」 | 值显示英文 `False`，未本地化 | 改为"否/是" | 中 |
| 01 输入 | 左侧参数树「二次电源型式」 | 值显示英文代号 `AcPhase` | 加中文名（如"交流相控"） | 中 |
| 01 输入 | 左侧参数树「安装姿态」 | 值显示英文 `Horizontal` | 改为"水平" | 低 |
| 01 输入 | 整页 | 首屏"下一步→点「核算整线」"蓝底提示醒目、位置合理，未发现问题 | — | — |
| 02 法兰优化 | 右侧「核算整线」按钮下方大段说明文字之后 | 未解算状态下右侧留白面积很大（约占页面一半），信息密度低 | 可接受（未解算前属正常状态），建议解算前放占位说明或缩小留白 | 低 |
| 02 法兰优化 | 右侧提示区"★ 舌长装不下铜排…（判据 舌片自由段）" | 用全名"舌片自由段"而非代号，未发现违规缩写；粗体"盘径或铜排尺寸"用真实加粗渲染，未见裸露 `**` 星号 | 无需改动（确认项） | — |
| 05 使用说明 | 整个右侧内容区 | 切到"使用说明"标签后，右侧只重复显示顶部状态栏（现在算的是/下一步提示/标签行），下方大片空白，未见任何操作说明正文 | 补上"使用说明"页正文内容，或确认此页确有内容但未渲染出来 | 中 |
| 05 使用说明 | 左侧参数树最下方、面板边框外 | "8 X 改了没用——由「整线核算」页上的控件决定"这行文字在树内已出现一次，面板边框下方灰底区域又重复出现同一行，像文字溢出到控件外 | 去除面板外的重复文字，排查是否为控件高度计算错误导致溢出 | 中 |
| 03 结果与出图（锁定态） | 页面顶部红底提示框 | "🔒未解锁"说明用完整中文段落写明原因与解法，未见判据代号，确认项 | 无需改动（确认项） | — |

## 汇总（2026-09-10，主开发者接手收尾：子代理看图三次断线，前 12 条是它们看 01／02／03／05 写的；03 子页与 04 子页只抽看，无新增）

按严重度：高 0、中 5、低 4、无问题记录 3。

最要紧的五条：
1. 「使用说明」页在抓图里只显示顶部状态栏与「参考工具」那排按钮（05_使用说明.png）—— 要分清是抓图工具没等页面渲染，还是页面真的空白；R28 抓图（deliverable/界面截图_R28_2026-09-10/05_使用说明.png）要再看一次。
2. ① 输入右侧 .3dm 文件行标签「HC1|HC2 .3dm」是内部片名，工程师看不出是哪一片 —— 改成「入口／HC1 与 HC2 之间的共用片／出口」这类说法。
3. 左侧参数树里的英文值（AcPhase／Horizontal／False）与哨兵值（铜排总热导 -1 = 自动）没有中文说明 —— 属性表要么本地化显示，要么在说明栏写清「-1 = 由程序算」。
4. ② 法兰优化 未解算时右侧大片空白 —— 可把「核算整线会做什么」的说明放进去，或让 ①②③④⑤ 状态条初始就说明各步。
5. 参数树分类名「8 X 改了没用」「9 X 改了没用」是给开发者看的口气 —— 改成「由核算页控件决定（此处只读）」「由程序算出（此处只读）」。

（本清单只列问题，不改代码；改法另开需求。）

## 修法（2026-09-10 晚，R34，用户「先做界面审排版那 9 条」）

| 条 | 修法 |
|---|---|
| 铜排总热导 −1 | 参数表显示「自动（程序自己算）」，说明栏首句写清填自动／留空 = 程序算、填数就用；`AutoOrValueConverter` |
| 分类 8／9 的「X 改了没用」 | 改「8 只读 —— 由「② 法兰优化」页上的控件决定（这里改了不算）」「9 只读 —— 由程序自己算出来（这里改了不算）」 |
| HC1\|HC2 .3dm 片名代号 | R33：只画一行「法兰图纸 .3dm」，各片同一张图 |
| 共用片抽热两侧均分 False | 是／否；DesignInputs 所有布尔属性都挂 `ChineseBoolConverter` |
| 二次电源型式 AcPhase | 单相交流（可控矽相控）等，按枚举 [Description]；`DescribedEnumConverter` |
| 安装姿态 Horizontal | 水平／垂直，同上 |
| ② 页未解算时大片空白 | 状态条初始行「还没跑：点核算整线后从 ① 开始，这一行跟着报每一步」；原有说明段保留 |
| 使用说明页抓图空白 | 抓图工具限制（WebView2 正文 DrawToBitmap 抓不到）；页面本身有内容，无运行时时给提示。不改 |
| 面板下方重复的分类名 | 参数表说明栏一开始选中第一项（`SelectFirstGridProperty`），显示的是那一项的说明 |
| （追加）首屏「下一步」两行与 ① 页黄底说明 | 第二行不再重复按钮名与时长，改成五步链；黄底说明写清两个入口、段数框、段表、哪些不用填 |
