# 功能落地排查 R1–R17（2026-09-11）

只读审查，未改代码。范围：HANDOVER.md「要求登记表」R1–R17。方法：逐行四问（界面入口／链路／结果可见／门），文件:行号取自当前 `feat/geom-and-flange-accounting` 分支源码，非 HANDOVER 文字自述。

| 行 | 判定 | 界面入口（文件:行） | 链路（文件:行） | 结果可见 | 门 | 缺什么／怎么补 |
|---|---|---|---|---|---|---|
| R1 段数由UI输入 | 齐 | `LineDesignPage.cs:278,988,991` `_segCount` NumericUpDown | `DesignSpec.Fit()`（各数组重排）／`LineSolver.FlangeCount` | 段表逐片控件随之增减，判据表随之变 | `SegmentCountTests.cs`（5 条，含真造页面加一行） | HANDOVER 自注「0/47」指全仓（含 CLI `Program.cs` 33 处、`Core` 12 处）硬编码 4 段假设未逐点验证；`Core` 12 处抽查（`DesignSpec.cs:93,128,448,462,523,537` 的 `new double[4]`）均是默认初值、会被 `Fit()` 重排，非死锁；CLI 不是工程师入口，不阻塞 APP 使用 |
| R2 每段管长可单独设 | 齐 | `LineDesignPage.cs:716-736` `SegRow.直接加热管长mm`（`_segGrid` 自动生成列） | `LineDesignPage.cs:2662/2686/2825→DesignSpec.SegLengthMm→LineRunner.cs:1218-1219`（供热/支承跨距/铂重） | 段长变化直接反映在铂重与判据表 | `SegLengthPerSegmentTests.cs`（真实，钉逐段独立生效） | 无 |
| R3 过热⇒加大截面且铂最省 | 半 | `LineDesignPage.cs:768` `_btnAuto`／`core.autoThick`（`1442,2157`） | `FlangeAutoSizer.SolveByLevel`（`FlangeAutoSizer.cs:831`） | 判据表＋轨迹说明加厚原因 | `OverheatDrivesSizingTests.cs`（31 条字面全钉 `SolveByLevel` 一处）＋`MeltRaisesCrossSectionTests.cs`；HANDOVER 自认 2/10 真出口 | 「不许一句话判死刑」链路已通，但「铂最省」这一半没有端到端断言（没有测试证明 APP 给出的厚度方案是候选里最省的，只证明「能算出一个可行厚度」）；补一条把 `SolveByLevel` 输出与搜形状/比价结果对照的行为门 |
| R4 t₁/t₂与r₁/r₂成对 | 齐（t=1 分支设计性休眠） | `LineDesignPage.cs:226-228,470-479` `_ringR1/_ringR2/_ringT2` | `DesignSpec`／`Solver` 环旋钮 | 判据表＋出图 | `RingShapeFreedomTests.cs`／`RingShapeInputTests.cs`（真实） | 「成对」可违反点未枚举（HANDOVER 自认「未知」）；t=1 时功能休眠是设计选择非缺陷，但没有测试钉死"何时该退出休眠" |
| R5 舌板开孔尺寸/孔径/厚度可优化 | 齐 | `LineDesignPage.cs:246,249` `_holeR/_holeAsp` 等逐片控件 | `Knob.TabHoleR` 旋钮→`Solver`→出图→存档→快照 | 判据表＋出图＋变更清单 | `TabHoleTests.cs`（7 条，含真写图再读回、真解电流场） | 无 |
| R6 孔大小形状由场定 | 半 | `LineDesignPage.cs:4265-4268` 只读行（槽心角／挖料形状／孔心／孔形状） | 大小：`Solver`旋钮；形状：`Solver.ProbeShapeFamily`比价；孔位：`Solver.cs:1862-1872` | 上述只读框 | `FieldGuidedSlotTests.cs`／`HoleRaisesCurrentDensityTests.cs`／`HolePlacementTests.cs`（真实） | 舌孔形状族因 R11 后孔径上界恒为 0，结构性到不了求解器比价（见 R13）；孔位场定此前"只印不用"，已被 R23（`Solver.cs:1869-1872`，2026-09-10）改为"未开孔片段采用场定值"，HANDOVER 原文仍停在 09-05/09-08 的旧描述——**登记表落后于代码** |
| R7 steps扩舌孔参数+UI也要有 | 齐 | `LineDesignPage.cs:238,246,249` `_slotDeg/_holeR/_holeAsp` | 写回 `DesignSpec`（`LineDesignPage.cs:2797-2802`） | 判据表＋出图 | `HalfWiredFieldTests.cs`（真实，4/12 是覆盖率口径非缺失） | 无（R5 自相矛盾的旧记录已由后续修正） |
| R8/R14 3DM读出参数化 | 齐 | `LineDesignPage.cs:783,5010` `_btnToAnalytic`／`AdoptShapeToAnalytic()`；门禁 `1417` | `ShapeToAnalytic.From(sh)` | 四个数写入 r₁/t₁/r₂/t₂ 控件、说明 | `ShapeToAnalyticTests.cs`（15 条，含闭环）；`--follow3dm` 走查 | 无（≥4 级图纸仍拒翻，属设计边界非缺陷） |
| R9 决定依据须落进APP | 半 | 无专属入口（跨全流程） | `Solver.cs:283,839,1071...` `res.Trace.Add`→`LineDesignPage.cs:3567` `_lastTrace=srD8.Trace.ToArray()`→`4748-4755` 拼进输出框 | 输出框显示求解器推理过程原句 | `CliUiParityTests.cs`／`EntryPointsTests.cs`／`SolverTraceReachesUiTests.cs`（均真实） | 机制本身通（trace 真的到了 `_out`），但"决定依据"没有一份枚举清单，分母定义不出来，HANDOVER 自认"未知"——本条是元要求，逐条功能核对需靠 R1-R34 各自验，非单一功能点 |
| R10 组合要搜出来 | 半 | 无专属入口（`ChooseKnob` 每轮自动挑） | `Solver.ChooseKnob` 多轮累积 | 最终多个旋钮同时非默认值，体现在判据表/出图 | `ComboAccumulateTests.cs`（真实，1h21m 真跑，钉跨轮累积） | 「一轮内协同」（两样一起才有效、各自单独无效）未造算例，`ChooseKnob` 仍一轮一根；孔位/离散形状尚未进候选池——HANDOVER 自认此项未做 |
| R11 舌片厚与圆盘厚解耦 | 齐 | `LineDesignPage.cs:4201` `Head("舌片厚 mm（算出来的，不是旋钮）")`，`443` `ShowTongues(d)` | `DesignSpec.SizeTongue`闭式→`Solver.ApplySectionFloor` | 只读框＋出图＋说明书剖面表 | `SectionSizingTests.cs`／`DesignSpecTongueTests.cs`／`DesignSpecStoreTests.cs`／`CloneCopiesEveryArrayTests.cs`（均真实） | 无 |
| R12 孔心/槽心位置由场逐案定 | 半 | `LineDesignPage.cs:4265,4267` `_slotCenter/_holeX` 只读框 | 槽心：`Solver.cs:1842-1850`（5/5 环全通）；孔心：`Solver.cs:1862-1872`（R23 后未开孔片段采用场定值，已开孔片冻结） | 只读框＋出图 | `UiWiring §35/35′`（真实实测回填）＋`FieldPlacementTests.cs`／`RemovalPriorityTests.cs` | HANDOVER 原文"舌孔孔心 ✗ 未接进几何"已被同仓 R23（09-10）代码改写为"部分接入"，**是登记表滞后而非代码缺失**；仍缺：已开孔片的位置不再随场重定（设计冻结，非 bug，但未在登记表说明） |
| R13 离散形状族入候选 | 半 | `LineDesignPage.cs:4266,4268` `_discShape/_holeShape` 只读框 | 圆盘：`Solver.ProbeShapeFamily`比价（6/6 环全通）；舌孔：同一比价函数，但孔径上界恒为 0（R11 后果）使该分支永不触发 | 只读框＋出图 spec `discShape/holeSides` | `ShapeFamilyTests.cs`（10 条，含真写图读回、慢门验证求解器真比价） | 舌孔形状族"结构性到不了"是 R11 的直接后果并被源码证实（`TabHoleTests`/`MinHoleRadiusTests` 显示现役构型孔径上界恒为 0）；HANDOVER 已自认，非新发现，但需在登记表标注这是"设计后果"而非"未实现" |
| R15 孔径<1mm不考虑 | 半 | `LineDesignPage.cs:1535` `Hole() => Num(0m, 0m, 30m, 1m, 2)`（`_holeR` 控件） | `DesignSpec.TabHoleRMinMm=1.0`／`TabHoleREffective` | 轨迹说明"按无孔算"；控件本身**不拦阻** | `MinHoleRadiusTests.cs`（9 条，真实，含 Core 层写入(0,1)当场炸） | Core 闭式规则齐全，但 UI 控件量程仍是连续 [0,30]，工程师能填 0.5，只在解完轨迹里被动说明"按无孔算"，没有输入层拦阻（如量程改成 {0}∪[1,30] 或失焦校验）；属于"新状态位默认没接上"同类风险，HANDOVER 自认未改，本次复核确认仍未改（`LineDesignPage.cs:1535` 未变） |
| R16 对话结论须落地APP | 半 | 无专属入口（过程性铁律，非单一功能） | §0.-3 规则 + 各功能自身门；示例 `Solver.Solve` 内 `MeltFloor` | 界面轨迹框能看到该规则的实例 | 无独立门（靠逐条功能各自的门） | 本条是项目级不变式而非可四问的单一功能，"功能清单"不存在使覆盖率无法定义；建议不再单列，改为对 R1-R34 逐条复核的常态动作（即本次审查本身） |
| R17 UI检视整理 | 齐 | `MainForm.cs:171-200` 三步页签①②③+参考工具+使用说明；`464` 手动分步展开逻辑 | 控件仍归 `LineDesignPage` 持有，`MainForm` 搬运 | 改前/改后截图 `deliverable/界面截图_改前_2026-09-08/`、`deliverable/界面截图_改后_2026-09-09/`（均已确认存在） | `UiWiring` 界面接线（★全部通过，含五页签/阶段轨/命令按钮双向对齐） | 无 |

## 缺（无）
本次 R1–R17 复核未发现"功能只在 CLI/测试里、界面完全点不到"的整条缺项（即无严格意义的"缺"）。最接近的两处风险已在半清单标注：R15 的输入层无拦阻、R9/R16 的"决定依据清单"本身不存在。

## 半（8 条，逐条一句）
- R3：加厚链路通，但"铂最省"没有端到端断言，只证明"能算出可行厚度"。
- R6：孔大小/形状已场定，孔位刚被 R23 接入（未开孔片段），HANDOVER 原文描述滞后于代码。
- R9：`res.Trace` 确实到了输出框，但"决定依据"无枚举清单，覆盖率算不出。
- R10：跨轮累积已验证，"一轮内协同搜索"（两样一起才有效）仍未做。
- R12：槽心角全链路通；舌孔孔心因 R23 已部分接入，但登记表文字仍写"未接"，需要更新。
- R13：圆盘形状族全链路通；舌孔形状族因 R11 后果孔径上界恒为 0，结构性到不了，非代码缺陷。
- R15：Core 闭式规则齐全，但 `_holeR` 控件量程仍是连续 [0,30]，无输入层拦阻，只能事后在轨迹里说明。
- R16：项目级铁律非单一功能，机制存在但没有可核对的"功能清单"，判定沿用 HANDOVER 自认。

## 意外发现（需要 HANDOVER 更新，不算新缺项）
R12/R13 登记表文字（09-08/09-09）在"舌孔孔心 ✗ 未接进几何"这一点上已被同仓更晚的 R23（2026-09-10，`Solver.cs:1862-1872`）代码修正，登记表本身没跟着改——这正是"低报与高报一样算搞混"的实例，建议随 R23 那行一并回填更新 R12/R13 的状态文字。
