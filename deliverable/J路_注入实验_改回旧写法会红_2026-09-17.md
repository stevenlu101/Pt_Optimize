# J 路注入实验：**把每一处改回旧写法，对应的门必须红**

2026-09-17，Opus 5（R48 J 路：求解器与界面安全线）

门写完了不代表门守得住 —— 一道**恒绿**的门比没有门更坏（它让人以为查过了）。
本轮 J 路落地的每一处改动，都把源码**改回改动前的写法**（或改成「这道门本该拦住」的写法），
重新编译、跑对应的门，记录它是不是真的红。

- 驱动脚本：`C:\Users\admin\AppData\Local\Temp\claude\D--WinForm-Pt-Optimize\7e065600-607b-4804-80c9-ea03a0587700\scratchpad\inject2.py`
  （`check` 逐条核「旧写法那串」在现文件里恰好出现一次 ⇒ 不会注到别处；`apply <批>`；`restore` 从整份备份**逐字节**还原并打印 md5 比对）。
- 28 条注入分 A/B/C/D 四批，每批：apply → `dotnet build Pt_Optimize.sln`（0 错误）→ 跑门 → restore（md5 逐字节相同）。
- 四批跑完后 `Pt_Optimize/Core/InsulationSearch.cs` 与基线 `git diff` 为空（该文件归 K/L 路，J 树只在注入期间临时改过、已逐字节还原）。

---

## 批 A（10 条注入 → 9 道门红）

| 注入 | 文件 | 改回的写法 | 红的门 |
|---|---|---|---|
| I1 J1 | `Core/Solver.cs` | `FinishCase` 里改回 `if (lastOpt.FineMm > 0) MeshAdapt.RefineWholeMesh(…)`（手抄 `ApplyCaseMesh` 三支里的一支） | `R48J_SolverMeshAndMarkerGateTests.J1_终局复核网格与最后一遍求根逐项相同`；`SolverFineRootTests.终局复核跑在最后一遍求根的网格上` |
| I2a J2 | `Core/FlangeAutoSizer.cs` | `CloneCase` 删掉 `JDesignAPerMm2 = c.JDesignAPerMm2` | `LineCaseCloneTests.克隆带上LineCase的全部公开字段_故意不拷的只有名单上三个`（消息：`CloneCase 漏拷：JDesignAPerMm2`） |
| I2b J2 | `UI/LineDesignPage.cs` | 图纸路径造算例不给 `JDesignAPerMm2` | `LineCaseCloneTests.图纸模式造的算例带着设定J` |
| I2c J2乙 | `Core/FlangeAutoSizer.cs` | `Solve` 删掉「底算例带解析板件却没给 makePlate」那道闸 | `R48J_SolverMeshAndMarkerGateTests.J2乙_…`（`Assert.Throws Failure: No exception was thrown`；这一跑真的把定尺寸器跑了 3 m 17 s ⇒ 非空转） |
| I3b J3 | `Core/Solver.cs` | `Gate` 删掉读 `GateWhy` 那一段 | `R48J_SolverMeshAndMarkerGateTests.J3_Gate与停机病因与安装报告接线` |
| I4a J4 | `Core/MeshVerify.cs` | 主循环删掉 `TierUnusableWhy` 那一问 | `…J4_加密复算的档可引用条件`（`主循环要在记进 Line／Trace 之前先问这一档能不能用`）；`…J4_没收敛的档判词如实` |
| I4b J4 | `UI/LineDesignPage.cs` | 已验戳改回 `res.Converged && res.Line is { Ok: true }` | `…J4_加密复算的档可引用条件` |
| I7a/b/c J7 | `Core/FlangeAutoSizer.cs` | 三个判定式改回「`Enumerable.Max` 跳过 NaN」 | `…J7_定尺寸器判定式NaN即判不了` |

**I4a 顺带复现了原病象**：注入后那一档没收敛的判词变成
`✗ **不在渐近区，不能说「数不再变了」**：最热铂高出热偶读数 变化缩得比格式能…` ——
把「外层耦合没收敛」说成了网格族的事，正是 P1-5 记的那一条。

## 批 B（10 条注入 → 9 道门红）

| 注入 | 文件 | 改回的写法 | 红的门 |
|---|---|---|---|
| I3a J3 | `Core/Solver.cs` | `PlateSlack` 删掉「本片判不了 ⇒ NaN」那一段 | `…J3_判不了标记_判据表求解器逐片裕度同一份` |
| I5a J5 | `UI/LineDesignPage.cs` | 不问 `RefineVerdict` 直接印「最轻的全过形状」并写回 | `…J5_搜形状精算结论只看Feasible` |
| I5b J5 | `Core/ShapeSearchPlan.cs` | `RefineVerdict` 改成不看 `feasible` | 同上 |
| I6 J6 | `Core/LineRunner.cs` | 整片热稳定改回「只评发热最大的一片」 | `…J6_整片热稳定逐片评取裕度最小_NaN判不了` |
| I9a P2-5 | `Core/ShellMesh.cs` | `ClampFullFaceActive` 改回「长度 = 单元数」 | `R48J_ClampFullFaceDefinitionGateTests.全假压接数组与只钉外圈逐位相同_都判未生效`；`R48RecipeFingerprintTests.a_…` |
| I9b P2-5 | `Core/ShellMesh.cs` | 生成器照写全假的 `ClampCell` 数组 | 同上 |
| I10a P2-7 | `Core/InsulationSearch.cs` | 默认评估函数热侧限值写死 5 | `R48J_InsulationSearchWiringGateTests.P2_7_默认评估函数的限值取非5的算例也跟着走` |
| I10b P2-8 | `Core/InsulationSearch.cs` | 内层网格 `MeshOpt` 不传细区半径 | `R48J_InsulationSearchWiringGateTests.P2_8_保温搜索内层网格真的带着细区半径` |
| I11a J11 | `Core/RampScreen.cs` | `TargetC` 改回 `const 1150` | `…J11_升温快筛目标温度只从LineCase来_文字照实` |
| I11b J11 | `Core/RampScreen.cs` | 判词改回「① 的交付判定由整线耦合解给出（限 72 h）」 | 同上；`RampScreenTests.ScreenCriterion_IsFindableButNotConfusableWithTheRealOne` |

## 批 C（5 条注入 → 4 道门红）

| 注入 | 文件 | 改回的写法 | 红的门 |
|---|---|---|---|
| I17 P3-11 | `UI/LineDesignPage.cs` | 「这次改了什么」不再遍历 `movedByPlate`（= 快照逐片记了、下游没人读） | `…P3_11_快照逐片记_两片对调也算改了`（**单独注入也红**，另跑一次确认：`Not found: "foreach (var (nm, x, y, u) in movedByPlat"…`） |
| I3c J3 | `Core/LineRunner.cs` | 唯一定义 `PlateUndeterminedWhy` 里删掉「压接段伸进圆盘」那一道 | `…J3_判不了标记_…`（消息：`FlangeOut.ClampIntoDisc 翻转：判据表判不了 True，唯一定义「」，求解器 Gate「」—— 三处不是同一份`）；`…J3_Gate与停机病因与安装报告接线` |
| I8 P3-11 | `UI/LineDesignPage.cs` | 快照不逐片记板厚／环倍率／槽／孔径／拉长比 | `…P3_11_快照逐片记_两片对调也算改了`（消息：`_tPlate：两片对调后快照没变`） |
| I12 P2-9 | `Core/InstallReport.cs` | 安装报告不印配方偏离那一句 | `…J3与P2_9_安装报告读判不了标记与配方偏离` |
| I13 P1-4 | `Core/InstallReport.cs` | 逐片表不读 `PlateUndeterminedWhy` | 同上 |

## 批 D（3 条注入 → 4 道门红；J8 的现状门 + 名单提公开函数的连带）

| 注入 | 文件 | 改成的写法 | 红的门 |
|---|---|---|---|
| I14 J8 | `Core/Solver.cs` | `ReclaimableKnobs` 名单里加一根板厚 | `R48J_KnobReachAndReclaimTests.J8_回收名单只有三根_另外六根退不回`；`R48TightenOnJudgeMeshTests.板厚不许进回收名单` |
| I15 J8 | `Core/Solver.cs` | 让 `Knob.Thick` 顺手写 `DiscRadiusMm`（等于「求解器接了增宽」） | `R48J_KnobReachAndReclaimTests.J8_九根旋钮都动不了盘径与舌半宽` |
| I16 J8 | `Core/Solver.cs` | 调用处改回**自己手写一份名单**（`var tunable = new (Knob K, double Lo)[]{…}`，等于又有两份配方） | `R48J_KnobReachAndReclaimTests.J8_回收名单只有三根_…`；`R48TightenOnJudgeMeshTests.二分的下端必须是约束盒下角_不是第一遍的值` |

> **顺带修好的一道旧门**：`R48TightenOnJudgeMeshTests` 的这两条原本是**手抄生产配方**的源码门
> （在 `TightenOnJudgeMesh` 的函数体文本里找 `(Knob.Insul,  opt.InsLoMm)` 那三行、找 `var tunable = new (Knob K, double Lo)[]` 那一段）。
> 名单一提成公开函数，它们当场红（2026-09-17 快套件首跑 2 红，就是这两条）——
> 正是「门不许手抄生产配方」那条规矩的实例。已改成调 `Solver.ReclaimableKnobs`（行为门），**门槛一个字没松**：
> 板厚仍然不许在名单里、下角仍然必须来自 `SolverOptions.InsLoMm／RingLo`（给 0.37／1.19 两个非缺省值验它真的带出来），
> 另加一条「调用处不许再自己写一份名单」。

> J8 这两道是**现状门**，不是「旧写法门」—— J 路这一轮对 J8 不改判定口径（理由见
> `deliverable/J路_J8_结论_旋钮不可达与回收名单_2026-09-17.md`）。它们的作用是：名单或旋钮集合一变就红，
> 逼着改的人把那份结论一起更新。

## 批 E（2026-09-17 独立复核后补，7 条注入 → 7 道门红；**一条一注入**，每条只红它那一道）

驱动脚本另起一份 `scratchpad\inject_J3.py`（同样带 `check`：七条「旧写法那串」在现文件里各命中一次）。
每条：apply → `dotnet build Pt_Optimize.sln`（0 错误）→ 跑 `R48J_SolverMeshAndMarkerGateTests` ＋ `R48J_ClampFullFaceDefinitionGateTests`（共 16 条）→ restore（两个文件 md5 逐字节 `same`）。

| 注入 | 文件 | 改回／改成的写法 | 红的门 | 失败消息 |
|---|---|---|---|---|
| N1a 必须修 1 | `Core/FlangeAutoSizer.cs` | `MinRaiseFactor` 里删掉 NaN 闸（回到 `if (hotAt(mid) <= limitC) hi = mid; else lo = mid;`） | `J7乙_加厚二分中间档算不出就判不了` | `中间档算不出（NaN）却没判「判不了」` |
| N1b 必须修 1 | `Core/FlangeAutoSizer.cs` | 生产二分改回**内联**旧写法（不调 `MinRaiseFactor`、没有判不了那一支） | 同上 | `Assert.Contains() Failure: Sub-string not found` |
| N2a 必须修 2 | `Core/LineRunner.cs` | 算不出那一支不印逐片（删 `+ RampPerPlateNote(lumped)`） | `J6丙_升温逐片附注两支都印_点名到片` | `Assert.Contains() Failure: Sub-string not found` |
| N2b 必须修 2 | `Core/LineRunner.cs` | 算不出那一支不点名是哪一片（改回原文案） | 同上 | 同上 |
| N3 应修 3 | `Core/LineRunner.cs` | `CoupleConverged` 去掉 `&& !rootNaN` | `P3_10_管根算不出不许判收敛` | `有段管根算不出还判了收敛 —— 判不了不算过` |
| N4 应修 4 | `Core/FlangeAutoSizer.cs` | 逐级那道闸改成「没给 `makePlateByLevel` 才抛」（= 交接文档原来写的那句） | `J2乙_定尺寸器底算例带解析板件时当场说` | `Assert.Throws() Failure: No exception was thrown`（这一跑真把定尺寸器跑了 35 s ⇒ 非空转） |
| N5 应修 5 | `Core/LineRunner.cs` | `FlangeLumpedAt` 的整面接触判定改回「长度 = 单元数」 | `R48J_ClampFullFaceDefinitionGateTests.全假压接数组与只钉外圈逐位相同_都判未生效` | `Assert.False() Failure` |

七次注入里，每次都是 **1 红 15 绿**；七次 restore 的两个文件 md5 全部 `same`。

> **又顺带修好一道手抄门**（与批 D 那次同型）：`OverheatDrivesSizingTests.加厚有用时取最小够用的那一档` 原本钉的是生产那一行的源码文本
> `if (HotAt(mid) <= opt.OverheatRaiseFromC) hi = mid; else lo = mid;`。复核要求在二分里加 NaN 闸 ⇒ 二分提成公开函数 `FlangeAutoSizer.MinRaiseFactor`，
> 这条当场红（2026-09-17 快套件首跑 1 红即此）。已改成行为门 ＋ 接线源码门，**门槛只高不低**：取的仍必须是「最小够用的那一档」
> （×2 上界、≥×1.2 才凉 ⇒ 必须取到 ×1.25，既够用又不比最小够用的厚出一格），另加原来没有的一条「中间档算不出时不许给档」。

> ⚠ 收尾那一跑的顺序坑（记一笔）：本节是在「跑前备份受追踪 deliverable」之后才补写的，
> 于是跑后的还原脚本把本文件当成「被套件改写」一起还原了一次 —— 这几行是还原之后重新补上的。
> 下次：先写文档再备份，或把自己正在改的文档排除在备份清单外。

—— 2026-09-17，Opus 5

---

## 还原核对

每批 restore 都打印了备份与还原后文件的 md5，四批全部 `same`；最后 `inject2.py check` 28 条全 `OK`
（旧写法那串在现文件里都不存在了 ⇒ 没有残留注入），`dotnet build` 0 错误。
