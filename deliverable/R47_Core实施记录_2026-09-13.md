# R47 Core 实施记录（2026-09-13，工作树 Pt_Optimize_r47，分支 feat/r47-drawing-path-dip）

工单：`deliverable/R47_图纸路径增量温降_工单_2026-09-13.md`。本记录只写 Core 侧做了什么、量到什么；UI（B 的界面、C 的页面接线、存档兼容）不在本记录。

## 1. 做了什么

| 项 | 改动 | 依据 |
|---|---|---|
| A+E 轴 | `FlangeMesher.GradedAxisCentered`：从管轴 0 出发向 +max／−min 各铺一条，起步 hPrev = 0 所在带的 H，末格 min(h, 余量) 贴边，拼成单调轴。`Build` 与 `BuildFromField` 都用它 | 工单 §2 A+E；实测旧轴 z 起步 8.46 mm（MeshAxisTests 里留了对照断言） |
| A 包络 | `ThicknessField.XMinMaterial/…`（精确材料包络）+ `MaterialEnvelope()`（NaN 时退回栅格 t>0 包络并写 Warning）+ `WithThickness()`；`Geometry3dm.LoadThickness` 从子进程新输出的几何包围盒填（`Pt_Optimize.Geom` thickness 模式加 xMinMaterial…四项）；`FlangeMesher.Rasterize` 从解析板填 | 工单 §2 A；隔离实验 V8a（留白 2→0 抽热 26.4→4.01 W） |
| **A 追加：两条路合成一份生成器** | `Build(g) = BuildFromField(Rasterize(g, 最细网格/4))`；`BuildFromField` 的覆盖率与厚度改成对栅格方格做**面积积分**（每个栅格节点代表以它为心、边长 = 步长的方格），不再用 4×4 子采样点查最近邻 | 同一套轴之后两条路仍差 1.8 W／电阻差 7 %（`R47_网格诊断_2026-09-13.txt` 第一版）：栅格步 = 网格/4 时每个子采样点都落在两个栅格节点正中间，平局由 Math.Round 偶数规则裁决，把舌片直边外一排格子判成有料（覆盖率恰 0.25）。面积积分没有采样点、没有平局；厚度 = 体积积分 ÷ 覆盖面积，焊脚这类比格子细的堆料按料算（原形心取厚在 2 mm 格上几乎量不到 1 mm 焊脚）。合成一份后「两条路对得上」由构造保证：同一解析板两条路**逐位相同**（三档倍率都是） |
| D 内带 | `BuildFromField(… hInner, innerRadius, twoTabs)`；LineRunner 图纸分支传 `c.MeshInnerMm/MeshInnerRadiusMm`；栅格步 = min(ThicknessStepMm, 最细网格/4)（`FlangeMesher.RasterStepFor` 只此一处）；h < 步长时 Warning 写「已到图纸分辨率」，Warning 进 `LineResult.Notes` | 工单 §2 D；V4b |
| B Core | `LineCase.FlangeFields`（内存厚度场入口，优先级 解析 > 内存场 > 文件）；`TabInsul3dmPerPlateMm` 逐片 + `TabInsul3dmAt(j)`，旧标量 `TabInsul3dmMm` 改成属性（读 = 第 0 片，写 = 同值填所有片）；`GeomForJudgeAt(j)`（短了用最后一份）；insulBoundaryX／tabBoundaryX 逐片取 GeomForJudge[j] → 没有则 `LineRunner.TangentFromField`（舌半宽 w、盘半径 R ⇒ x = −√(R²−w²)，等宽舌几何，附注说明会偏）→ 推不出则 `MarkUndeterminedIfInsulBoundaryUnknown` 把 ②′ 与 ③ 判成无法判定并附注「图纸没有切点，保温分界判不了」；**不再 new FlangePlate()**。`FlangeAutoSizer.CloneCase` 带上新字段 | 工单 §2 B |
| C Core | `MeshVerify.Run(Func<double hMid,double hInner,LineCase>, h0, radius, innerR, …)` 工厂重载，DesignSpec 重载改成调它；`Run(PlateShapeAnalyzer.Shape, wallMm, factory, …)` 图纸入口；`RequiredMeshFor(Shape, wallMm)` 特征 = 环宽（盘半径−孔半径）+ 宽于 3 个栅格步的各级径向宽 + 槽径向宽（焊脚可选），取不到返回 Refused 不抛；`Shape.StepMm` 新增 | 工单 §2 C；第一版没设 3 步地板时焊缝凹圆弧被切成 0.006 mm 宽的「级」⇒ 起始网格 0.002 mm，内存吃光 |
| F 自检 | `FlangeMesher.IsHoleFace`（三份 TagHole 判定收成一份，口径 3 mm 不变）；`ShellMesh.HoleRadiusMm/HoleTagMaxROverMm/HoleTagLengthBeyondMm`；LineRunner 写进 `FlangeOut.HoleTagOverMm/HoleTagBeyondWeldMm`，「②′管孔净流入」附注加「管孔定温环吃到孔边以外 x mm」，不改判定 | 工单 §2 F |

## 2. 改前／改后（导航网格，`R47_改前_…txt` vs `R47_改后_…txt`）

| 设计 | 量 | 改前 | 改后 |
|---|---|---|---|
| Builtin[0]（三段） | ③ 最差 | 4.718 K（HC3）✓ | 17.480 K（HC1）✗ |
| | 抽热 片0/1/2/3 W | 1.583 / 1.540 / 1.124 / 1.974 | 7.441 / 6.337 / 5.324 / 6.885 |
| | 法兰铂重 g | 1082.6（合计 3547.4） | 1080.4（合计 3545.2） |
| | J 峰 片0/1/2/3 | 34.75 / 20.65 / 19.75 / 35.76 | 34.69 / 20.61 / 19.71 / 35.88 |
| | 网格 | 676 格 | 976 格 |
| Builtin[1] W06（三段） | ③ 最差／抽热最小／合计 | 记录值 6.124 K／0.820 W／2656 g（改前没单独量，取档里的记录值） | 17.077 K／5.814 W／2655.9 g |
| 盘Ø56 记录（两段） | ③ 最差 | 9.303 K（HC2）✓ | 11.259 K（HC2）✗ |
| | 抽热 片0/1/2 W | 3.205 / 3.410 / 3.639 | 4.286 / 4.267 / 3.999 |
| | 法兰铂重 g | 960.2（合计 2603.4） | 961.1（合计 2604.3） |
| | J 峰 片0/1/2 | 14.94 / 13.85 / 12.72 | 16.05 / 14.88 / 13.67 |
| | 网格 | 583 格 | 856 格 |

**改前／改后的数怎么读（R47 复修 M6 改口：不写「新数是对的」）**：`R47_网格诊断_2026-09-13.txt` 对同一片同一输入把精确几何生成器（4×4 子采样，铺在新轴上）与栅格积分生成器各加密到 ×0.5、×0.25：
- Builtin[0] 片 0：精确几何 13.258 → 12.746 → 13.744 W；栅格积分（Build）12.454 → 12.660 → 13.754 W。两条生成器在同一套轴上对得上（导航网格差 0.8 W、×0.25 差 0.01 W）。
- 盘Ø56 片 1：精确几何 4.101 → −3.443 → −0.231 W；栅格积分 4.996 → −3.250 → −0.263 W。这一片抽热本身接近 0，导航网格上两条生成器都在 ±5 W 里晃（4.1／5.0 → −3.4／−3.3 → −0.2／−0.3）⇒ **盘Ø56 记录的导航网格 ③（11.259 K）仍不可信**，以加密复算为准。
- Builtin[0] 三段整线：导航网格 17.480 K；新生成器加密到 0.125 mm 为 20.48 K，**中带确认没过、未收敛**（审查实测 2026-09-13）⇒ 修网格后本档还没有一个算准了的数，只知道它不过（上限 10）。
- 档里的旧复核值 10.329 K（0.250 mm）是老轴上量的：老轴从端点起铺、管孔 −z 半边比 +z 半边粗 4 倍（MeshAxisTests 里留了对照：老轴第一格 8.46 mm），那次复核**无效**，留着只作历史。旧导航网格的 4.720／1.583 W 是老轴 + 子采样平局上的数，同样不可复现。

⇒ 两个基线设计与盘Ø56 记录在修好的网格上都**不过**（W08／W06 的 `VerifiedNote`／`Binding` 已按这个口径改：修网格前／修好后两组数并列，注明本档不过）。要不要重新解由主线定。

## 3. 门

- 快门：855 条（含慢），快门 819 条**全过**（第一次跑唯一红 = HandoverGateCountTests 钉测试数 840 → 已同步改 855）。**没有一条钉数的旧门因网格变化而红**：解析路径的回归数门（SolveCostRegression、Verification 等）都没有钉导航网格上的判据值。
- 六个 deliverable 记录文件（孔位对比、孔形对比、开孔的电流代价、形状族_对比、按场开槽_效果、移除优先级）由快门里的仪器测试重写，数随网格变 —— 那是记录文件，不是门。
- 慢门（本次跑过）：DrawingPathParityTests ×2 ✓、TabInsulPerPlateTests ✓、MeshVerifyLineCaseTests 真解 ✓、两个仪器 ✓。FieldConvergenceGateTests／BestShapeFineMeshTests／Pt_Topo AppJudgeTests **没跑**（工单慢门，留给合并前）。
- 走查 UiWiring 第一次跑 3 项红，全是**钉数**：「页面路径复现设计记录 ③」（靶 4.720 K，得 17.480）、「…②′」（靶 1.123 W，得 5.324）、「除法兰截面 J 外全过」（③ 17.5/10 不过）。处置：
  - 档 W08／W06 的记录值**不改**（那是历史记录），在 `VerifiedNote` 里追加 R47 一段，说明记录值是修网格之前的导航网格上的数、修好后是多少、本档不过；
  - UiWiring 「复现设计记录」的靶改成 R47 之后的数（17.480／5.324／−0.248／3545.2，带日期与依据的注释）—— 这一节验的是页面路径与整线链落到同一个解；
  - 「除法兰截面 J 外全过」改成「除法兰截面 J 与法兰增量温降外全过」，指名放行并写明依据（档自己的复核值 10.329/10 早就不过，旧导航网格 4.720 是低报）；③ 的数仍由「复现设计记录 ③」钉着（容差 1 K）。
- 第二次走查 ★ 全部通过（EXIT 0）；`--cli --uishot` 20 张 PNG 正常生成（界面本次没改，图只做存在性核对）。

## 4. 没做（不在 Core 范围或工单说明不动）
- B 的界面（逐片舌保温表、`_tabIns3dm` 去掉、走查节 Map3dm）、存档 fd.json 逐片舌保温读写与旧档兼容。
- C 的页面接线（`VerifyMeshAsync` 改用工厂重载、`FineResolveAsync` 的 fineMm 真传、CurrentSnap）。
- Pt_Topo 侧：`DesignDomain.ToField` 造的场没有精确包络 ⇒ 走栅格包络退路（会在 Warning 里说）；AppJudge 反射塞缓存那条路不动。

## 5. 复修（2026-09-13，三路反方审查的「要改」项，编号照审查）

| 编号 | 改了什么 | 依据 | 门 |
|---|---|---|---|
| M1 轴锚点 | `GradedAxisCentered(…, anchors)`：几何锚点必须落成节点（铺到锚点前把步长收到锚点上，锚点之后按原本想要的步长继续，不让短格拖小后面的格）；`Build` 传解析板的锚点（`FlangeMesher.AnchorsOf`：z ±舌端半宽／±延长段半宽／±切点半宽，x 切点／舌端）；`BuildFromField` 没传就从厚度场推（`AnchorsFromField` ← `FlangeMesher.TangentFromField`，`LineRunner.TangentFromField` 转调，推法只有一份） | 舌半宽 < 盘半径时 z=±w 落在格子中间又是「面积折了面长没折」 | `MeshAxisTests.轴锚点_必须落成节点_单调_对称锚点给对称轴`、`舌半宽等于盘半径减5的板_Build的z节点含正负半宽_x节点含切点与舌尖`（舌片段格子覆盖面积 = 整格）；慢仪器 `量舌半宽25与22p7的盘56片1导航网格与加密抽热` → `R47_复修M1_舌半宽锚点_2026-09-13.txt` |
| M2 探针漏料 | `Pt_Optimize.Geom` thickness 模式：采样点落在包围盒面上（|x−bb.Min.X|<tol 等）向内挪 **5 个容差**再打射线。挪 1 个容差不够：CurveBrep 把离面一个容差以内的点也算交点，z=+30 整排仍无料、z=−30 一排厚度 0.22～0.39（配对配错） | 实测 入口 层步 1／0.5／0.25 栅格面积 7550／7636／7679 mm² 随步长变；修后 7641／7682／7703 | `RealDrawingParityTests.真图纸入口层与解析板同输入…`：网格面积 7706.9 vs 7709.1 mm²（差 0.029 %）、抽热 12.413 vs 12.454 W、发热 1087.2 vs 1087.5 W（修前 0.836 %／10.779 W／1076.6 W —— 差 1.7 W）；仪器 `仪器_真图纸与解析板栅格逐点差在哪` → `R47_真图纸逐点差_2026-09-13.txt`（修后只差环级分界与盘外缘各几个点，1.5 + 1.0 mm²） |
| M3 Warning 越滚越长 | LineRunner 不再改 `tf.Warning`；「已到图纸分辨率」只写进本次 `res.Notes`（按片去重） | tf 是共享实例（LoadThickness 缓存／FlangeFields） | `DrawingResolutionNoteTests.同一场跑三次Run_该句每次只出现一次_共享的Warning不变`（两片各一句、原有警告带出、Warning 长度不变、同一实例） |
| M4 栅格步地板 | `FlangeMesher.RasterStepFloorMm = 0.05`、`RasterStepForFile = max(网格/4, 0.05)`、`RasterAtFloor`；文件路径用它，到地板才写「已到图纸分辨率」；内存场栅格步是给定的，网格比它细同样算到了分辨率 | 探针耗时 ∝ 步⁻²：实测 入口 层步 1／0.5／0.25 各 6.7／8.6／15.8 s（约 5 s 起步 + 65 µs/点）；MeshVerify 三档 0.322／0.161／0.081 mm 各 12.0／29.6／99.8 s（101612／399620／1584135 点）⇒ 0.05 mm 约 4.1 M 点 ≈ 5 分钟一片，0.025 mm 就要 20 分钟 | `DrawingResolutionNoteTests.文件路径栅格步有地板…`；慢门 `RealDrawingParityTests.真图纸走加密复算至少三档不抛_量每档探针用时与点数` → `R47_真图纸加密复算_2026-09-13.txt`（1 段两片：1.288 mm 2550 格 ③ 17.025 K → 0.644 mm 6624 格 18.096 K → 0.322 mm 19776 格 19.310 K，未收敛；中带确认通过） |
| M5 验收门不同义反复 | `R47MeshDiagInstrumentTests.BuildOldAxis` 改成 `BuildExactOnNewAxis`（精确几何生成器铺在新轴 + 锚点上），`DrawingPathParityTests` 三方对拍：精确几何 vs Build（栅格积分）vs 图纸路径，两个案例（盘Ø56 片 1、Builtin[0] 片 0）× 倍率 1／0.5；图纸侧再加栅格步 0.1（<1 W）与生产旧默认 1.0（记录并自证不够） | 审查量出的数在上面 §2 | 8 条慢门全过；1.0 mm 栅格在盘Ø56 片 1（焊脚 1.02 mm）上偏 1.803 W（2.298 vs 4.101）—— 这正是生产口径改成「网格/4」的依据，门钉「1.0 偏 > 1 W 且比网格/4 差」 → `R47_复修M5_栅格步对拍_2026-09-13.txt` |
| M6 改数依据改口 | DesignSpec W08／W06 `VerifiedNote`、`Binding`（全名、修网格前／修好后并列、注明不过、旧复核值无效）；UiWiring 16 节注释；本记录 §2；HANDOVER R47 行 | 见 §2 | 走查 3 节说明书判据表读 DesignSpec（无判据代号） |
| M7 细网格重解跨模式 | `CommandApplicable("core.fineResolve")` 与 `FineResolveAsync` 入口都加 `Equals(_verifiedSnap, CurrentSnap())`；ToolTip 说「参数（含几何来源／图纸文件）动过了」 | 解析模式复核完切到图纸模式，_meshVerify 仍 Converged | 走查 37 节（切模式后复核快照 ≠ 当前快照、不适用、ToolTip 点名几何来源、两处源码门） |
| M8 说明书 §2.9 | 「舌保温｜与解析模式共用「① 输入」页的逐片舌保温表，逐片可不同，0.3–80 mm」；新行「加密复算／细网格重解｜复核用的就是这张图纸，要先「分析几何变数」；没分析过按钮灰着，鼠标停上去会说明理由」 | 表的上下界 = `SizerOptions.InsLoMmConst/InsHiMmConst` = 0.3／80 | 走查 37 节钉这两句、且「默认 0 = 裸舌」不再出现 |
| M9 载入设计记录 | `LoadDesignSpecFrom`：GeomSource = 图纸 ⇒ 切到 .3dm 模式（放在灌板厚之前 —— 切模式会把 _tPlate 与 _keepOther 整组交换）、逐片回填文件名；文件不存在 ⇒ 「★ 图纸找不到，本档不能复现」且末句不给复现；`DesignSpecStore` 读图纸档时 Notes 加 `DesignSpec.DrawingRecordNote`；另存盖几何来源抽成 `StampGeomSource`（走查往返门走同一条路） | 工单 §2 B 存档 | 走查 37 节（载入切模式／文件名回填／读档说明／找不到明说／另存 → 读回 → 载入 模式与文件名对） |
| M10 真门 | `TabInsulPerPlateTests.推不出切点的场_两条判据无法判定_不用默认板`（半个圆盘、材料全在 x ≥ 0 喂 LineRunner.Run：两条 Undetermined、Ok=false、Note 含「保温分界判不了」、FlangeOut.InsulBoundaryUndetermined）；源码门 `源码门_LineRunner不再new默认板`（代码行不含 `new FlangePlate()`）；④ 强度那处只读管的利用率，改传真板或 `Mechanics.TubeOnlyPlate` | 工单 §2 B | 两条快门 |
| M11 HANDOVER | R47 行补：生成器已换成栅格积分（与精确几何差 ≤0.9 W）、图纸模式无裸舌、Pt_Topo AppJudge 反射塞缓存那条路会因栅格步键不命中而失败、三个设计不过的实情 | — | HandoverGateCountTests 计数 |
| M12 顺手 | 「此前显示的是导航网格（{lc.MeshFineMm} mm）」（`LineResult.MeshFineMm` 由 LineRunner 填）；3dm 模式厚度组标题「法兰厚度倍数 k（图纸整体 ×k，1.0 = 原尺寸；…）」（`_tPlateHead`，SyncGeomSource 切字）。**没做**：MeshVerify「按上一档格数 ×3.4 预估超上限先停」—— W06 档的复核是在 0.125 mm／47884 格（> 上限 40000）那一档收敛的，现行规则是「跑完这一档再停」，预估先停会让那类设计永远收不到，要先定上限的口径；「内带 z 向收到 min(innerR, 盘半径)」—— 轴范围已经是 ±盘半径，带超出范围不改任何节点，改了没有效果 | | |

### M1 的数（`R47_复修M1_舌半宽锚点_2026-09-13.txt`，盘Ø56 片 1，meshcmp 口径）

| 舌半宽 | 审查实测改前（导航 → ×0.25） | 改后（导航 → ×0.25） | 发热 |
|---|---|---|---|
| 25 | −29.1 → −57.5 W | −49.083 → −56.549 W（810／10526 格） | 574.0／604.5 W |
| 22.7 | −63.7 → −97.3 W | −94.351 → −105.529 W（754／9744 格） | 643.2／684.6 W |

导航网格与 ×0.25 之差从 28／34 W 收到 7.5／11 W。数本身是负的（法兰往管里灌热）是窄舌的物理：舌 56 → 50 → 45.4 mm 电阻 +12 %／+23 %，发热 511 → 574 → 643 W，多出来的热保温 2.8 mm 的舌片散不掉、铜排只带 208 W，只能进管子。这不是网格病，导航网格上仍偏 7～11 W 是 ±5 W 那类分辨率问题，加密复算才作数。

### 真图纸（`设计记录_管壁0.8mm.3dm`「入口」层）vs 解析板 Builtin[0] 片 0

| | 修 M2 前 | 修 M2 后 | 解析板 |
|---|---|---|---|
| 网格面积 mm² | 7644.7（差 0.836 %） | 7706.9（差 0.029 %） | 7709.1 |
| 抽热 W | 10.779 | 12.413 | 12.454 |
| 发热 W | 1076.6 | 1087.2 | 1087.5 |

### 复修的门（2026-09-13 实跑）

- 编译 `dotnet build Pt_Optimize.sln -c Release` 0 错；快门 `dotnet test --filter "速度!=慢"` **830/830 全过**（4 m 57 s；总数 874 含慢，HANDOVER 计数句已同步）；走查 UiWiring **★ 全部通过**（含新 37 节，两次实跑）；`--uishot` → `deliverable/界面截图_R47复修_2026-09-13/`（① 页图纸模式那张能看到「法兰厚度倍数 k（图纸整体 ×k，1.0 = 原尺寸…）」与「舌保温：用下面「逐片舌保温 mm」那张表」；使用说明页那张只有工具条 —— WebView2 的内容不进位图，与 R47 那次抓图同样 239064 字节，说明书两句由走查 37 节钉）。
- 慢门：DrawingPathParityTests 6/6 ✓、RealDrawingParityTests 3/3 ✓（含 MeshVerify 真图纸三档）、TabInsulPerPlateTests ✓、MeshAxisTests 慢仪器 ✓、R47MeshDiagInstrumentTests ✓、R47NavGridInstrumentTests 复跑（M1 之后三个设计导航网格数逐位不变：17.480／17.077／11.259 K —— 它们的舌半宽 = 盘半径，锚点与端点重合）、FieldConvergenceGateTests 13/13 ✓；BestShapeFineMeshTests 起跑后未在本次收工前跑完（日志见 scratchpad slow_gates2.log）。

## 6. 第三轮（2026-09-13，复审的「要改」与「建议」N1–N8）

| 编号 | 改了什么 | 依据 | 门 |
|---|---|---|---|
| N1 图纸路径锚点推法 | `FlangeMesher.TangentFromField`：舌半宽 = **直边段**的材料半宽（逐列取半宽到栅格，同一值**连续**出现最长的那段；跨度并列取出现次数多的），不再取「舌尖第一列有料的半宽」；切点 x = 反推值 −√(R²−w²) 与「半宽开始超过 w 的列」互相印证（max(3 mm, 6 栅格步) 内一致取反推值，不一致取实测列并在附注说明）。两种推法为什么换写在注释里；`AnchorsFromField` 与 LineRunner 的保温分界都走它（推法仍只有一份） | 舌尖倒角／圆角／收窄时第一列只剩中间一小段有料，半宽取小 ⇒ z 锚点落不到直边上（面积折了面长没折复发）；直边段（≥ 压接 40 + 自由段 100 mm）一定是最长的定值段，盘的圆弧半宽逐列都在变 | `MeshAxisTests.舌尖倒角3mm的场_锚点从直边段推得w25与切点_抽热与解析板差小于1W`（快）：盘 R28／舌半宽 25 栅格化（步 0.5、留白 2）后舌尖切 3 mm 倒角 —— **旧推法 w 22.0／xT −17.32；直边段推法 w 25.0／xT −12.61（真 −12.61）**；抽热 解析板 −49.083 W（810 格）／倒角场·直边段锚点 −49.083 W（808 格，差 0.000）／倒角场·旧锚点 ±22 −28.634 W（806 格，**差 +20.449 W**，与审查实测 20 W 同）→ `deliverable/R47_第三轮N1_倒角实验_2026-09-13.txt` |
| N2 参考行也标无法判定 | 吃法兰场的判据名单收成一份 `LineRunner.dependsOnFlangeFields`（只读出口 `DependsOnFlangeFields`），`MarkUndeterminedIfFieldsFailed` 与 `MarkUndeterminedIfInsulBoundaryUnknown` 都读它；推不出切点时法兰最高温−管温／自给率／热平衡残差／圆盘区最高温／热收支等全部 Undetermined、Ok=false、Note 说「保温分界判不了」 | 保温分界判不了 ⇒ 这片法兰的场就不是这个设计的场，所有吃场的行都不可信；名单两份会「补一条漏一条」 | `TabInsulPerPlateTests.推不出切点的场_两条判据无法判定_不用默认板` 扩成按名单逐条断言（≥6 条命中、管 J 不被顺手标掉）；`FieldConvergenceGateTests.不该标的没被标进去` 源码门按原名字仍找得到名单本体 |
| N3 发丝格 | `GradedAxisCentered`：锚点落在计划节点 s+h 之后、离它不到 hWant/4 时把那个节点挪到锚点上（这一格最多 1.25·hWant，不插新节点）；落在计划节点之前照旧收短 | 原来先放 s+h 再收到锚点 ⇒ 0.02 mm 的发丝格（锚点 2.02／−16.005），单元长宽比几百 | `MeshAxisTests.锚点离计划节点不到四分之一步_节点挪到锚点上_不造发丝格`：锚点 2.02／−16.005，**最小格 1.980 mm ≥ hWant/4 = 0.5**，节点单调、锚点仍是节点、2.0／−16.0 不再是节点 |
| N4 验收门口径 | `DrawingPathParityTests` 注释写明导航网格上 0.9 W 是两种生成器的方法差（面积积分 vs 4×4 子采样 + 形心取厚），随倍率收敛（×0.5 0.19 W、×0.25 0.03 W）；门改成「倍率 1 差 < 1 W 且倍率 0.5 差 < 0.3 W」（Build vs 精确几何、图纸 vs 精确几何两处同一把尺） | 只钉 ×1 抓不到「差不收敛」 | 慢门 6/6 实跑 ✓（数见下） |
| N5 图纸模式另存 | `DesignSpec.ThicknessScale`（逐片 k，Fit／Clone 带上）、`IsDrawingRecord`、`DrawingRefusal`；`PageToDesignSpec` 在 .3dm 模式把 k 写 ThicknessScale、TabThickMm 写 NaN、当场盖几何来源；`DesignSpecStore` 写 `thicknessScale`、`tabThickMm` NaN↔null（R47 第二轮之前把 k 记在板厚栏的旧图纸档读回按 k 接、板厚改 NaN、Notes 说明）；`DesignSpec.BuildCase` 读到图纸档返回带 `LineCase.RefusedWhy` 的算例，`LineRunner.Run` 原句返回 Ok=false 不算不抛；CLI `--judge` 先印拒绝句退出；`MeshVerify.Run(DesignSpec)` 拒答（Converged=false、Verdict 原句）；说明书 `ManualPage.BuildHtml` 读到图纸档印拒绝句、图 2～4 与逐片板厚表略去、判据表「圆盘盖得住管孔」那行印「判不了（图纸档没有板厚）」；`fd.Describe()` 图纸档印「厚度倍数 k」；`LoadDesignSpecFrom` 图纸档灌 k（没记 k 的旧档 1.0），解析档板厚 NaN 时不动控件；配套清单／安装报告图纸档印「按图纸 ×k」不印 NaN | 此前 k 当毫米进档，读回是一片「板厚 1.00」的解析板，BuildCase 真会照它造解析法兰去算 —— 算的是另一个零件 | `DesignSpecStoreTests.RoundTrip_图纸档厚度倍数k存读逐字_板厚为NaN_拒绝造解析板_解析档不受影响`（快；豁免名单加 ThicknessScale 并写明理由）；走查 37 节：真 k 值往返（页面 k → 档 → 读回 → 载入 k 框逐字）、BuildCase／LineRunner／MeshVerify／说明书四处拒绝 |
| N6 说明书字面 ** | `ManualPage` 首段「咬住它的」过 `Md()`；`Criteria.Html` 「它在管什么」列 `**` 转 `<b>`（`Criteria.BoldHtml`）；grep 整个 UI：ToolTip（`Row()` 两处改 `TextFmt.Strip`、「请先选好入口 .3dm」）与 MessageBox（另存参数动过／参数动过提示／再跑一次／导出可回读 3DM 四处）去掉 `**`；其余印到界面的字串都经 `TextFmt`／`Plain()`；判据代号在界面字串里没有（只在注释与 `LineResult.Key` 常量） | ToolTip／MessageBox 没有富文本，`**` 会原样印出来 | `DocRefTests.ManualPage_BuildHtml_输出不含字面星号`（四档内置 + 图纸档，且图纸档无 NaN）；走查 3 节「说明书全文不含字面 **」 |
| N7 换图纸清分析结果 | `_shapeKey`（分析时记「文件\|图层」）+ `InvalidateShapeIfDrawingChanged()`：键不符就清 `_shape/_levels/_levelScale/_surrFid`、`SyncAnalysisPending`、输出框说明；三个入口都调：`PickFile`、`LoadDesignSpecFrom` 回填、文件框／图层框 TextChanged | 分析 A 后换成 B，加密复算与 GeomForJudge 会拿 A 的分析算 B 的图 | 走查 37 节：分析 A 后载入图纸 B ⇒ 分析结果清空、GeomForJudge 为空、加密复算不适用、「分析几何变数」重新亮起且 ToolTip 点名；文件框改字／换图层也清；同一张图不清（对照）；源码门 PickFile 与 TextChanged 接线 |
| N8 W08 Binding | 「5.2/10」改成与 `FlangeDipK` 同一个数 4.720/10，注释写明 5.2 是 2026-08-28 基线换收敛口径之前的 5.182；W06 的 6.124 与 FlangeDipK 一致，不动 | 档自己不许说两个数 | 走查 3 节说明书读 W08 |

### 第三轮的门（2026-09-13 实跑）

- 编译 `dotnet build Pt_Optimize.sln -c Release` 0 错；快门 `dotnet test --filter "速度!=慢"`：第一次跑 832/834，两条红都是钉数／钉名字（HandoverGateCountTests 计数 874 → 878；FieldConvergenceGateTests 源码门找 `string[] dependsOnFlangeFields`，我把字段改了名 —— 改回原名、只加只读出口，测试不动），改后这两条 + 新门复跑全过（总数 878 含慢）。
- 慢门 `DrawingPathParityTests` 6/6 ✓（N4 新口径：×0.5 差 < 0.3 W）。
- 快门终跑（ShowTongues 图纸档 NaN 守卫补上之后）：**834/834 全过**（5 m 30 s；总数 878 含慢，HANDOVER 计数句已同步：+4 = MeshAxisTests 2 快 + DesignSpecStoreTests 1 快 + DocRefTests 1 快）。
- 走查 UiWiring：第一次跑在 37 节「载入读回的图纸档」处崩（`ShowTongues` 拿 NaN 板厚转 decimal 溢出 —— 正是 N5 把板厚栏改 NaN 之后新暴露的路），加守卫后第二次跑 **★ 全部通过**（EXIT 0，587 项 ✓；3 节「说明书全文不含字面 **」、37 节 N5／N7 各项都在）。
- `--uishot` → `deliverable/界面截图_R47第三轮_2026-09-13/`（24 张 + 索引），① 页图纸模式那张（01x）用 Read 看过：「法兰厚度倍数 k（图纸整体 ×k，1.0 = 原尺寸…）」组标题与逐片 k 框、「舌保温：用下面「逐片舌保温 mm」那张表」在，没有判据代号／开关名／星号；使用说明页那张（05）仍只有工具条（WebView2 的内容不进位图，与前两次同），说明书内容由走查 3／37 节与 DocRefTests 钉。
- 没做：Pt_Topo 侧不动（铁律）；`FieldConvergenceGateTests`／`BestShapeFineMeshTests` 等其他慢门本轮没重跑（本轮改动不碰它们的路，DrawingPathParityTests 6 慢实跑过）。
