═══ 单次成本分解（0.8 档，导航网格）═══
DesignSpec.Plate()　0.10 µs/次（20000 次共 2 ms）
  ⇒ 耦合解最坏 600 轮 × 4 片 = 2400 次 ⇒ 0.2 ms/场解
Solver.Eval() 第 1 次　40.7 s（收敛 True）
Solver.Eval() 第 2 次　37.1 s

对照：轨迹实测 72 次场解 / 76.0 分钟 ⇒ 63 s/次
⇒ 若 Plate() 那一项只有几十 µs，它不是原因；
  单次 Eval 若本来就是几十秒，那 60 分钟的对帐预算**从来就不够**，
  「超时」拦的是预算，不是退化 —— 预算该按实测重定，并且要打印实际耗时。
