# 搜形状 Core 驱动：实施记录（2026-09-23）

工作树：`wtSHAPE`（会话暂存区），基于 07644dc，未提交。平台：Linux 镜像（预跑），Windows 待重录。

起因：业主 2026-09-23 21:0x 原话「让搜形状里面这几套算法真正跑起来，全力攻关跑通计算的优化方案」；决 102 定为「先做甲最小版」（`deliverable/R48_决102_出解路线_选项与代价_2026-09-23.md` §1.1、选项甲）。本次只做今夜能在 Linux 上对 W08／W06 挂机实跑的最小版：不改 Solver、MeshVerify、界面。

## 1 改了哪些文件

| 文件 | 性质 |
|---|---|
| `Pt_Optimize/Core/ShapeSearchDriver.cs` | 新建。`ShapeSearchOptions`、`HardCritValue`、`ShapeRow`、`ShapeSearchResult`、`ShapeSearchDriver.Run／SchemeCard／NoFeasibleReport` |
| `Pt_Optimize.Tests/R48ShapeSearchRunTests.cs` | 新建。整夜实跑器（慢门，只印不判），W08／W06 两行 Theory |
| `Pt_Optimize.Tests/R48ShapeSearchDriverTests.cs` | 新建。(b)(c) 快门；(a) 冒烟为慢门 |
| `deliverable/R48_搜形状_Core驱动_W08_本次开跑于2026-09-23_213654.txt` | 冒烟参数实跑的证据档（部分，见第 5 节） |

## 2 照搬了什么（出自 `UI/LineDesignPage.cs` 的 `SearchOneFamilyAsync`，07644dc 约 3177～3650 行）

逐段照搬，判定与阈值不动；缺省值全部作为 `ShapeSearchOptions` 的字段缺省，出处写「照抄界面 SearchOneFamilyAsync 的值」：

| 段 | 界面 | 驱动 |
|---|---|---|
| 基准 | 先算页面当前形状（`R0now > 5 && hw0now > 1`） | 先算种子自己的形状（`EvalSeedFirst`，缺省是） |
| 单点评估 | `EvalShape`：`seed.Clone()` 改盘半径、舌半宽、锥形；舌长 = √(R²−hw²) + 压接 + `GeometryScreen.FreeTabMinDefaultMm`；R 低于闭式下界跳过；`Solver.Solve(AllowTabCuts, MaxRounds = screenRounds, ScreenCoarseMm)` | 同式，`Eval` |
| 批评估 | `EvalShapesBatch` → `ShapeBatchEval.RunAsync`（4 路） | 同，路数 `Lanes`（界面 4，跑器缺省 2） |
| ① | 起点表最大值、舌宽取最宽比例（hw = R，切线族）解一次 | 同 |
| ② | 不动点 ≤ 3 次：`GeometryScreen.MinDiscRadiusMm(plates)`，贴下界 0.05 mm 停 | 同 |
| ③ | 下界不可行 ⇒ 二分 ≤ 3 次、区间 ≤ 1 mm 停 | 同（只在已核实可行上端时，见 3 (b)） |
| ④ | 黄金分割 ≤ 4 点、区间 ≤ 1 mm 停、上端 min(bHi + 6, Rsafe)、已算过 ±0.5 mm 去重并前挪 0.5 | 同 |
| ⑤ | 最优盘径上试其余舌宽比例（0.75／1.00 里除最宽外） | 同（无可行点时不试，见 3） |
| ⑥ | 邻域爬山：`ShapeSearchPlan.Neighbours`（盘径 ±步、舌宽 ±档、翻转锥形）、`Worth`、`Improved`（0.5 g）、`Refine` 减半到 `MinDiscStepMm` 0.625、最多 `SearchMaxExtend` 6 轮 | 同，规则全调 `ShapeSearchPlan` |
| 精算 | `MeshVerify.RequiredMeshFor(win.d, baseIn)` 给细网格与细区半径初值，`Solver.Solve(MaxRounds = finalRounds 40)`，`ShapeSearchPlan.RefineVerdict` | 同 |

粗筛 16 轮、精算 40 轮、粗筛平坦区网格 0（关）均为界面值。

## 3 改了哪两处（变因）与另一处写明的差异

(a) 起点表不再写死 {25, 30, 35}。
- 旧：`SearchDiscs = { 25, 30, 35 }`，经 `LiveDiscs` 抬到下界；① 起点永远是 35。
- 新：调用方必须给盘半径上端 `MaxDiscMm`（缺省 NaN，没给就抛）；缺省表 = 从闭式下界（按 `LiveDiscs` 的 0.5 mm 向上取整）起每 `ShapeSearchPlan.DiscStepMm`（5 mm）一点到上端，再过 `LiveDiscs`。调用方也可给表（`DiscGridMm`）。
- 变因：写死的表把上端封在 35，大盘与切线族宽舌（探针里唯一的正面信号 R = w = 45，决 102 报告 1.3 (b)）到不了。
- 上端的出处：探针给的表，无出处。跑器缺省 50 mm，出处原样印进证据头与无解报告。

(b) 起点不可行时向上外推。
- 旧：界面默认表最大值可行，二分区间的可行上端没有核对（`LineDesignPage.cs:3436`）。
- 新：① 不可行 ⇒ 从表最大值起每次 + DiscStepMm 再解，直到可行或到上端。找到可行点 ⇒ 它成为 Rsafe（二分与黄金分割的上端、爬山的候选出发点）。到上端仍无可行点 ⇒ 不二分、不做黄金分割、不试其余舌宽、不爬山、不精算。② 不动点仍照做，因为它只用解出来的板厚，不依赖上端可行；② 若解出可行点，后续照常从它走。最后仍无可行 ⇒ 输出「无可行形状」报告：盒子（下界、起点表、外推到哪、上端与出处）、计数、逐形状停因（原样取 `SolverResult.StopWhy`，空则取 `Message`）、三条硬判据值与裕度、铂重、卡住的硬安全线、每条硬判据在盒内最好的裕度、固定的边界保温条件、盒外下一根杠杆。
- 注意：缺省表铺到上端，所以缺省配置下外推只在「表最大值 + 5 ≤ 上端」时发生（例如调用方给了比上端短的表）；缺省表的最大值本身就是离上端不到 5 mm 的那一点。

另一处差异（不是阈值，写明不静默）：早筛的管孔半径取 `DesignSpec.HoleRadiusMm`（壁厚 + 内径/2），界面写「壁厚 + 25」。变因：业主「通用型解法」原则（换内径也要出结果）；内径 50 时逐位相同，W08、W06 都是 50。

其余与界面的细差：
- 界面在 ② 之后按 `rows[^1]` 读上一行；驱动直接用本次评估返回的行。界面里 ② 与 ③ 的点都 ≥ 闭式下界、不会被跳过，所以两者等价。
- 界面在 ⑤ 不看最优盘径是否可行；驱动在没有任何可行点时不试（没有「最优盘径」可言）。
- 批内每个形状算完先印一行「（批内先到）」，批完再按原顺序编号贴一遍：进程被杀也留得住批内已算完的（界面是批完一起贴）。

## 4 没搬的

- 界面进度条、状态面板、阶段显示（`_prog`、`Note`、`_stages`）。
- 两族并跑（界面「两个都算」）：驱动一次只跑一族；跑器按现役设计的解法设定只跑「不挖舌孔」（`AllowTabCuts = false`，出处：界面下拉预设第 0 项，与 `R48LEndToEndTests.ProductionOptions` 同一份）。
- 写回页面控件、「搜形状结果」下拉、`ShapeReview.Build` 形状体检。
- 界面改调驱动：Windows 待办（第 8 节）。

## 5 门表

| 门 | 平台 | 结果 | 覆盖 | 不覆盖 |
|---|---|---|---|---|
| 镜像三步重建 | Linux | `Compile Remove` = 29（28 档界面排除 + `CliUiParityTests`），新三档都在编译里，Release 编过 | Core 与测试编译 | 界面、Program.cs |
| 快套件过滤 `FullyQualifiedName~R48ShapeSearchDriverTests&速度!=慢` | Linux | 4／4 过，250 ms | (b) 与三条 (c) | (a) |
| `R48ShapeSearchDriverTests.b_驱动里没有写死的盘径表_上端来自调用方` | Linux | 过 | 去注释后的驱动源码里没有 {25,30,35} 与任何两个两位数以上的数组字面量；缺省上端 NaN、不给就抛；跑器读 `SHAPE_MAXDISC` 且印「探针给的表，无出处」 | 其他形式的写死常数 |
| `R48ShapeSearchDriverTests.c_表最大值不可行_外推两步后可行_二分与爬山从那里开始` | Linux | 过 | 注入假求解：表 {27, 32}、R ≥ 40 可行 ⇒ 外推到 37、42；② 在闭式下界不可行；第一次二分 = (② + 42)/2；爬山从最轻可行点出发；精算用细网格与 40 轮；方案卡含边界保温四项 | 真求解器上的答案 |
| `R48ShapeSearchDriverTests.c_全不可行_不二分不爬山不精算_报告含每形状停因` | Linux | 过 | 全不可行 ⇒ 无 ③④⑤⑥ 行、无精算调用、报告含每形状停因、上端出处、三条判据全名、卡住项、基准行 | 同上 |
| `R48ShapeSearchDriverTests.c_缺省起点表从闭式下界起每步一点到上端` | Linux | 过 | 缺省表首点 = 下界 0.5 mm 取整、步长 5、末点 ≤ 上端且 + 5 > 上端；① 在末点、hw = R | 同上 |
| 相关既有门：`R48DeliverableWriteGuardTests`、`BannedVocabularyTests`、`ShapeSearchPlanTests`、`ShapeBatchEvalTests`、`NoCriterionCode*`、`ShapeSearchParallelTests`、`ShapeSearchTaperFamilyWiringTests`、`ShapeStepRefineTests` | Linux | 43／43 过 | 证据档写法、禁词、搜形状规则件 | 界面接线 |
| `R48ShapeSearchDriverTests.a_冒烟_真求解器_一轮_只验接线`（慢门） | Linux | 第一版（上端 31.6）过，1 s，只走到闭式预检；改上端 32.0 后跑 41 min 未完，手动停止，未判 | 真求解器接线 | 答案；32.0 版未跑完 |
| `R48ShapeSearchRunTests` W08 冒烟参数（慢门，只印） | Linux | 出证据档，1 行完成后手动停止（见 5.1） | 端到端出证据档、表头、逐轮与逐形状实时追加 | 答案；②～⑥、精算、方案卡在真求解器上没跑到 |

### 5.1 真求解器冒烟（只验接线，不验答案）

同机争用：本机另有两棵工作树（`wtB0`、`wtC_…`）在跑测试与编译，loadavg 升到 40～45（4 核），下面的时长只作量级，偏长。

- 第一次冒烟（上端 = 闭式下界 26.6 + 5 = 31.6）：起点表只剩 {27.0} 一点，① 在盘半径 27.0 被求解器的「圆盘盖得住管孔＋焊脚」场解前闭式检查挡下（需要 27.060，缺 0.060，焊脚 1.260），1 s 出「无可行形状」报告，没碰场解。这一跑走通了无解报告分支，但没量到场解耗时；此后把冒烟上端改成「起点表第一点 + 5」= 32.0（表 {27, 32}）。这也说明 ② 不动点只往小盘跳：起点在真下界之下 0.06 mm 时不会往上补，外推 + 5 mm 越过上端就停（照搬界面的行为，未改）。
- `a_` 冒烟（上端 32.0、粗筛 1 轮、平坦区网格 20 mm）：跑了 41 min 仍未结束（一轮之内），已手动停止；因此把它标成慢门、只点名跑。
- 跑器 W08 冒烟（`SHAPE_SCREEN=1 SHAPE_FINAL=1 SHAPE_MAXDISC=lb+5`，平坦区网格 0，并发 2）：证据档 `deliverable/R48_搜形状_Core驱动_W08_本次开跑于2026-09-23_213654.txt`。表头（平台、树、争用、种子、族、上端与出处、参数）与逐轮行实时写出；基准形状（盘 Ø60、舌宽 60）第 1 轮开头行在 2.4 min 写出（盘板厚 0.73/1.26/1.26/0.73，与种子一致）。第 1 行（基准）在 5136 s（85.6 min）后写出：1 轮、场解 31 次、不可行（停因「跑满 1 轮仍未全过，结果未收敛，不作数」，1 轮截断，不是不可行的证据）、铂重 4247.4 g，三条硬判据按 `LineResult.Checks` 原样印出（最热铂高出热偶读数 15.539／≤5、管根低于热偶读数 3.896／≤5、管孔净流入 −3.875／≥0；1 轮截断的值，不作任何结论）。随后 ① 盘 Ø64（R = w = 32）开跑，23:0x 手动停止（证据档末尾已写明）；本档没有方案卡也没有无解报告。确认的是：真求解器接线、逐行实时写档、停因与判据取值、DisplayName 过滤只选 W08 一行（另用 `SHAPE_MAXDISC=abc` 让 W08 行在解析处即失败，确认过滤只命中 W08）。未确认的：真求解器上 ②～⑥、精算、方案卡这几段（由 (c) 的假求解门覆盖控制流）。

每形状耗时估计（推断）：
- 实测：基准形状 1 轮 5136 s、场解 31 次，约 166 s／次场解（同机 loadavg 40～45 持续约一半时段，偏长）。
- 按任务口径「一轮耗时 × 16」：5136 × 16 ≈ 82000 s ≈ 23 h／形状（推断，这是上界式外推）。
- 按场解次数外推（推断）：第 1 轮已含 31 次场解，而决 102 报告 1.2 节实测一次完整求根（最多 40 轮）W06 导航档 42 次场解、129.8 min；多数场解发生在前几轮。据此 16 轮粗筛一个形状约 1.5～3 h；闭式预检挡下的形状 1 s；到顶很快停下的形状可能少于 1 h。
- 整夜 12 h、一个设计：串行段（①、外推、②、③、④）每步一个形状，按 1.5～3 h 计只够 4～8 个形状，多半停在 ③／④ 之内，到不了 ⑤⑥ 与精算（推断）。因此 12 h 内拿到方案卡的可能性低；证据档里逐形状的停因与三条判据是今夜最可能拿到的东西。

## 6 怎么启动整夜实跑

在工作树的镜像目录下（镜像已按第 5 节三步建好）：

```
cd <wtSHAPE>/.linux
# W08
SHAPE_MAXDISC=50 SHAPE_LANES=2 SHAPE_SCREEN=16 SHAPE_FINAL=40 SHAPE_SEEDFIRST=0 \
SHAPE_CONTENTION="与 W06 搜形状同机并跑" LANG=C.UTF-8 \
nohup timeout --kill-after=120 43200 dotnet test Tests/Tests.csproj -c Release --no-build \
  --filter "FullyQualifiedName~R48ShapeSearchRunTests&DisplayName~W08" -- xUnit.MaxParallelThreads=1 \
  > ../../shape_W08.log 2>&1 &
# W06
SHAPE_MAXDISC=50 SHAPE_LANES=2 SHAPE_SCREEN=16 SHAPE_FINAL=40 SHAPE_SEEDFIRST=0 \
SHAPE_CONTENTION="与 W08 搜形状同机并跑" LANG=C.UTF-8 \
nohup timeout --kill-after=120 43200 dotnet test Tests/Tests.csproj -c Release --no-build \
  --filter "FullyQualifiedName~R48ShapeSearchRunTests&DisplayName~W06" -- xUnit.MaxParallelThreads=1 \
  > ../../shape_W06.log 2>&1 &
```

- 证据档：`deliverable/R48_搜形状_Core驱动_{W08|W06}_本次开跑于…txt`，每个形状一行、每轮一行实时追加，被 timeout 杀掉也留下已算完的；看进度 `tail -f`。
- 上端 50 时的起点表（推断，按缺省表规则算）：W08 闭式下界 26.6 ⇒ {27, 32, 37, 42, 47}，① 在 R = w = 47（盘 Ø94）；W06（壁 0.6，下界 26.2）⇒ {26.5, 31.5, 36.5, 41.5, 46.5}，① 在 R = w = 46.5。缺省表的最大值离上端不到 5 mm，所以外推在这组参数下不会发生。
- `SHAPE_SEEDFIRST=0`（选定，界面缺省是 1）：种子形状（盘 30、舌半宽 30）已有端到端结果（决 102 报告 1.2 节：W08 4380 g、W06 3477 g，都到顶不可行），整夜只够算几个形状，不再花一个形状的机时重算它。要与界面逐段一致就改回 1。
- 并发：两跑各 2 路，批段最多 4 个求解线程占满 4 核；①②③④ 与精算是串行段，各占 1 核。另有别的长跑同机时，把 `SHAPE_LANES` 降到 1。
- 12 h（43200 s）是时间闸，不是判据；到点被杀时证据档里是已算完的部分，没有方案卡与无解报告，读档时按「未跑完」处理，不当无解。
- 跑完或被杀后：在工作树 `git checkout -- deliverable`（还原被门改写的受跟踪文本；带开跑时刻的新证据档不受影响）。

## 7 不覆盖

- 不判答案：整夜实跑只印；方案卡里的「判决网格精算全过」来自 `Solver.Solve` 的 `Feasible`，不是另验；三关（带玻璃稳态、升温、空管）未接进驱动。
- 形状与保温强耦合（决 102 探针：盘 30 上舌半宽不能超过盘半径；舌 ×1.25 与 ×1.5 两例间两条判据变号）：本驱动的保温（管保温、圆盘保温 20 mm、铜排夹持 450 °C）是固定输入，不是搜索维度；方案卡把这些写成「这个方案只在这些条件下成立」。
- 只跑「不挖舌孔」一族；挖舌孔族未跑。
- 上端 50 mm 无出处；只有邻域停因写「步长已收到分辨率下界」时才是 ±0.625 mm 分辨率上的局部最优，轮数用完（MaxExtend）停下的不是（方案卡印停因，见第 10 节）。
- Linux 数是预跑，待 Windows 重录。

## 8 Windows 待办

- 界面 `SearchShapeAsync` 改调 `ShapeSearchDriver.Run`（进度条经 progress 回报；写回与下拉照旧），界面删掉那份 ①～⑥ 实现；`tests/UiWiring` 走查。
- 界面的起点表与上端：把写死的 {25, 30, 35} 换成「上端 + 出处」输入（出处缺失时印「无出处」）。
- 在 Windows 上重跑 `R48ShapeSearchDriverTests` 与一次整夜实跑，重录。

## 9 成本

- 人时：约 2.5 h（读码、写驱动与门、镜像、冒烟）。
- 机时：镜像编译两次各约 2～3 min；快门 < 3 s；真求解器冒烟约 单核 85.6 min（跑器冒烟）＋ 41 min（`a_` 冒烟，未跑完即停）＋ 1 s（第一次冒烟）（同机争用 loadavg 40～45）。

## 10 对照核实（2026-09-23 23:0x～，Linux）

逐段对照 `Core/ShapeSearchDriver.cs` 与 `UI/LineDesignPage.cs` 的 `SearchOneFamilyAsync`（3177～3640 行）。

**业主要求的两处改动**：(a) 起点表不写死：缺省 = 闭式下界按 0.5 mm 向上取整起、每 5 mm 一点到上端，再过 `LiveDiscs`；上端 NaN 即抛。界面与驱动都只用表的最大值（`discs[^1]`），表里其余点从不求解，所以 (a) 实际上等于「① 从不超过上端的最大表点起」。(b) ① 不可行向上外推，每步 5 mm 到上端；外推找到可行点后 Rsafe 改为该点（黄金分割上沿 `min(bHi+6, Rsafe)` 跟着变）；没有已核实可行的上端时不二分、不做黄金分割；全程无可行出「无可行形状」报告。缺省表铺到上端，缺省参数下 (b) 不会触发。

**必要的适配（去界面）**：种子 `seed.Clone()` 代替 `PageToDesignSpec()`，锥形取 `seed.TabTaper` 代替页面勾选；进度条、`Note`、`_out` 改为 progress 与逐形状行；批内并发路数由调用方给（界面写死 4）；一次只跑一族，「两个都算」与 Provenance 解法标记不搬；精算后的 `ShapeReview.Build`、写回控件、下拉、`AdoptSolvedDesign` 不搬，换成方案卡／无解报告；`ShapeBatchEval.RunAsync` 同步等待；批内先到的形状先印一行（多印，不改结果）；阶段名改为 基准／①／①外推／②／③／④／⑤舌宽／⑥邻域n；先算基准做成开关（缺省开，同界面）。早筛管孔半径改取 `DesignSpec.HoleRadiusMm`（界面「壁厚+25」），依据业主通用型原则，内径 50 时逐位相同。判定条件、常数、次序（不动点 3 次、容差 0.05；二分 3 次、1 mm；黄金分割 4 点、1 mm、+6 mm、±0.5 mm 去重；邻域 `ShapeSearchPlan` 同一份；精算 `MeshVerify.RequiredMeshFor` + 40 轮 + `RefineVerdict`）逐项相同。舌长规则 `√(R²−hw²) + 压接段 + FreeTabMinDefaultMm` 与界面、`R48NMeshGateTests.SetRW` 逐字相同。

**本次改对的四处（原稿多余或错误）**：
1. ⑤ 其余舌宽比例：原稿在没有可行点时跳过，界面无条件试。改回无条件（无可行点时更窄舌宽正是该试的方向，跳过等于静默缩小搜索盒）。代价：全程无可行时每个设计多一个形状（量级 1.5～3 h，推断）。快门 `c_全不可行…` 改为断言 ⑤ 有一行、③④⑥ 与精算没有。
2. 方案卡状态行：原稿 `Feasible` 即写「判决网格精算全过」；没做第二遍细网格求根时改写「只在导航网格上」，并在「精算结束」那一行同样标出。新快门 `c_精算没做第二遍_方案卡写不可交付_不写判决网格全过`。
3. 方案卡「局部最优只在 ±0.625 mm 分辨率上成立」原稿无条件印；改为印邻域停因（`ShapeSearchResult.ClimbStop`／`ClimbConverged`），轮数用完停下的写明「不是局部最优」。
4. 边界条件：原稿管保温只印内层厚（`TubeInsulMm`），读来像只有一层；改为印三层（内层厚取 `DesignSpec.TubeInsulMm`，中外层取 `DesignInputs.Layer2／Layer3`，与组算例同一规则）。舌端铜排边界原稿一律印「夹持温度」；改由 `ShellThermal.ClampBoundaryOf` 判种类（定温／热导／自由端），热导时写明夹持温度不用。字段都是现有字段，没有新常数。

**门（Linux）**：镜像重建 `Compile Remove` = 29；`R48ShapeSearchDriverTests` 快门 5/5 过；相关既有门 43/43 过。真求解器冒烟见下。

## 11 09-24 按夜跑数据改的六件事（2026-09-24，Linux 预跑，待 Windows 重录）

工作树：`wtSHAPE2`（会话暂存区），基于 cc49836，未提交。起因：夜跑证据档 `deliverable/R48_搜形状_Core驱动_W08_本次开跑于2026-09-24_001304.txt`（粗筛 16 轮，① 盘 Ø94 每轮约 2 h～2 h 40：各轮开头行「已用」之差 125～156 min，HANDOVER 结果条记 2 h 10 min～2 h 55；12 h 闸切在第 6 轮）与 `…_021617.txt`（粗筛 2 轮，一形状 49～54 次场解 = 2.1～2.4 h，第 3 形状整个重解一遍）；HANDOVER §0.-21「W08 搜形状夜跑结果」条；全局解决方案第 2 版 `deliverable/R48_全局解决方案_出解路线_2026-09-24.md` §4 L0／L1、§5。判据、阈值、Solver 的判定与旋钮分派、ShellThermal 一处没碰（判据换向另由 wtCRIT 做）。

### 11.1 改了哪些文件

| 文件 | 性质 |
|---|---|
| `Pt_Optimize/Core/SameStateReuse.cs` | 新建。A：算例全字段指纹（SHA-256）与一次 Solve 内的 LRU 缓存（容量 64，选定，只影响命中率） |
| `Pt_Optimize/Core/Solver.cs` | 只动三处：`EvalRaw` 里加同状态复用（开关 `SolverOptions.ReuseSameStateSolves`，缺省关）；终局复核后放大那一支加跳过（开关 `SolverOptions.SkipRadiusGrowthAfterFinalCheck`，缺省关）；`SolverOptions`／`SolverResult` 加字段（两开关、只供门用的 `SolveProbe`、`SkippedRadiusGrowthToMm／Why`、`SameStateReuses`） |
| `Pt_Optimize/Core/ShapeSearchDriver.cs` | D 并行首遍（`ParallelFirstPass`，改回 false = 界面原算法）；C 粗筛 8 轮；E 舌宽比表与闭式肩部预筛（`ShoulderJPrescreen`）；粗筛传 B、A 两开关（`ScreenSkipRadiusGrowth`、`ScreenReuseSameState`）；逐形状行加「同状态复用」「粗筛本该放大到 mm」两列；方案卡与无解报告加「粗筛设置」一行 |
| `Pt_Optimize.Tests/R48ShapeSearchRunTests.cs` | 跑器：粗筛缺省取选项（8）；新环境变量 `SHAPE_PARALLEL／SHAPE_SKIPGROW／SHAPE_REUSE／SHAPE_SHOULDER／SHAPE_WFRAC`（都是改回用）；证据头加「粗筛轮数」「算力选项」两行；末尾加粗筛场解合计、复用、跳过放大、预筛、首遍分支 |
| `Pt_Optimize.Tests/R48ShapeSearchDriverTests.cs` | 改钉 3 处（变因见 11.7）；新快门 d×3、e×4 |
| `Pt_Optimize.Tests/R48SolverScreenCostTests.cs` | 新建。A、B 求解器侧快门 4 条；慢门 A（开关两跑逐位）与慢探针（生产导航网格归因） |
| `Pt_Optimize.Tests/R48F7AdaptiveRadiusTests.cs` | 门 f 白名单补登记 `ShapeSearchDriver.cs「FineRadiusMm = finFineR」`（cc49836 起门 f 即红在这一行：驱动精算照搬界面那一行，当时没登记）；规则与其余条目不动 |

### 11.2 A　归因「一轮 27～31 次场解」

粗筛（`FineMm = 0`，只有导航遍）一次 `Solver.Solve` 的结构（行号为本树 Solver.cs）：每遍开头验下角 1 次（`MeltFloor`，:1953，兼第 1 轮基准，:467 已有去重）；每轮开头按最新几何重算 J 截面下角（:465，闭式）；第 2 轮起每轮基准场 1 次（:468）；场定孔位变了有切口的片 ⇒ 重解 1 次（:481）；然后对本轮每一条违反（片 j × 三条逐片判据）先 `ChooseKnob` 比价、再 `RaiseUntil` 二分求根；遍末终局复核 1 次（`Finish`，:1712，带升温，不经 EvalRaw）。

| 步骤 | 每轮场解次数：上界 ／ 典型 | 出处（本树 Solver.cs） | 同一状态重解？ |
|---|---|---|---|
| 每遍开头验下角 | 每遍 1 ／ 1 | `MeltFloor` :1953；:467 把它当第 1 轮基准（改前已去重） | 否 |
| 每轮基准场（第 2 轮起） | 1 ／ 1 | :468 | **是**：d = 上一轮最后一条落地后的状态，那个状态在它的二分／上界探针里已解过；只有 :465 按 J 抬了板厚时才是新状态 |
| 场定孔位后重解 | 1 ／ 0～1 | :481 | 否（孔位变了） |
| 每条违反：`ChooseKnob` 比价基准 r0 | 1 ／ 1 | :942 | **是**：第一条的 d 就是本轮基准场那个状态；其后每一条的 d 是上一条落地（或退回）后的状态，已在上一条里解过 |
| 每条违反：候选各抬到上界 | 5（+ 形状族） ／ 管孔净流入 2～3（t₂、r₂、板厚）；管根低于热偶读数 3～4（舌保温 + 槽张角形状族 2～3 个；不挖舌孔族剔掉舌孔两根）；最热铂高出热偶读数 3～5（t₂、r₂、t₁、r₁、板厚；r₁／r₂ 在倍率 = 1 时闭式跳过、不花场解） | :1059；形状族 :2495 | 否 |
| 每条违反：`RaiseUntil` 上界 | 1 ／ 0（比价时已量，带过来） | :1420 | 不适用 |
| 每条违反：`RaiseUntil` 二分 | 14（BisectMaxIter） ／ ⌈log₂((上界 − 起点)/图纸格)⌉：板厚 (6 − 约 3)/0.01 ⇒ 9；舌保温 (20 − 0.3)/0.5 ⇒ 6；t₁／t₂ (2.5 − 1)/0.01 ⇒ 8；槽张角 约 300°/1° ⇒ 9；r₂ (16 − 6)/0.01 ⇒ 10 | :1486 | 否 |
| 每条违反：格点对齐／走格 | 1 + CertWalkMaxSteps 10 ／ 0～1 | :1524（`EvalAt`，经 :1541 `WalkConservative`） | 否 |
| 遍末终局复核 | 每遍 1 ／ 1 | `Finish` :1712 | 否 |

合起来：一条违反典型 10～14 次（上界 1 + 5 + 形状族 + 14 + 11），一轮 = 1 + (0～1) + Σ违反。两条违反 ⇒ 约 22～30 次，与夜跑 021617 第 1、2 形状（2 轮 49、54 次，含验下角与终局复核各 1）对得上；001304 盘 Ø94 一轮 2 h 10 min～2 h 55 的轮里旋钮变化是 3～4 条违反落地（第 1 轮：片1／片2 舌保温 0.30 → 3.50／6.50、片0／片3 槽张角 0 → 278°／291.91°；第 2 轮：片0／1／2 板厚 2.94 → 3.03／6.00／6.00、片3 舌保温 → 1.00；第 3～5 轮各 3 条：片3 板厚、片1／2 外级 t₂ → 2.5、r₂ → 16、t₁ → 2.5），按上表约 35～55 次／轮，按首次场解 2.2 min 计 77～121 min，同量级（推断；夜跑证据档不印每次场解，逐次计数见下面的实测）。

**明显的重复**：每轮「基准场」1 次 + 每条违反的「比价基准 r0」1 次，都是在解刚解过的同一个状态，逐位得到同一个结果。约占每轮 (违反条数 + 1) 次：按结构估，两条违反且都二分的轮约 3/26 ≈ 12%；生产导航网格实测一形状两轮 10/49 = 20%（见下）。**去重（A）**：`EvalRaw` 造好算例后取全字段指纹，同一次 Solve 里指纹相同就交回上次的 `LineResult`（Solver 不改写 LineResult：`grep` 核过没有对 LineResult 字段赋值、没有按引用比较），调用方照旧过 `Gate`／熔化判定 ⇒ 不改判定、不改阈值、不改旋钮分派。改回 = `ReuseSameStateSolves = false`（缺省）。

**C4′ 放大重做确是 021617 第 3 形状重解的原因**（由推断改为代码结构证实）：粗筛 `FineMm = 0`，一次 Solve 里调用 `Rounds` 开新一遍（从第 1 轮数起）的只有两处：:779 首遍、:881「第一遍（细区半径放大后重做）」；后者唯一入口是 :861 `else if (g.Grew)`，即终局复核后细区没盖住热点、半径放大（cc49836 同一段在 :849／:869）。021617 第 25、26 行（`[盘Ø74.0／舌宽55.5] 第 1 轮`、`第 2 轮`，已用 272.9、355.5 分）之后第 27、28 行又从 `第 1 轮` 起（已用 455.7、537.5 分），板厚回到下角 1.95/2.65/2.65/1.95、舌保温回到 0.30（:869 前的 ToLowerCorner），孔心 −107.5 → −121（新半径上的场定孔位）；第 30 行 场解 129、轮 4、21853 s。

**实测归因（生产导航网格）**：慢探针 `R48SolverScreenCostTests.探针_生产导航网格_…`，证据档 `deliverable/R48_搜形状_生产导航网格_场解归因探针_本次开跑于2026-09-24_180144.txt`；设置与夜跑 021617 第 1 形状相同（W08 种子、盘 R = w = 37、不挖舌孔、2 轮、粗筛平坦区网格 0），另开同状态复用与跳过放大。每次整线解经只供门用的回调 `SolveProbe` 取调用栈，按 Solver.cs 里调用语句原文所在行归到步骤（行号由门在运行时从源码找，不手抄）。「解/复用」= 真解场／同状态复用（没花场解）：

| 步骤 | 第 1 轮 | 第 2 轮 | 合计 |
|---|---|---|---|
| 每遍开头验下角（兼第 1 轮基准场） | 1/0 | 0/0 | 1/0 |
| 每轮基准场（第 2 轮起） | 0/0 | 0/1 | 0/1 |
| ChooseKnob 比价基准 r0 | 0/4 | 0/5 | 0/9 |
| ChooseKnob 候选抬到上界 | 4/0 | 13/0 | 17/0 |
| RaiseUntil 二分中点 | 20/0 | 0/0 | 20/0 |
| 合计（不含遍末终局复核 1 次） | 25/4 | 13/6 | 38/10 |

慢门 A 第二版（同一设计、1 轮，开与关两跑）：关 = 验下角 1 + 比价基准 4 + 候选抬到上界 4 + 二分中点 20 = 29 次（+ 终局复核 1 = 30）；开 = 25 次真解 + 4 次复用（比价基准 4 次全部复用）+ 终局复核 1 = 26；两跑旋钮终值、判据表、轨迹、停因逐位相同。

读法：第 1 轮 4 条违反（四片管根低于热偶读数，候选里槽张角在盘 37、板厚 1.56 时上界为 0、闭式淘汰不花场解，只探舌保温 1 次），每条二分 5 次（舌保温 0.3 → 7.0／3.0／4.5／17.5，格 0.5）；第 2 轮 5 条违反，候选都抬到上界仍补不上（板厚到 6.00），留着不退、没有二分。**场解 39 + 复用 10 = 49，与 021617 第 1 行的场解 49 相等；铂重 4999.8 g、三条硬判据 16.082／4.748／−1.930 与 021617 第 1 行逐字相同**（同一代码路径、同一设计，开复用后少解 10 次、结果不变）。本形状重复解占 10/49 = 20%，比按结构估的一成高：第 2 轮没有二分，比价基准的占比变大。墙钟 153.1 min（同机争用：开跑时 loadavg 2.3，期间 8～22，另有本树慢门 A 两线程与别的实施者的编译与测试）。

### 11.3 B　粗筛关掉终局复核后的放大重做

`SolverOptions.SkipRadiusGrowthAfterFinalCheck`（缺省 false = 生产逐位不变）。true 时：终局复核后细区没盖住热点、`GrowFineRadius` 给出放大（Grew）⇒ 不改计划、不改半径、不重做，把本该放大到的半径与理由原句记进 `SolverResult.SkippedRadiusGrowthToMm／Why`，break。只截 Grew 这一支：到上限仍盖不住（Refused）与峰位算不出两支照旧判不了；复核判定与 Feasible 的来源（`Finish` 里 `res.Best.AllOk`）不动。导航遍之后、第二遍之前那一处放大（只在 FineMm > 0 时发生）不动。驱动只在粗筛传 true，赢家精算不传。证据：逐形状行末列「粗筛本该放大到 mm」、跑器末尾「跳过放大重做的形状 n 个」、方案卡与无解报告「粗筛设置」一行。**这些行热点处的温度类判据按细区半径计划的规则不算数，只作粗筛排序，不可交付**（写在行里）。

### 11.4 C　粗筛 8 轮、全过即停

`ShapeSearchOptions.ScreenRounds` 16 → 8。**选定**：依据夜跑 001304 与 021617；界面的 16 轮在 4 核 Linux 上一形状 30 h 以上，不可用；跑过再校。注释与跑器证据头「粗筛轮数」一行照写。**全过即停**：核实求解器本身已如此，驱动不另加：`Rounds` 里本轮没有违反（todo 空）且其余硬判据也没有违反 ⇒ `res.Feasible = true`、停因「第 k 轮全过」（:629），随即 break；之后只剩终局复核 1 次。

### 11.5 D　起点表并行首遍

① 改为起点表全部点（闭式下界起每 5 mm 到上端，舌宽比取最宽 = 切线族）用 `ShapeBatchEval` 并行解一遍（`Lanes` 路，按盘径从大到小提交：大盘解得慢，先占道，整批墙钟最短）。然后：有可行点且最小可行点 Rhi 左边还有表点 ⇒ 左邻 Rlo（不可行，因为 Rhi 是最小可行点）与 Rhi 之间 ③ 二分（≤ 3 次到 1 mm）、④ 黄金分割（≤ 4 点）照旧，不再做 ②；全可行或最小可行点就是表首点 ⇒ 直接从下界做 ②，板厚取最小可行点那一行的；全不可行 ⇒ 表最大值之上每 5 mm 一点到上端并行一批外推，外推里有可行点同第一支，仍全不可行 ⇒ 不二分、不黄金分割，② 取表最大值那一行的板厚。黄金分割上沿 `min(bHi + 6, Rsafe)` 的 Rsafe 取首遍（含外推）已核实可行的最大盘径。**这是对界面算法的变更**（界面只在表最大值上串行解一次），变因：4 核并行、串行首遍是瓶颈（001304 在 ① 一个形状上用满 12 h）。判定与阈值不动。改回 = `ParallelFirstPass = false`（界面原算法与改前驱动同一段代码，快门 `c_改回界面原算法_…` 钉它）。

### 11.6 E　舌宽比表与闭式肩部预筛

`WFrac` {0.75, 1.00} → {1.00, 0.875, 0.75}：先切线族（①～④ 用 1.00），⑤ 按序试 0.875、0.75。0.875 = 1 − `ShapeSearchPlan.FracStep`（与邻域的舌宽步长同一格）。**选定**，依据：决 102 探针 `deliverable/R48_决102_形状杠杆探针_W08_盘径_本次开跑于2026-09-23_203224.txt`「盘+5」例（R 35、w 30）舌盘交界截面 J 11.670 > 限 11；021617 ⑤ 舌宽 55.5（盘 Ø74、比例 0.75）三条硬判据比同盘径切线族全劣化（36.4 对 16.1 K、9.0 对 4.7 K、−10.1 对 −1.9 W）。改回 = 传 {0.75, 1.00}。

**闭式肩部预筛**（`ShoulderJPrescreen`，改回 false）：w < R 的候选先按 J ≈ I/(t·2w) 估盘舌交界截面 J，逐片取最大；超过计算极限 `DesignSpec.JCheckAPerMm2`（= 设定 J + 1，`SectionSizing.JCheckOf`；现役 11）⇒ 不进场解，印「闭式跳过：肩部 J 估计 x > 11（…；估计、只作预筛，不是判据）」。I 取参照形状求解后的设计电流（`SolverResult.DesignCurrent.PlateA`），t 取参照形状求解后的板厚；参照 = ⑤ 用同盘径切线族那一行，⑥ 用当前最优行；参照没有设计电流或板厚 ⇒ 估不出、照常求解（说出来）。**局限（写明）**：真判据（法兰截面 J，`SectionSizing.Cuts`）的交界截面是「边条（弦扣管孔）× 圆盘侧厚 + 焊弧 × 孔边厚」，不是 2w × t（R 35／w 30 时 23.1 + 41.1 = 64.2 mm 对 2w = 60 mm）；求解器每轮按 J 抬板厚（`ApplySectionFloor`），被跳过的形状加厚后可能过 J，本预筛不覆盖那种形状。

### 11.7 改钉的门（变因）

- `R48ShapeSearchDriverTests.c_表最大值不可行_外推两步后可行_二分与爬山从那里开始` → 改名 `c_改回界面原算法_…`、加 `ParallelFirstPass = false`，断言一条没改。变因：D 成了缺省。
- `c_全不可行_…`：⑤ 原钉「一行、比例 0.75」→ 两行、先 0.875 后 0.75，并加「首遍分支 = 全不可行」。变因：E。
- `c_缺省起点表…`：原钉「① 在表最大值上」一行 → 另加「表里每一点都有一行 ①、hw = R」。变因：D。
- `R48F7AdaptiveRadiusTests.门f` 白名单：补登记驱动精算那一行（见 11.1）。

### 11.8 门表（Linux）

| 门 | 结果 | 覆盖 | 不覆盖 |
|---|---|---|---|
| 镜像三步重建（`mk_linux.sh` → `fix_mirror.py` → Release 编译） | `Compile Remove` = 29；编过，0 警告 | Core 与测试编译（新档两个经通配收进） | 界面、Program.cs |
| 基线（改前 cc49836，同一过滤：33 个读 Solver.cs 的测试类 + 搜形状相关） | 185／187；红 2：`R48TightenOnJudgeMeshTests.回收这一步必须排在第二遍求根之前`、`R48F7AdaptiveRadiusTests.门f_单一来源源码门…` | 改前状态 | 慢门 |
| 改后同一过滤 + 新类 + `NoCriterionCode*`、`ShapeSearchParallelTests`、`ShapeSearchTaperFamilyWiringTests`、`ShapeStepRefineTests`（`速度!=慢`） | 216／217，3 min 7 s；红 1 = 基线就红的 `R48TightenOnJudgeMeshTests`（11.13）；门 f 转绿（补登记） | 读 Solver.cs 的全部源码门、搜形状规则件、证据档写法、禁词 | 慢门；界面接线 |
| 点名门：`R48ShapeSearchDriverTests` 12、`R48SolverScreenCostTests` 4、`SolverIsSeedFreeTests` 8、`SolverFineRootTests` 6、`R48F7AdaptiveRadiusTests` 快门 8、`ShapeSearchPlanTests` 15、`ShapeBatchEvalTests` 3、`R48DeliverableWriteGuardTests` 2、`BannedVocabularyTests` 4 | 62／62，9 s | 见各门注释；新门：d×3（首遍对全部表点各调一次、二分区间取自首遍、全不可行并行外推且 ② 取表最大值那一行、全可行直接 ②）、e×4（粗筛 8 轮与三开关只在粗筛、跳过放大进行与方案卡、肩部 J 公式、预筛与改回）、A／B 源码门（关时与改前逐字相同，SHA-256 对 cc49836）、指纹门、LRU 门 | 真求解器上的分支走向 |
| 慢门 A 第一版（盘 R = w = 32、2 轮、粗筛平坦区网格 20 mm，开关两线程并跑） | **没出结果**：同机争用下 3 h 时间闸切掉（18:01～21:01） | 无 | 全部；已改第二版 |
| 慢门 A 第二版 `A慢_开关两跑逐位相同_只少场解_并印每步场解归因`（盘 R = w = 37、1 轮、生产导航网格） | 过，80.8 min（两线程并跑，loadavg 约 2）：关 场解 30、开 场解 26 + 复用 4；旋钮终值、判据表（名、值位、过、判不了）、轨迹、停因逐位相同；证据档 `deliverable/R48_搜形状_粗筛场解归因_同状态复用逐位门_本次开跑于2026-09-24_222334.txt`（文件名时刻是写档时刻，档末补记开跑 21:02:43；门已改为开跑时取） | 开与关旋钮终值、判据表、轨迹、停因逐位；场解次数守恒 | 判决网格（FineMm > 0）；第 2 轮起基准场的复用（第一版本该覆盖，没跑完） |
| 慢探针 `探针_生产导航网格_一形状两轮_…`（只印） | 跑完 153.1 min：场解 39 + 复用 10 = 49 = 021617 第 1 行；铂重与三条硬判据逐字相同（11.2） | 生产导航网格上的每步场解归因；开复用、开跳过放大后与改前夜跑同一设计的结果对照（印出来的精度） | 逐位（对照的是夜跑证据档里三位小数） |

### 11.9 冒烟（只验接线）

跑器 W08，`SHAPE_SCREEN=1 SHAPE_FINAL=1 SHAPE_MAXDISC=lb+5 SHAPE_SEEDFIRST=0`，并发 2 路，`timeout 1500`（25 min）；证据档 `deliverable/R48_搜形状_Core驱动_W08_本次开跑于2026-09-24_180833.txt`（末尾补记了被时间闸切掉）。确认：证据头「粗筛轮数」（写明选定与依据）与「算力选项」两行；驱动「算力选项」一行；「① 并行首遍：起点表全部 2 点 盘Ø{54.0, 64.0}…」一行；两点同时派出，盘 Ø54 在 1 s 内被求解器场解前闭式检查挡下（需要 27.060 mm）、盘 Ø64 第 1 轮开头行在 2.7 min 写出；逐形状表头多两列。未确认：首遍之后的分支在真求解器上的走向（25 min 内一个形状一轮都跑不完）。同机争用：开跑时 loadavg 22.49。

### 11.10 W08 启动命令与时间估计

在工作树镜像目录下（镜像已按三步重建，`Compile Remove` = 29）：

```
cd <wtSHAPE2>/.linux
SHAPE_MAXDISC=50 SHAPE_LANES=4 SHAPE_SCREEN=8 SHAPE_FINAL=40 SHAPE_SEEDFIRST=0 \
SHAPE_PARALLEL=1 SHAPE_SKIPGROW=1 SHAPE_REUSE=1 SHAPE_SHOULDER=1 \
SHAPE_CONTENTION="单跑 W08；并行首遍 4 路（同机另有长跑时在此写明）" LANG=C.UTF-8 \
nohup timeout --kill-after=120 43200 dotnet test Tests/Tests.csproj -c Release --no-build \
  --filter "FullyQualifiedName~R48ShapeSearchRunTests&DisplayName~W08" -- xUnit.MaxParallelThreads=1 \
  > ../../shape_W08_0924.log 2>&1 &
```

- 起点表（上端 50，推断，按缺省表规则）：{27, 32, 37, 42, 47}；按盘径从大到小提交：47、42、37、32 先占 4 路，27 等第一个空出的道（W08 盘 27 在求解器场解前闭式检查处 1 s 挡下，冒烟 180833 已见）。
- 时间估计（推断，按夜跑实测每轮墙钟外推，未扣全过即停与到顶早停）：盘 Ø94（R 47）每轮 2 h 2 min～2 h 36（001304 各轮开头行「已用」之差：125、122、150、156、149 min）⇒ 8 轮约 16～21 h；R 37 每轮约 1 h 3 min（021617 第 1 形状 2 轮 7595 s）⇒ 8 轮约 8.4 h；R 32 每轮约 1 h 12（021617 第 2 形状 2 轮 8613 s）⇒ 约 9.6 h；同状态复用约省 1 成（11.2）。**并行首遍的墙钟由 R 47 定，约 15～19 h，12 h 闸会再切在 ① 里**；之后 ③ 二分 ≤ 3 个形状、④ 黄金分割 ≤ 4 个形状是串行的，⑤ 两个形状、⑥ 每轮 ≤ 5 个形状并行，精算 40 轮在判决网格上（本次未量）。整趟是几天量级，不是一夜（见 11.13）。
- 12 h（43200 s）是时间闸，不是判据；被杀时证据档里是已算完的部分，按「未跑完」读。跑完或被杀后在工作树 `git checkout -- deliverable`。

### 11.11 不覆盖

- 真实形状上的答案（粗筛 8 轮是否够、并行首遍在真求解器上的分支走向）：要整夜实跑；Linux 数待 Windows 重录。
- B 的真场行为：只有源码门（去掉新插的块与改前逐字相同）与驱动侧注入门；「终局复核后真的没盖住热点」那一支要整形状粗筛跑完才触发，本次没有真场跑到。
- A 在判决网格上（FineMm > 0：第二遍与回收 `TightenOnJudgeMesh`）的逐位：慢门只跑了导航遍（粗筛平坦区网格 20 mm）与生产导航网格探针；精算缺省不开复用（`FinalReuseSameState = false`）。
- 肩部预筛是估计，被跳过的形状加厚后可能可行（11.6）。
- 全不可行时 ② 仍按规格做一次（板厚取表最大值），首遍已覆盖到表首点，这一次多半不给新信息（成本一个形状），未改（见待决定）。
- 界面改调驱动（Windows 待办，同第 8 节）。

### 11.12 成本

- 人时：约 5 h（读码与证据档、归因、写驱动与求解器两处、门、记录；等长跑约占一半）。
- 机时（Linux，4 核，同机另有别的实施者的编译与测试，loadavg 1～22 不等，耗时只作量级）：镜像三步重建 1 次 + 增量编译 5 次，各约 1～3 min；快套件 217 条 3 次各约 3 min、点名 62 条 9 s；慢门 A 第一版 2 线程 × 3 h（被时间闸切掉、无结果，白花）；慢门 A 第二版 2 线程 × 80.8 min；慢探针 1 线程 × 153.1 min；冒烟 2 路 × 25 min。

### 11.13 待决定

- 【待决定 1】W08 一夜出答案做不到（11.10）：选项 (a) 上端降到 42（表 {27, 32, 37, 42}，4 路一批；首遍墙钟由 R 42 定，推断 11～15 h），代价：R 47 那一点不再看（001304 里它 4943～5913 g，是表里最重的）；(b) 粗筛再降到 4 轮，代价：更多形状停在「跑满 4 轮未收敛」，不可行不能读成真无解；(c) ③ 改成 4 路并行的四分（一批 3 个内点，区间一次缩到 1/4，3 次串行变 1 批），代价：对界面算法再改一处、要新快门，多花 0～1 个形状的机时；(d) 粗筛开平坦区网格（`SHAPE_COARSE`），代价：粗筛判据跑在更粗的网格上，排序可能与生产网格不同。
- 【待决定 2】全不可行一支里 ② 仍按规格做一次（板厚取表最大值）：首遍已覆盖表首点（离闭式下界 ≤ 0.5 mm），这一次多半不给新信息，成本一个形状（最多 8 轮）。选项：保留（现状）／全不可行时不做 ②。
- 【待决定 3】精算开同状态复用（`FinalReuseSameState`）：精算 40 轮在判决网格上是整趟最贵的一步，复用约省 1 成；代价：先补一道判决网格（FineMm > 0，含回收 TightenOnJudgeMesh）上的开关逐位慢门。
- 【待决定 4】肩部预筛只是估计（11.6）：选项：保留（现状，省场解、可能漏掉加厚后可行的窄舌形状）／改用判据同一份闭式截面（`SectionSizing.Worst`，含边条与焊弧，t 仍取参照板厚）／关掉。
- 既有红门（不是本次改动引起，cc49836 基线即红）：`R48TightenOnJudgeMeshTests.回收这一步必须排在第二遍求根之前` 钉的是 F7′ 之前的原文 `Rounds(opt, "第二遍`，F7′ 把第二遍收成 `FinePass(tag)` 之后门找不到守护对象；要改门（钉 `FinePass(` 里 `TightenOnJudgeMesh` 在 `Rounds(opt, tag)` 之前），属 Solver 判定一侧的门，本次没碰。
