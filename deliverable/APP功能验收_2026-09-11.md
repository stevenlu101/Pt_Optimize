# Pt_Optimize APP 功能验收（收尾，2026-09-11）

> 用户 09-11 工单第 2 条：「若完成 1，进入 APP 功能验收的收尾工作」。第 1 条（补齐边角料 + 焊接下界小算盘）已以 R39 进库（857dbe9）。
> 验收对象：分支 `feat/geom-and-flange-accounting` 到 857dbe9 之后的工作树（含本文与倒表工具 `--cli --flowdump`）。
> 原则：每一条「过」都要有机器门或抓图撑着；本文只汇总，不自己重算任何数（铁律①）。

## 1. 验收范围

| 项 | 数量 | 出处 |
|---|---|---|
| 页（阶段轨） | 5：① 输入／② 法兰优化／③ 结果与出图／参考工具／使用说明 | `deliverable/Flow登记表_2026-09-11.tsv`（`Pt_Optimize.exe --cli --flowdump` 倒出） |
| 计算链 | 9（A 单段解析／A′ 单段扫描／B 分段解析／B′ 分段法兰／C 整线耦合★／C′ 定尺寸／C″ 形状搜索／D 升温闸／—） | 同上 |
| 按钮命令 | 25 | 同上；走查 20 节钉「登记表 ↔ 真界面双向对得上」 |
| 需求登记 | R1–R39（HANDOVER 登记表） | `HANDOVER.md`；09-11 全面排查两份报告（缺 0、半 9 → R35 补齐） |
| 表格控件 | 5 张（分段核算段表／结果表／分段控温点／判据表／焊接下界盘径系列） | `--uishot` 表格自检 |

## 2. 验收方法与结果总览

| 门 | 做什么 | 本次实跑结果 | 记录 |
|---|---|---|---|
| 快门（单元测试，Trait 速度≠慢） | 判断逻辑、源码钉子、登记表一致性、用例计数钉 | **804/804 通过**（4 m 32 s）；全套用例 830 = 804 快 + 26 慢，`HandoverGateCountTests` 反射数出与登记一致 | `deliverable/APP功能验收_快门记录_2026-09-11.txt` |
| 接线走查（UiWiring 默认） | 造真窗体、逐节核对按钮／门禁／写回／提示／排版 | **★ 全部通过**：47 节、515 项 ✓ | `deliverable/APP功能验收_走查记录_2026-09-11.txt` |
| 抓图（`--cli --uishot`） | 真 Show 窗体逐页画 PNG（含可滚动面板逐屏）、表格字被裁自检 | 17 张；「表格自检：5 张表，表头与行高都装得下字」 | `deliverable/界面截图_R39_2026-09-11/`（索引.txt） |
| 自检门（提交钩子） | 编译 → 快门 → 走查 → A–E 自检（冷／热启动同答、定尺寸不往烧断方向优化…） | R35／R37／R36／R38+R39 四次提交全部过钩子 | git log 4677c07…857dbe9 |
| 慢门（Trait 速度=慢，发版前才跑） | 真跑场解的仪器与对拍 | 见 §6（本次单独实跑） | `deliverable/APP功能验收_慢门记录_2026-09-11.txt` |

## 3. 逐页验收

| 页 | 页上有什么 | 抓图 | 走查覆盖 | 结果 |
|---|---|---|---|---|
| ① 输入 | 参数表（左）、管／法兰几何来源／法兰形状（含锥形舌片、解法三选一）／法兰厚度／舌片厚与叉臂只读／舌保温／环倍率／槽／孔／场定位置只读／保温与夹持／几何工艺／分段控温点表；工具条：分析几何变数、◈ 图纸几何 → 参数 | 01、01s1–01s4（往下滚四屏） | 5／9／10／14／15／16″／16⁗′／16⁗″／16⁗‴／16⁗⁗／22／23／29／35 系列 | 过 |
| ② 法兰优化 | 核算整线（主键）、手动分步（自动定厚／搜形状／加密复算／细网格重解／导出可回读 3DM／厚度灵敏度扫描）、解法阶段条 ①②③④⑤、逐段明细与诊断勾选、输出框 | 02 | 0／4／7／13／14／16／16‴／16⁗／17／17′／18／20／26／27／28／28′／35‴ | 过 |
| ③ 结果与出图 | 判据表、法兰温度场／电流密度场／管轴向剖面三张图；导出本页 3DM、另存为设计记录、保存、读取 | 03、03a–03c | 12／24／30／32 | 过 |
| 参考工具 | 设计记录 ▾／载入设计记录／导出设计记录 3DM；升温可达性趋势、计算 (F5)、两个扫描、导出 CSV、核算全线、选最省牌号、最小壁厚、核算法兰；五个子页：升温可达性趋势／分段核算／单段报告／轴向剖面／焊接下界小算盘 | 04、04a–04e | 1／2／11／18／21／33 | 过（复现键已按用户 09-11 选的 B 去掉，R37） |
| 使用说明 | 本页即完整说明书（判据全表、限值出处、按钮表、常见误用、核算整线 vs 搜形状对照） | 05（WebView2 正文抓图抓不到，页面本身有内容；以 `deliverable/说明书_渲染样本.html` 为准） | 3／31／34／35‴ | 过；文案重写是第 3 条工单，另记 |

## 4. 逐命令验收（25 条，顺序照 Flow 登记表）

| 命令 | 页 | 链 | 读页面控件 | 耗时 | 验收依据 | 结果 |
|---|---|---|---|---|---|---|
| 升温可达性趋势 | 参考工具 | D 升温闸 | 读 | 毫秒 | 走查 33（造好了要接上、门禁真的读它）；`DesignCurrentTests` | 过 |
| 计算 (F5) | 参考工具 | A 单段解析 | 读 | 秒级 | 走查 18（参考工具 12 个都在）；`SegmentSolver` 系列快测 | 过 |
| 扫描：保温厚度 | 参考工具 | A′ 单段扫描 | 读 | 秒级 | 走查 18；扫描逻辑快测 | 过 |
| 扫描：铂发射率 | 参考工具 | A′ 单段扫描 | 读 | 秒级 | 同上 | 过 |
| 导出 CSV | 参考工具 | A 单段解析 | 读 | 即时 | 走查 18／20 | 过 |
| 核算全线 | 参考工具 | B 分段解析 | 读 | 即时 | `LineSolverTests`；抓图 04b（段表与结果表） | 过 |
| 为各段选最省牌号 | 参考工具 | B 分段解析 | 读 | 即时 | 走查 18／20 | 过 |
| 按强度取最小壁厚 | 参考工具 | B 分段解析 | 读 | 即时 | 走查 18／20 | 过 |
| 核算法兰（分钟级） | 参考工具 | B′ 分段法兰 | 读 | 分钟级 | 走查 18／20／26（互斥） | 过 |
| 核算整线 | ② 法兰优化 | C 整线耦合★ | 读 | 十几分钟～两小时 | 走查 16（页面路径复现设计记录五项差在千分位）、16⁗（阶段条）、28（下一步指向）；`OneClickPipelineTests`（流水线四条腿＋三条护栏）；`SolverWiredToUiTests` | 过 |
| 分析几何变数 | ① 输入 | C 整线耦合 | 读 | 分钟级 | 走查 29（.3dm 模式下 ⑤⑥ 判得了）；`Walk --follow3dm` | 过 |
| ◈ 图纸几何 → 参数 | ① 输入 | — | 读 | 瞬时 | `ShapeToAnalyticTests`（三级逐位／两级／四级抛／闭环／真图纸）；`DrawingTaperArmReadbackTests`（R35：认锥形与叉臂） | 过 |
| 导出可回读 3DM | ② 法兰优化 | — | 读 | 十几秒 | `TabHoleTests`（孔／环半径逐片／形状族写进图也读得回来）；`ReadableArmExportTests`（R39：叉臂段） | 过 |
| 载入设计记录 | 参考工具 | — | 不读 | 即时 | 走查 2／35／35′（片数不同不崩、逐片回填）；R35 载入后参数表重读 | 过 |
| ◆ 加密复算（算到数不再变） | ② 法兰优化 | C 整线耦合 | 读 | 10～40 分钟 | `FieldConvergenceGateTests`；走查 28′ | 过 |
| ◆ 细网格重解（加密不过后） | ② 法兰优化 | C 整线耦合 | 读 | 十几分钟到数小时 | `FineResolveAfterVerifyTests`（R26）；走查 28′ | 过 |
| 自动定厚（手动分步） | ② 法兰优化 | C′ 定尺寸 | 读 | 数分钟～半小时 | 走查 7（写回不连环触发）、13、28；自检门 E 段（不往烧断方向优化） | 过 |
| ◇ 搜形状（手动分步） | ② 法兰优化 | C″ 形状搜索 | 读 | 几十分钟 | 走查 14／16‴（结果下拉）／35‴（对照表与门禁）；`Walk --searchshape quick`（R38：两族＋锥形）；`ShapeSearchParallelResultTests`（R36：串并逐位相同） | 过 |
| 厚度灵敏度扫描 | ② 法兰优化 | C 整线耦合 | 读 | 很久 | 走查 18／20／26 | 过 |
| 导出本页 3DM | ③ 结果与出图 | — | 读 | 十几秒 | 走查 12／32（出图 → 读回来）；`Export3dmMatchesSolvedTests`（Rhino，含舌根加厚带） | 过 |
| 导出设计记录 3DM | 参考工具 | — | 不读 | 十几秒 | 走查 12（失效档拦截）；`ExportOptimized3dmTests`（慢） | 过 |
| 另存为设计记录 | ③ 结果与出图 | — | 读 | 即时 | 走查 24／30；`DesignSpecStoreTests` | 过 |
| 保存 | ③ 结果与出图 | — | 不读 | 即时 | 走查 21（参数对象只有一份） | 过 |
| 读取 | ③ 结果与出图 | — | 不读 | 即时 | 走查 21 | 过 |
| 打开 Markdown 版 | 使用说明 | — | 不读 | 即时 | 走查 18／20 | 过 |

另有两个不是按钮、但工程师天天用的入口，也在验收里：「设计记录 ▾」（走查 1：切档不触发重算）与「搜形状结果 ▾」（走查 16‴：选一个就写回、含锥形与［解法］族名）。

## 5. 设计因果链验收（用户 09-08 定的四步）

| 步 | APP 里是什么 | 机器门 |
|---|---|---|
| ① 20 °C/h 升温电流 | `DesignCurrent`：温控空管升温全程峰值电流，逐段各算、共用片矢量合成 | `DesignCurrentTests`；判据「升温」；走查 33 |
| ② 按 J（预设 10，工程师可设）定截面 | `SectionSizing`：舌片各处、舌盘交界、孔缘与各级环；舌片厚闭式 I/(J·最窄宽)；叉臂随切口加厚（R29） | `SectionSizingTests`／`TongueResizeTests`／`TabArmBandTests`／`DesignJTests` |
| ③ 温场／电场定孔 | `FieldPlacement`（槽心角、舌孔孔心按场写回，开孔后冻结）；舌孔形状族比价（R13／R23） | `FieldPlacementTests`／`MinHoleRadiusTests`／`ShapeFamilyTests` |
| ④ 终验全体 J < J+1 | 判据「法兰截面 J」限 = 设定 J + 1；熔化只判断不加厚（R22） | `SectionSizingTests.JCheckOf`；`MeltFloorTests`／`MeltIsNotASolutionTests`（慢） |
| 形状搜索 | 盘径／舌宽／锥形（R38）局部搜，两族各自最轻不比重量（R32），⑥⑦并行（R36） | `ShapeSearchPlanTests`／`ShapeSearchTaperFamilyWiringTests`／`ShapeSearchParallelResultTests` |

## 6. 慢门实跑（发版前门）

| 项 | 结果 |
|---|---|
| 非 Rhino 慢门 23 条 | 22 通过、1 失败（见下）；最长的三条：SolveCostRegressionTests 2 h 9 m、BestShapeFineMeshTests 盘56舌56 1 h 45 m、ShapeSearchParallelResultTests 1 h 44 m；总计约 9 h |
| 失败的那条 | `PerPlateDivergeTests.爆炸不许被印成没变好`：门自己的防空转断言把它判红——09-06 的巨孔坏例在 J 定截面新链下第一轮就停在「法兰截面 J（没有旋钮能治）」，一根旋钮都没抬、一行「没变好」都没印。处置：案例换成 R36 对拍里确实印过「没变好」的候选（2 段、盘Ø66／舌宽35、MaxRounds=2），不变式「没变好括号里必须是像样的物理量」一字不动；单独重跑 **通过（13 m 3 s）** |
| 要开 Rhino 的三条（Export3dmMatchesSolvedTests／ExportOptimized3dmTests／ShapeFamilyTests） | 不能与提交钩子同时跑（钩子按映像名清 Pt_Optimize.Geom），在本次提交之后单独跑，结果补记在 `deliverable/APP功能验收_慢门记录_2026-09-11.txt` 末尾 |

逐条记录：`deliverable/APP功能验收_慢门记录_2026-09-11.txt`。

## 7. 已知限制（验收时如实记录，不算不过）

- 「两个都算」下核算整线只对页面那族做细网格精算；另一族要在「搜形状结果 ▾」里选它再点一次核算整线（R32 未做项；搜形状里 R38 已两族各精算）。
- 使用说明页正文是 WebView2，`--uishot` 抓不到（页面本身有内容），说明书验收以 `deliverable/说明书_渲染样本.html` 与走查 3／31／34 为准。
- 用户拍脑袋画的 Y 舌片示例 `deliverable/拍脑袋Y形舌片_示例_Pt_Heater4_2026-09-10.3dm` 只有「默认值」一层且含整套装配，读回工具认不出法兰（要把法兰单独放到「法兰」层才能读）。
- 走查里 17／18／20 等几节开头仍报「防抖定时器挂着」，只是警告（那几节改了控件没收拾），不影响判定。
- 慢门里 `PerPlateDivergeTests.爆炸不许被印成没变好` 的旧案例在 J 定截面的新链下走不到它要验的分支，已换案例并重跑通过（§6）。

## 8. 结论

五页、九链、二十五条命令逐条有机器门或抓图撑着；四道机器门本次实跑全过（快门 804、走查 515 项、抓图 17 张 + 表格自检、四次提交过钩子）；慢门结果见 §6。第 1 条工单的边角料已补齐（R35–R39），剩下的限制已在 §7 如实列出，不再有「看起来正常的错数」这一类未交代的东西。
