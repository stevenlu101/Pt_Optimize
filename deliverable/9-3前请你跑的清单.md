# 9/3 前请你跑的清单

> 目的：额度恢复前，先把**只有跑一次才拿得到**的数据攒好，9/3 之后我直接拿它改代码，
> 不用再花时间去跑。
>
> 每条都写了：**怎么跑 · 多久 · 把什么发我 · 我要它干什么**。
> 按重要性排，做不完就从上往下做。

程序在：`D:\WinForm\Pt_Optimize\Pt_Optimize\bin\Release\net8.0-windows\Pt_Optimize.exe`

---

## ① ★★★ 你操作一遍界面，告诉我哪里卡住（**只有你能做**）

**怎么跑**：直接双击开程序，按页签顺序走一遍：

```
设计记录 → 载入设计记录（选 0.8 档）
① 先决条件      看看它说什么
③ 整线核算      点「核算整线」，等它跑完
                 再点「自动定厚」
④ 定尺寸/搜形状  看看解锁了没有
⑤ 交付          试试能不能出图
```

**发我**：

- 每一步的**截图**（尤其判据表那一块）
- 哪一步你**不知道下一步该点什么**
- 哪一句话你**看不懂**（特别是还有没有 ②′ ③ ⑤ 这种代号漏网）
- 哪个按钮点了**没反应**，或者反应跟你预期的不一样

**我要它干什么**：你要的「傻瓜式操作」和「新的操作说明」，得先知道**你在哪里卡住**。
我今天自己抓图抓到一处漏网（舌长提示里的「判据 ⑤」），但我抓的是静态图 ——
**点下去之后的流程只有你能验**。

> ⚠ 我已经知道的一个洞，你会撞上：③ 页解完之后 ⑤ 交付会解锁、能出图，
> 但那个数**没经过网格无关复核**。你如果撞上了，请确认一下界面有没有拦你。

---

## ② ★★★ 0.6 档的敏感度矩阵

```bash
Pt_Optimize.exe --cli --quiet --sensmatrix --wall 0.6 > D:\sens06.txt 2>&1
```

**多久**：约 15 分钟　**发我**：`D:\sens06.txt`

**我要它干什么**：今天把 t₂ 加进求解器旋钮表，依据全部来自 **0.8 档**的矩阵。
而这个项目已经实测过「**灵敏度会随形状变号**」（窄舌 −1.4、宽舌 +0.056，方向相反）。
⇒ 0.6 档若给出不同的符号，那个决定就得改。**这是目前最可能推翻今天结论的一跑。**

---

## ③ ★★ 0.8 档重新做网格无关复核

```bash
Pt_Optimize.exe --cli --quiet --solve --verifymesh --wall 0.8 > D:\w08cg.txt 2>&1
```

**多久**：约 20–40 分钟（换 CG 之前要 1 小时）　**发我**：`D:\w08cg.txt`

**我要它干什么**：0.8 档上一次复核是**换 CG 之前**跑的，而 CG 那一改把停机判据
从「步长」换成了「真残差」，解的位置微移过。0.6 档已经用新代码重跑过（③ 9.453，全过），
0.8 档还没有。两档都要在同一套代码下才谈得上对比。

---

## ④ ★★ 逐级定厚求解器的第一次数值验证

```bash
Pt_Optimize.exe --cli --quiet --leveltest > D:\leveltest.txt 2>&1
```

**多久**：不确定（**这段代码写好之后一次没跑过**，可能几分钟，也可能报错）
**发我**：`D:\leveltest.txt`，**报错也要发** —— 报错本身就是结果

**我要它干什么**：`LevelSolver` 是给 `.3dm` 那条路用的求解器，写完至今**零次运行**。
本项目的经验是「写好没跑过的代码 = 还没写」（`--monotone` 就是这样，一跑就发现
第一版符号是反的）。

---

## ⑤ ★ 完整灵敏度扫描

```bash
Pt_Optimize.exe --cli --quiet --vary > D:\vary.txt 2>&1
```

**多久**：约 27 分钟　**发我**：`D:\vary.txt`

**我要它干什么**：界面上好几条提示语里的斜率数字（壁厚、纤维保温、板厚那几条）
出处都是 `--vary`。这些数**上一次量是什么时候我说不准** —— 而这半个月模型改了很多处
（CG、真残差、逐片求解、渐变环放开）。重量一次，对不上的就是要更正的。

---

## ⑥ 图纸那条路的端到端（有 `Pt_Heater1.3dm` 才跑）

```bash
Pt_Optimize.exe --cli --quiet --from3dm <你的.3dm路径> --verifymesh > D:\from3dm.txt 2>&1
```

**多久**：不确定　**发我**：`D:\from3dm.txt`，报错也发

**我要它干什么**：解析路（程序生成形状）与图纸路（读 `.3dm`）**从来没有逐条判据对过账**。
两条路算同一个零件应该给同一组数，对不上就是有一条错了。

---

## 跑的时候注意

- 这些命令**都可以中途 Ctrl+C 停掉**，已经印出来的部分仍然有用 —— 发我就行。
- ②③⑤ 会跑很久，中间会一直刷进度行（每 20 秒一条）。**只要还在刷就是活的**，
  别当成死机（这一条今天刚补过：以前它会静默几十分钟）。
- 磁盘上会多出 `1.0/`、`figs/` 这类图片目录，可以直接删。

## 我这边 9/3 之后接着做的

```
界面     网格无关复核补成按钮 + 出图闸要求它（今天查出来的交付级洞）
         判据代号清干净（Core 的 Note 那一批还没动，我今天切太粗改坏过一次，要重来）
         两个「不得用于交付」的开关移出工程师参数表
说明     按你①的反馈重写操作说明
清单     第 12 件（理想化无关性对账）、四叉树各向异性版
```
