# 保温求解器实现计划（2026-09-14 Opus 5，只读规划，A～D 合入后实施）

## 0. 已知量（出处都在 deliverable/）
| 量 | 值 | 出处 |
|---|---|---|
| 管保温对铂重 | 5 / 7.5 / 8.5 / 10 mm 对应 4246 / 4041 / 3982 / 3909 g（设计电流降，舌片厚和板厚下角跟着降） | 第一轮 R0/R1/R2，第二批 B3 |
| 片0 圆盘保温 9→10.5 mm | 抽热 6.048→4.108 W，管根 +4.82 K，折合 R≈2.48 K/W；片1 管根只动 −0.15 K；段电流两次都是 1072 A | 第二批 B1/B2 |
| 抽热对管根的灵敏度 s | 0.171～0.357 W/K，管根 ±5 K 内是线性的 | 管根接线与灵敏度 |
| 整面压接对比外圈钉住 | 抽热 +16.4～+34.3 W，舌区峰 −12.6～−20.1 K | 压接整面接触AB |
| 单片耗时（细带） | h=1 不到 1 s，h=0.5 约 2～3 s，h=0.25 约 16 s | 离散误差预算 |

⚠ 第一轮、第二批、方案 A 都按「外圈钉住」算。A 合入后，这些数据里窗口的位置全部作废，只剩方向还能用。

## 1. 搜索结构
**变量**：管保温 T、逐片圆盘保温 D_j、逐片舌保温 W_j，都是「层数 × 0.5 mm」。
- 0 层就是裸露：ShellThermal.cs:321 里厚度小于 0.05 mm 按铂表面算，有出处，不再用 0.3。
- 上界暂取 20 mm（41 层）。**这个上界没有出处，要用户给。**

**外层：管保温 T，枚举，不走步。** T 越厚铂越轻，但冷侧越差（B1→B3 片0 增量温降 15.0→22.8 K），可行性不一定随 T 单调。做法：先按 1 mm 步枚举 5～12 mm，再在最厚的可行档两侧补 0.5 mm。每换一档重算设计电流、舌片厚和板厚下角。

**中层：工作点不动点。**
1. 整线跑两个稳态（带玻璃、空管），并行。
2. 逐片取工作点：接头电流 I_j、管根 T*、抽热 Q*。再取管侧响应 R_j：段求解器单独扰动抽热，不解法兰。
3. 内层逐片选点，得到新设计，回到第 1 步。

四片选中的格点和上一轮逐位相同，才算收敛。如果在两点之间来回跳：两点都上整线，用同一条规则按整线值判，不许随手挑。首轮用导航整线，最后两轮必须用 0.5 mm。

**内层：逐片 (D, W) 格点。**
- 工作点固定，每个格点单片解一次，再用线性闭合修正管根：T根 = T* − R(Q − Q*)。片0 的 sR≈0.53，不闭合会错几 K。
- 每个格点两个稳态各解一次。
- 四片之间只通过管根（邻片影响不超过 ±0.8 K）和电流（B1/B2 电流不变）耦合，所以四片同时独立算：确定性，可以并行。

**每步次数与墙钟**（单片按 18 路并发估，整线两态并行）：
| 步骤 | 求解次数 | 墙钟 |
|---|---|---|
| 整线 0.5 mm × 2 态 | 2 | 8～14 min |
| 管侧响应 R | 段求解器 5 次 × 2 态 | 待测，估不到 1 min |
| 粗筛 h=1 穷举（41² × 4 片 × 2 态） | 约 13.5k | 约 9 min |
| 单调验过后改走阶梯 | 约 2k | 约 1.5 min |
| 入围点细算 h=0.5（含闭合复算） | 约 1k | 约 3 min |
| 选中点做理查森（加算 0.25） | 8 | 约 1 min |

- 一次中层迭代约 17～27 min，每个 T 档约 1 h。
- T 粗扫只做导航整线加粗筛：每档约 12 min，走阶梯后约 5 min。
- 全程估 2.5～4 h。

## 2. 粗筛与入围
- **网格**：单片 h=1.0，和判决网格同一族：同一个细区半径、压接锚点、细带、整面压接。
  - 不用 2.0 mm 导航网格定工作点。锚点改动之前，2.0 和 1.0 的净流入是 −6.53 对 +0.84 W（R48_导航网格偏差），A 合入后没量过。
- **偏差**：每片每态在工作点取 9 个格点（四角、四边中点、中心），h=1 和 h=0.5 各算一次。
  - 偏差界 B_c = 1.5 × max|Δ|，下限 0.05 K／0.02 W；冷侧另加 R·ΔQ。
  - 参考量级：整面压接 h=1→0.5 抽热差 0.07 / 0.37 W，峰值差 0.45～0.71 K。
- **入围**：两态所有判据都满足 slack(h=1) ≥ −B_c 才入围。
  - 不漏的门：细算后若有入围点 |Δ| > B_c，就把 B_c 翻倍重筛，留痕。
  - 只有「差距不超过 B_c 的点全部细算后仍不过」，才可以说窗口是空的。
- **选点**：要求 slack ≥ U_c，U_c = 单片理查森 + 耦合剩余估计 + R·U_Q。然后按全序挑：
  1. 最小的 (slack − U)/U 最大；
  2. 总层数少；
  3. D 小。

  ⚠ 这条全序是设计偏好，**要用户确认**。
- **单调**：每片每态第一次必须穷举，逐行逐列查序（容差 0.05 K／0.02 W）。全部单调才准走阶梯；阶梯每一步顺带验序，一旦破序就退回该列穷举。

## 3. 代码怎么改
**重写**
- Solver.cs:263–264：分派表去掉 Knob.Insul，行键换成 B 的热侧／冷侧键。
- Solver.cs:295：删掉「舌保温压到下角」。保温改由新的 InsulationSolver 在 Solver.cs:430（FieldPlacement 之后）每轮重选。
- Solver.cs:754 ChooseKnob、1119 RaiseUntil：量的裕度换成「这个几何下保温格点能达到的最好裕度 g*」。二分和前提自检保留。
- Solver.cs:1033 TightenOnJudgeMesh：名单去掉 Insul（保温直接在判决网格上选）。
- Solver.cs:1667 PlateSlack：接 B 的公开逐片函数，不手抄基准和对数平均。
- Solver.cs:2266 InsLoMm、2289 QuantInsulMm=0.1：改成 InsulLayerMm=0.5 加层数上界。

**复用**：ApplySectionFloor(1484)、MeltFloor(1562)、CoverCheck(1299)、FieldPlacement(2028)、Finish(1365)、EvalRaw 的网格配方(1437)、RaiseUntil 的二分骨架。

**新增**
- Core/InsulationSolver.cs：格点、粗筛与细算、线性闭合、全序、不动点、T 枚举。每轮打印选中格点、各判据裕度、判据取自哪一片。

**提成公开函数**
- LineRunner.cs:1377–1709 的逐片法兰体提成 `SolvePlate(c, j, I, T根)`：RunOnce 和求解器都调它；R48ErrorBudgetTests:80–101 的手抄改为调它。
- LineRunner.cs:1307–1369 的段循环提出管侧响应函数，用来求 R_j。

**后续**
- DesignSpec.cs:83 TubeInsulMm 改成逐段数组，牵连 BuildCase(923) 和 DesignCurrent.cs:94（那里写死了「每段同一根管」）。
- LineDesignPage.cs:1595 的舌保温输入改为只显示；报告给出管、盘、舌的厚度分布。

**要声明的记录值**：SolverIsSeedFreeTests、R48TightenOnJudgeMeshTests、SolverFineRootTests、ExportShowsSolvedKnobsTests、BranchMarksAreReachedTests。

## 4. 与花铂旋钮配合
- **先保温，后铂**。某片在当前几何下格点全空，才动板厚和环 t₁ t₂ r₁ r₂。这些旋钮只抬，二分到 g* ≥ U 的最小值，仍是最小可行点。抬哪根，由「夹死窗口的是哪一对判据」加现有的当场比价决定。
- 花铂旋钮每次探针都重选保温（阶梯 + h=1，约 20 s）。不许在保温固定的前提下判「旋钮有没有用」，那正是 TightenOnJudgeMesh 修过的病。
- 槽和孔（省铂）放到第二阶段「比铂重」：从 0 往上二分，到 g* 仍 ≥ U 的最大值为止。管保温 T 各档之间的比较也放在第二阶段。

## 5. 最小可行实验
**设置**：W08 几何，T = 7.5 mm，带玻璃和空管两态。两个角各走中层不动点，最多 3 轮：
- 角 L：全部 0 层；
- 角 U：全部 40 层。

**跑前写死的判读**
- J1 单调：片0、片1 两态穷举，超容差的序违例为 0，才允许走阶梯。
- J2 不漏：隔一取一共 441 点算 h=0.5。「h=0.5 可行但被粗筛剔除」的点数为 0，并且 9 点定的 B_c 能盖住这 441 点的 |Δ|。
- J3 闭合：每片 5 个入围点加 4 个角点，按闭合后的管根重解，|ΔQ| ≤ 0.1 W，峰值差 ≤ 0.2 K。
- J4 与起点无关：两个角的终点，四片两态选中格点逐位相同。不同就两个都报，然后停。
- J5 弱耦合：内层预测的裕度和整线判出的差 ≤ max(U, 0.5 K／0.3 W)。
- J6 算力：18 路并发效率 ≥ 70 %。

**成本**：约 2～2.5 h。

## 6. 风险
1. 整面压接后抽热大涨，所需保温可能超过 20 mm 上界。顶到上界时报「到顶」，不报「无解」。
2. 共用片热侧有一项是管根本身高于基准（R48_压接对齐与保温台阶 §8：1～93 K），法兰保温救不了。内层要识别出来交给外层（管保温沿管长分布），不许返回假解。
3. 共用片抽热「各半」还是「双扣」没定。B2 对 B5：片1 抽热 −3.53 对 −2.68 W，片2 热侧 +5.70 对 +7.47 K，R_j 会跟着变。
4. 单调可能不成立。历史上舌保温对圆盘区最高温「升 4 降 2」；盘半径处还有敏感度拐点。
5. 冷侧 U 里有 R·U_Q，约 1 K。窗口若比 U 窄，会出现大片「判不了」。
6. 空管和带玻璃两态的窗口可能不相交，要按态报是哪条判据夹死。
7. 图纸路径不支持逐片圆盘保温（LineRunner.cs:1490）。先只做解析路径，图纸路径明确拒绝。

**notes_for_handover**
- 保温求解器计划已出。实施前要用户回答三件事：保温层数上界、同铂重可行点之间的选点全序、共用片抽热「各半」还是「双扣」。
- 第一轮、第二批、方案 A 的数据在整面压接合入后只保留方向，窗口位置作废。
- 单片法兰体和管侧响应要先提成公开函数，再写求解器。