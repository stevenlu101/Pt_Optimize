<!-- HANDOVER 顶部节草稿（C4′，F7 自适应）。节号由编排者合并时定（本稿暂记 §0.-2x）；不要直接当成 HANDOVER.md 的现行内容。第一版（决 29 (1)）的草稿已作废，被本稿整份替换。 -->

## 0.-2x ★★★★ F7：细区半径**自适应**（决 29 改定）—— 单一来源的计划：初值 = 盘／孔半径 + 设计热长度，解后按热点放大，到板料外缘仍盖不住才拒答 —— 2026-09-23，Opus 5.5（C4′，云端 Linux 会话）

> 依据：HANDOVER §0.-21 决 29（业主 2026-09-23 16:1x 改定为「自适应」，①～⑦）；执行计划各组详稿 C4 方案 (3) 的通用版。完整记录（单一来源、放大规则、各处消费者、门表含覆盖／不覆盖／阈值、三支归因、审查逐条处置、重录、待办、成本）：
> `deliverable/R48_F7_细区半径解耦_实施记录_2026-09-23.md` **§10**（§1～§9 是第一版 决 29 (1) 的实施，作废、保留作历史）；**§11 是审查后修改**（32 条核实成立的发现逐条处置、慢门在终版代码上重跑）。
> **没有一个阈值为了过门而挪动**：热点检查 `MeshAdapt.PeakVerdict` 与 `PeakMarginMm = 10` 原样。耗时一律「争用下量得、不作判词」（同机另有别的会话，load 5.7～26）。

### 甲　机理与改动

- **第一版为什么作废**：决 29 (1)（**开发者缺省，不是业主原话** —— 第一版记录与草稿把它写成「业主决定」，已更正）= 半径 max(盘半径, 孔半径) + 10，与热点检查「峰 + 10 ≤ 半径」相顶：舌区峰按定义在盘外，凡带舌片的设计「◆ 加密复算」每档判「本次复核不算数」（审查 L1／P1 阻断）。这条推理还要两个前提：按半径分区；C3（决 28 跨界格两区都算）合入之前（审查 L4）。
- **现在：细区半径 = 计划**，全仓只有 `Core/MeshAdapt.cs` 一处：
  - **初值** r₀ = max(盘半径, 孔半径) + ℓ_t。ℓ_t = √(kA/β′)，**复用停机放大闭式 1 + ℓ_t/Δx 的同一个函数** `SegmentSolver.EndTempFixedPointAt`；闭式在管根温度处取，初值阶段没有场 ⇒ 取控温点（与段解自己报的 `DecayLengthMm` 逐位相同，快门钉住）；各段取最大。它是**管**的热长度（业主指定的量，不是法兰板上热扩散长度的物理推导；W08／W06 上够用是实测、不外推）。~~只决定初值、即只决定成本~~ →（审查 L-2／R-1 更正）初值**不影响热点检查能否放行**（盖没盖住由解后核热点 + 放大判），但放大 0 次时它就是判决网格的半径，以离散误差量级改动判据值（判决 4 行 ≤ 0.042 K／0.027 K／0.012 W，W06 盘10 半径 59 → 60.513 同格数差 0.057 K，导航 ≤ 0.19 K；无判词翻转），另外决定成本。初值超过上限时截到上限（审查 L-1；R ≥ 上限网格逐节点相同）。W08 ℓ_t = 23.697 ⇒ r₀ = **53.697 mm**；W06 20.513 ⇒ **50.513 mm**（旧 59.0，(1) 40.0）。
  - **放大**：每次整线解后读最远热点（与 `MeshVerify.Run` 核的同一个量），r + 10 > 半径 ⇒ 半径 := min(上限, max(r + 10, 半径 + 一粗格))，重建网格再解；半径只增不减，每次至少一粗格 ⇒ 有限步。一粗格在加密复算里取**当档**粗区（11·h/2，随档减半 —— 开发者缺省，改成与档无关的量【待决定】，审查 L-3）。
  - **上限** = 板料外缘（材料包络 `MaterialEnvelope` 的最大半边长，到这里整块板已全是细格）；W08／W06 = 140 mm。到上限仍盖不住 ⇒ **拒答**：「细化半径已放大到上限，仍盖不住热点 … 这次复核的温度类判据不算数」（与 `PeakVerdict` 同判词口径）。理由照实写（审查 L-1／R-4／F3 更正）：细区已铺满板料、**不是**网格分辨不出梯度，是热点检查规则（r 峰 + 10 ≤ 半径）字面执行的**口径拒答**；实际被拒的条件是「最远热点离管轴的半径 > 板料最大半边长 − 10」（W08／W06 r > 130），不是「贴着板料外缘」，也不归给设计。**【待决定】**：全细网格上还要不要按「峰 + 10 ≤ 半径」拒答；径向 r 与方带 max(|x|,|z|) 在上限处不一致（舌尖角的热点恒被拒答）。定之前统一在拒答一侧。
  - **记录**：初值、余量₀ 与输入（段、T、kA、β′、壁、牌号）、上限、每次放大的原因与数、终值、放大次数 —— 进 `FineRadiusPlan.Describe()`，挂在 `LineCase.MeshFineRadiusPlan`（只作记录）、证据头（`EvidenceHeader.ForLineCase` 多一行）、`MeshRecipe.FineRadiusMm`（生效参数记录）、各结果对象（`MeshVerify.Result`／`SolverResult`／保温搜索与可行窗口报告）。
  - **改回**：`tabLengthFactor = 0.35` ⇒ 旧规则逐位、不放大；`adaptive = false` ⇒ 新初值、不放大（只供归因）。改回**不复原**改前的两处（审查 R-1／R-2／T6）：导航支半径（Solver 没给半径的导航求根、可行窗口导航扫描：改前 50，改回计划给 59 —— 审查后新加只供门的 `SolverOptions`／`InsulWindow.Options.NavUsesCaseDefaultRadius` 可落回 50）；保温搜索与可行窗口的「峰位算不出 ⇒ 判不了」不看 Adaptive（保留、登记，【待决定】）。改回计划下四个循环没有实场逐位门。
- **谁用它**（审查 R5／P2／F1／L2／R2／P6／L3）：加密复算（盖不住 ⇒ 放大、从第一档重来，档间只比同一半径；到上限当场拒答、不空跑完阶梯 —— P8）、求根（导航遍后放大给第二遍；终局复核后盖不住 ⇒ 放大、重做最后一遍与终局复核；到上限 ⇒ **判不了**；**导航求根不再落回算例缺省 50**）、保温搜索与可行窗口（跑完核全部整线解，盖不住 ⇒ 放大、整趟重跑；到上限 ⇒ 不给建议／判不了；可行窗口导航支也取计划）、图纸细网格重解（取复核终值 ＋ 解完核一次，界面，未编译）、`--meshadapt`（删掉手抄的 0.35·舌长，取计划、印热点句，不放大）。终验可行窗口从复核终值起扫（审查 F4）；只导航重做前旋钮回约束盒下角（审查 F1）。
- **业主 ①～⑦ 逐条对照**（审查 R-3；「部分做到」一律是开发者缺省、可改）：

  | 业主原话 | 做到／部分做到 | 在哪 |
  |---|---|---|
  | ① 初值 = max(盘半径, 孔半径) + 余量₀（该设计热长度，同一函数） | 做到；超上限截到上限是开发者缺省（L-1） | `MeshAdapt.FineRadiusPlanOf`／`ThermalLengthMm`／`CapToPlate` |
  | ② 导航解后读最远热点，放大后再建判决网格 | Solver（有第二遍时）做到；加密复算每档解后都核（超集）；保温搜索与可行窗口整趟跑完才核（开发者缺省）；**部分做到**：图纸细网格重解只核一次不放大、图纸自动定厚不核、界面「核算整线」／QuickCase／LineDump 单次场解不经计划 | `Solver.Solve`、`MeshVerify.RunCore`、`InsulationSearch.Run`、`InsulWindow.Measure`；界面 3918～3924 |
  | ③ 判决解后再校、只增不减、每次至少一粗格、上限板料外缘、到头才拒答 | 加密复算、Solver、保温搜索、可行窗口做到；「一粗格」取当档（开发者缺省）；**部分做到**：图纸细网格重解没到上限就判不算数（交给「◆ 加密复算」放大）；全细网格上的拒答口径【待决定】 | `GrowFineRadius`／`PeakAtCapVerdict` |
  | ④ 初值、每次放大的原因与数全部印出、进配方与证据头 | 做到（`Describe()`、证据头、`MeshRecipe.FineRadiusMm`、各结果对象）；`DiscardedTiers`／`RadiusTrace` 没有界面呈现方（可选项 F7，未做） | `FineRadiusPlan` |
  | ⑤ PeakMargin 一个不动 | 做到 | `PeakMarginMm = 10`，计划规则门钉 |
  | ⑥ 改回参数保留旧规则 | 半径值做到；**部分做到**：导航支 50 与「峰位算不出 ⇒ 判不了」两处不复原（见上） | `LegacyTabLengthFactor`、`NavUsesCaseDefaultRadius` |
  | ⑦ 只许一个来源，导航／判决／保温搜索／可行窗口／图纸细网格重解／--meshadapt 全走它 | 规则只此一处（F7 门 f）；**部分做到**：单次场解（核算整线、QuickCase、LineDump）与图纸 `FlangeAutoSizer` 导航搜索（图纸自动定厚）仍用算例缺省 50 —— 统一与否【待决定】（10.9-4／10.9-5，不替业主选） | 门 f 白名单 |

- **热点位置的已知漏报**（审查 R1）：盘区峰、舌区峰全格取最大，不受影响；`LocalStabRMm` 继承 #33（局部热稳定前 12 候选预筛，真最小值落点 r ≈ 66～70 mm，报出 31.5／33.1）。局部热稳定是参考量。**决 99【待决定】**（审查 L-4／R-2 更正：不替业主排除选项 B、C）：若选 A（修成全格精算），自适应会把半径放大到约 76～80（上限 140 内），不会像 (1) 那样全体拒答；但所有加密复核证据仍要重跑，W08／W06 放大次数从 0 变成每次调用至少 1 次（别的设计未测），每次放大的代价见戊，判决格数随半径上涨（+10 mm 实测 +12 %，推到 76～80 约 +27～33 %，外推未实跑）—— 自适应不是零成本，是把一次性重跑换成每次调用多付一遍。可另提给业主的选项：r₀ 取上一次终值或导航遍热点，免掉每次都放大（【待决定】）。在那之前本节的终值只对报出的位置成立。
- 舌区峰 30.50／31.02 = 盘半径 + 半格（判决 h 1.0／导航 h 2.0 的分区边界外第一圈格），不是另起的舌根峰（审查 L6）—— **这是 cff38c6 单树、C3（决 28）合入之前的数**（审查 M4）：C3 合入后跨界格也进舌区峰，W08 舌峰落到 29.504（< 盘半径 30）。

### 乙　门（Linux 镜像实跑；阈值没挪）

| 门 | 结果 |
|---|---|
| F7 门 a 解耦（快） | **过**：舌长 80～230，初值逐位不变（53.697／50.513；手造 Shape 53.697／57.697）；改回 40.0 → 90.5（改回 ⇒ 红） |
| 改回门（快） | **过**：改回半径与 F7 前公式原文逐位（两个入口、含负号舌长）；计划不放大；热点句抽取逐字同句 |
| 计划规则门（快） | **过**：ℓ_t = 段解 `DecayLengthMm` 逐位；初值、上限、放大量按定义式逐位（盖住／一般／大步／截到上限／到上限拒答／峰位 NaN／改回／量不到上限）；`PeakMarginMm = 10` |
| F7 门 c 单向（快，合成注入生产主循环） | **过**：热点两次外移 ⇒ 两次放大，半径序列非降，每次放大后从第一档重来（作废 1 ＋ 3 档），收敛那三档都在终值半径上，热点句 null |
| F7 门 c′ 单向（慢，实场） | **过**：W08 盘20 初值故意给 35：第 1 档 r = 33.14 ⇒ 35 → 46 ⇒ 重来，2.0／1.0 mm 两档都盖住（r 33.14／31.50），终值热点句 null（24.3 min） |
| F7 门 d 终止（快，合成） | **过**：假热点 = 上限 + 20 ⇒ 放大到上限 140、再解一次、拒答返回（解次数 = 放大 + 1，只在第一档，P8）；判词与原句逐项；峰位 NaN、改回两支走旧路；最坏情形放大 ≤ ⌈(140 − 53.697)/5.5⌉ = 16 步 —— **只对热点出在第一档成立**（审查 L-3） |
| F7 门 d₂（快，合成；审查 L-3 新加） | **过**：热点只在第 2 档越界 ⇒ 一粗格 2.75（当档）、放大 32 次、整线解 66 次、作废 64 档，最后拒答 |
| F7 门 L1（快；审查 L-1 新加） | **过**：初值超上限截到上限；「初值在上限之上」与「从下面放大到上限」两条路判词相同（纯函数 ＋ 生产主循环）；到上限的拒答理由写「口径拒答，不是分辨率拒答」，量不到上限才保留分辨率理由 |
| F7 门 R1（快；审查 R-1 新加） | **过**：只供门的改回开关 `NavUsesCaseDefaultRadius` ⇒ Solver 导航落回 50、可行窗口导航支记 50；生产 ⇒ 计划初值；给定半径 150 截到 140 |
| F7 门 F1（快，源码；审查 F1 新加） | **过**：只导航重做前 ToLowerCorner → ApplySectionFloor → NavPass |
| F7 门 f 单一来源（快，源码） | **过**：全部生产 .cs（含界面、命令行、Geom）里「0.35×舌长」0、`RequiredFineRadiusMm` 在 MeshAdapt 外 0、`PeakMarginMm` 在 MeshAdapt 外 0、细区半径赋值 19 处全在白名单（逐条写来源）；F7 前三行原文全被抓到 |
| F7 门 b 初值覆盖门（慢；原名「自适应门」，审查 T1） | **过**：W08／W06 × 10／20 判决，生产主循环：**放大 0 次**、终值 = 初值、最远热点 31.50、热点句全 null（**只对报出的热点位置成立**，#33 真最小值 r ≈ 66～70 在细区外，未覆盖 —— 审查 T2）；**本门未走到放大分支**（关掉放大或改回 59 按构造照样过）。审查后在终版代码上重跑：5／5 慢门过，数值与旧档逐位相同（`…_203435`） |
| F7 门 e 改回逐位（慢，对改前树 cff38c6） | **过 6／6**（覆盖 = 旧规则半径值 ＋ 固定半径单次场解上记录行中性；**不覆盖**改回计划走四个循环、改回不复原的两处 —— 审查 R-2／T6；审查后重跑：6／6 逐位（`…_203435`））：改回六行半径、格数、三条判据、铂重、轮数逐位，转储去掉 F7′ 两类记录行后 SHA 与改前逐字节相同。与门 b 各自断言（审查 F2）；到合并树上参照要重出 |
| 探针 g 初值敏感度（慢，只印） | 半径再大 10 mm：三条判据动 ≤ 0.019／0.015 K、≤ 0.007 W（4 个判决行） |
| 定向快门（68 类，46.9 min） | 371 条：过 362、红 9 = F6 后本就红的 6 条（NSliver 门1／门4、ClampRecipe d、ClampFace c、NMeshInject 门e、计数门）＋ 本单引起、随后改掉的 3 条（接线门 1 按变因重录；SolverFineRoot 两条：一条改代码不改门，一条源码门按变因改钉）。改后复跑 14 类 77／77 过；重建 `.linux` 编译 0 错 |
| 审查后（终版代码，2026-09-23 19:44～20:56） | 重建镜像 0 错 0 警告、Compile Remove 29 条（没有静默剔除）；逐类快门：F7 两档 10／10、Solver 源码门四类、可行窗口门、MeshAdapt／MeshVerify、FinishMesh、接线门 1、保温搜索、分工况、配方指纹等全过，`R48ClampRecipeTests.d` 仍红在导航段（与 F6 同数，F6 后本就红）；慢门类 5／5 过（1.09 h），数值与旧档逐位相同，新档 `…_203435`（实施记录 §11） |

### 丙　开 − 关归因（改前树 ／ (1) 固定 40 ／ 自适应；三条硬判据 最热铂 K／管根 K／净流入 W）

- 判决 4 行 Δ自适应−改前：W08 盘10 −0.002／+0.002／0.000，W08 盘20 −0.002／0.000／0.000，W06 盘10 −0.038／+0.027／+0.012，W06 盘20 −0.042／+0.021／+0.011（(1) 那一支 −0.029～−0.039 K 等）。单元 4164 → 3864（W08）、4188 → 3708（W06）。
- W08 导航 2 行 Δ −0.19／+0.13／+0.07 与 −0.18／+0.13／+0.08 —— 比判决行大且对半径不单调；~~是导航网格自己的离散误差~~ → 推测是导航网格（h 2.0、粗区 11）自己的离散误差，没有门验证；导航不作判决。表里「改前」= 59 只对 4 个判决行是改前树生产；导航 2 行的 59 是改前探针强制给的（审查 R-2）。
- **导航 50 对计划初值**（审查 R-5／F5 补；只导航那几条路 —— 界面自动定厚、`--solve` 不带 `--fine`、可行窗口导航支、「核算整线」—— 改前用算例缺省 50；单次场解开 − 关，不是求根）：W08 盘10／盘20 Δ(r₀ − 50) = −0.242／+0.158／+0.090 与 −0.234／+0.164／+0.095（格数同为 1026，没有过／不过翻转）；W06（50 对 50.513）三条判据、铂重、轮数逐位相同（`…开关归因_三支并列_本次开跑于2026-09-23_203435.txt`）。**求根一侧没做归因**（不覆盖）。
- 放大 0 次 ⇒ 位移全部来自初值（「新初值、不放大」一行与自适应去行后 SHA 逐位相同）。没有判词翻转。耗时与轮数争用下量得、不作判词。

### 丁　重录、没跑、不覆盖

- **已重录**：接线门 1 Linux fb2c3248… → **bc732f5d…**（变因「决 29 自适应」：转储多 4 行记录；去掉后回到 fb2c3248…，证据 `deliverable/R48_F7自适应_接线门1转储去行对拍_2026-09-23.txt`）。源码门 `SolverFineRootTests.网格该多细只有一处来源`、`MeshVerifyReachableTests`、`SolverWiredToUiTests` 按变因改钉（后两条镜像排除）。
- **审查后改钉**（按变因写明）：`R48UInsulWindowGateTests` 源码钉（终验窗口 `null` → `winOpt`，变因「决 29 自适应：窗口从复核终值起扫」，审查 F4）；`R48ClampRecipeTests.d` 的 h=1 段半径钉回旧规则 59（审查 R-4：记录取自 59 网格，F7′ 对这道门零位移，F6 重录只带 F6 一个变因；门 d 仍因 F6 红在导航段，消息与 F6 那跑同数）。
- **要重录、本树不做**：LineDump 六条 Windows（加变因「两类记录行」）；`R48NMeshGateTests` 门f 现役数（59.0 → 53.697／50.513）；`R48MStopTolCostTests` 门d 四行参照（**合并树上重做**，C2／C3／SEG／RING 也动场 —— 审查 F3）；成本门；J2／J3 与两份复原设计自证靶。~~界面「自动定厚」（导航求根半径 50 → 计划初值）~~ → 那是生产行为变更、没有受跟踪记录可重录（审查 R-5），开 − 关见丙。
- **合并时的阻断与必做**（审查 M1／M3／M4；本树基于 cff38c6，这些档在本树里不存在或不冲突，**本树改不了、合并提交里改**）：
  - M1（阻断）：已合入的 C2／C3／SEG 测试档还有 5 处调用被删的单参 `RequiredMeshFor(DesignSpec)`：`R48F3FaceHeatGateTests.cs:543`、`R48F4Cases.cs:62`、`R48SegContinuousRootSlowTests.cs:90` 改 `MeshVerify.RequiredFineMmFor(d0)`；`R48F4Cases.cs:68`、`R48SegContinuousRootSlowTests.cs:96` 改 `MeshVerify.RequiredMeshFor(d, p)`（p = 同函数里交给 BuildCase 的那个）。变因「决 29 自适应：签名加工艺参数」；这些门的数是否因初值 59／40 → 53.697 移位要重跑后按变因重录。`fix_mirror.py` 会把编译不过的测试档**静默剔除**：建镜像后核 Tests.csproj 的 Compile Remove 集合 = ui_excluded.txt（28 档）∪ {CliUiParityTests.cs}，共 29 条，多一条就是有档被剔了。
  - 同一次合并还有 Evidence.cs、ShellMesh.cs、R48PropsWiringGateTests.cs 三处文本冲突。接线门 1：头注两边记录史都留，`LinuxRecord` 常量暂取分支值，补一行「合并 C4′ 后：C4′ 单树记录 bc732f5d2a58850d0db597ab16981558ae1767369817f680f318915c2e23da24；合并树实数待本波合并完统一重录」。统一重录的中性证法：合并树上同时设 `GateRevertFaceHeat` 与 `ZoneByMaterialFraction = false`，剔掉 F3 改回说明那一句，再去掉 RING 24 行、SEG 1 行、C3 1 行、C4′ 4 行新成员行，SHA 应回到 fb2c3248…；回不到就逐条拆开找原因（M3）。
  - M3：已合入各节里「C4 不动／不改接线门 1」的前提过时 —— `R48F3FaceHeatGateTests.cs:48`、`:78`，HANDOVER.md:37，`deliverable/R48_F3_面发热_实施记录_2026-09-23.md:168` 改写或加前向注「C4′（决 29 自适应）→ bc732f5d…，转储多 4 行记录、数值场不动」；`R48SegContinuousRootSlowTests.cs:49` 里 C4 的描述「59 → 40」改成「C4′ 自适应：每个转储多 1+3 行记录」；NewMembers 正则补不补 C3／RING／F7 成员并进该档已有的【待决定】。
  - M4：HANDOVER.md:93、:134、:185 与 `R48SegContinuousRootSlowTests.cs:49` 加前向注〔→ §0.-2x（C4′／决 29 自适应：细区半径 = 计划，W08 初值 53.697／W06 50.513，要求 ≤ 43.697／≤ 40.513；(1) 的 40.0 作废）〕，不删原句；`R48F4ZoneShareGateTests.cs:437` 是条件句，不改。本树 `MeshAdapt.cs` 那句「舌区峰恒 > 盘半径」已补 C3 前提。
  - M2：`FlangeAutoSizer.CloneCase` 已照拷 `MeshFineRadiusPlan`（本树改了；`LineCaseCloneTests` 加了 Sentinel，镜像排除 ⇒ Windows W8）。
- **Windows 待办**（W1～W9，实施记录 10.9）：`Program.cs`（--meshadapt、--cgbench／--quadbench／--solve --fine、两处「本次复核不算数」不再宣布「判据以复核为准」—— 审查 P3）；`UI/LineDesignPage.cs`（精算／自动定厚签名、终验三关用复核终值、「⛔ 判不了」一支、`VerifyFactory3dm` 先造算例、图纸细网格重解用复核终值并核热点、`MeshVerifyRefusedWhy`）与 `UI/Flow.cs`（做过复核但热点拒答时不再说「还没加密复算」—— 审查 P4）；`tests/UiWiring`（~~3269 行签名~~ → 3269–3270 行已改为传 `VerifyFactory3dm` 工厂造的算例 `factory(1.0, 1.0)`，未编译；要核它与页面 `BuildCase()` 给出的 ℓ_t 逐位相同 —— 审查 M5）；镜像排除、本单改过却从没编译过的 5 个测试档：`LineCaseCloneTests`、`MeshVerifyReachableTests`、`SolverWiredToUiTests`、`R48J_SolverMeshAndMarkerGateTests`、`R48J_ExperimentProbeTests`（7 处签名，慢探针，至少编译 —— 审查 M5／R-6／M2）；审查后的界面改动（拒答快照 `_refusedSnap` 与 `FlowState.MeshVerifyRefusedAtCap`、`Flow.Next`／另存对话框／细网格重解按钮三处「做过了、判不了」、终验窗口带复核终值、自动定厚「会复现这张表」改成照实 —— 审查 F2／R-7／F4／F5）加一条 UiWiring 探针走「拒答 → 改参 → 重解 → 门控文案」；真图纸路径；文档 `docs/Pt_理论模型_v7.0.md` 893／1051／1064／1155／1243／1398 六处（审查 R6）。以上界面与命令行改动都是读着改、没编译。
- **合并后慢门**（门名带类名，审查 T5）：`R48NMeshGateTests` 门c（盘径 28～34 × 25 点，要印每点终值半径与放大次数）；F7 门 b／c′／e 在合并树上重跑（C3 改舌区峰取法、C2／SEG 动场，放大次数可能不再是 0；按读码推断 C3 把舌峰往里挪、C2 把局部热稳定格往里挪，放大次数应仍为 0、门 c′ 仍放大一次 —— 推断，没实跑）；F7 门 e 参照在「合并前一刻」重出，用存档的改前探针源码 `deliverable/R48_F7_改前探针源码_2026-09-23.cs.txt`（审查 T4）。
- **不覆盖**：放大分支在 Solver、保温搜索、可行窗口三处一次都没实跑到（W08／W06 放大 0 次，这三处整线解不能注入；只导航重做回下角只有源码门）；F7 门 b 没走到放大分支、它的 null 只对报出的热点位置成立（#33）；改回计划走四个循环的逐位、改回不复原的两处；P6 那一笔的**求根一侧**归因；单次场解（算例缺省 50：界面「核算整线」、QuickCase、LineDump）不经计划、不核热点 —— 统一与否【待决定】；图纸路径 `FlangeAutoSizer` 自己的搜索各轮不放大、图纸自动定厚不核热点 —— **【待决定】**（审查 R-3：对业主 ⑦／②③ 的部分做到，不替业主选）；别的设计、形状扫描、空管工况、更细的加密档；初值超上限（短舌设计）只在合成门上验过；52 个改签名测试档里 5 个没进镜像编译。

### 戊　成本

- 人时约 4 h。代码 Core +718／−61、界面与命令行 +74／−18（未编译）、测试新档 735 行 ＋ 旧档重写 187 行 ＋ 52 档改签名。
- 机时（争用下量得、不作判词）：定向快门 46.9 min；慢门 82 min；门 c′ 24.3 min；改后复跑 4.5 min。
- 生产：判决格数 −7 %～−12 %（初值比 59 小）。~~放大时每次多一遍整线解~~ → 每放大一次要重做的量（审查 F6／L-3 更正）：加密复算作废并重解已走过的 k 档（一粗格随档减半：第 1 档最多 16 次放大，第 2 档 32 次／66 次整线解，第 3 档 63 次／192 次，现役最坏约 503 次）；求根一整遍（`TightenOnJudgeMesh` ＋ 最多 `MaxRounds` 轮，界面自动定厚 40；只导航时整遍 NavPass）＋ 终局复核；可行窗口整趟重扫（约 1 h，共用 3 h 闸）；保温搜索整趟重跑（层数 × 约 20～37 min，无闸，目前只有测试调用）。W08／W06 现役放大 0 次 ⇒ 这笔为 0 —— **只对决 99 修好之前成立**（见甲）。界面「10～40 分钟」提示没改（可选）。
- 测试条数：F7 两档 ~~11 条（快 6、慢 5），对 cff38c6 **+11**~~ → 审查后 **15 条（快 10、慢 5），对 cff38c6 +15**（新加 F7 门 d₂／L1／R1／F1 四条快门；第一版 6 条里的 4 条慢档已删）；「应为」由编排者合并时改。

### 合并时在旧处补注（编排者合并时执行，只追加前向注、不删不改旧句；行号是 815ef53 上的，先 grep 原句定位）

① HANDOVER §0.-21 决 29 那一条（`grep -n '决 29：原缺省 (1)' HANDOVER.md`，约 :288）句末追加：〔→ §0.-2x：已落地并合入分支；W08 初值 53.697／W06 50.513，现役设计放大 0 次；初值超上限截到板料外缘并印出；单次场解半径 50、FlangeAutoSizer 各轮不核热点、全细网格上要不要仍按「峰 + 10 ≤ 半径」拒答等【待决定】见该节 丁〕
② HANDOVER §0.-25 那句「C4 实跑不动接线门 1」（`grep -n 'C4 实跑不动接线门 1' HANDOVER.md`，约 :37）追加：〔→ §0.-2x：C4′（决 29 自适应）→ bc732f5d…，转储多 4 行记录（算例.MeshFineRadiusPlan 1 行、各片 Recipe.FineRadiusMm 3 行），数值场不动；门 3 靶重录时要一并计入〕
③ HANDOVER §0.-24 门 d 那行「与 C4（F7）合并后的细区 40.0（要求 ≤ 30.0」（`grep -n '细区 40.0' HANDOVER.md`，约 :93）追加：〔→ §0.-2x（C4′／决 29 自适应：细区半径 = 计划，W08 初值 53.697／W06 50.513，要求 ≤ 43.697／≤ 40.513；(1) 的 40.0 作废）〕
④ HANDOVER §0.-24 第 10 条「合并 C4 后要求 ≤ 30.0，舌峰半径正好跨线」（`grep -n '合并 C4 后要求' HANDOVER.md`，约 :134）追加同 ③ 的前向注
⑤ HANDOVER §0.-23 那句「C4（F7 细区半径 59 → 40，CostKitCase／GateFCase 经 MeshVerify.RequiredMeshFor）」（`grep -n 'F7 细区半径 59 → 40' HANDOVER.md`，约 :185）追加：〔→ §0.-2x：C4′ 是决 29 自适应，半径 = 计划初值 W08 53.697（不是 40），且每份转储多 1+3 行记录〕
⑥ HANDOVER §0.-17 R 线那句「细区 1.000 mm／细区半径 59.0 mm（`MeshVerify.RequiredMeshFor`」（`grep -n '细区半径 59.0 mm' HANDOVER.md`，约 :924）追加：〔→ §0.-2x：F7 后细区半径由计划给（W08 初值 53.697），59.0 是 F7 前旧规则 0.35·舌长 的值〕
⑦ HANDOVER 那句「**F7**：细区半径仍跟舌长走（`MeshVerify.RequiredMeshFor`），盘径扫描里细区半径 59.0 → 62.9 mm 连续变」（`grep -n '细区半径仍跟舌长走' HANDOVER.md`，约 :1176）追加：〔→ §0.-2x：F7 已落地，细区半径不再跟舌长走〕
