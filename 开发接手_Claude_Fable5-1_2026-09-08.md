# 开发接手 · Claude Fable 5.1 · 2026-09-08

> 这是我（Claude Fable 5.1，2026-09-08 接手的开发 agent）**自己的 MD**。`HANDOVER.md` 是前任的全史（400 KB），
> `督导_*.md`／`督导/` 是督导的。这一份只记五件事：**主旨、你的要求、对照现状、你的答复与登记、我在做什么**。
> 规矩照旧：你说一条我登记一条（标 ✗），做完才改状态；✅ 必须附证据。

---

## 一、主旨与原则（你 2026-09-08 原话，逐条）

1. **主旨与目的**：一个**能用、能造，然后再节约铂金用量**的铂金直接加热优化系统。
2. 所有计算必须符合**第一性原理**；禁止凑答案，禁止一切作弊的方法。
3. 所有优化都必须有**理论计算依据**。
4. 目前 UI 的输入分两种：① UI 输入框输入；② 3DM 图档输入。
5. 优化手段：**自动搜索法兰尺寸**（须符合主旨与目的）
   - **A**：t1/t2（法兰阶梯厚度分布）、t0（舌片厚度）、r1/r2（法兰半径尺寸）
   - **B**：以计算温度场与电场（含舌片与法兰）为基准，找出适合挖洞的地方与形状
6. 若有不清楚或遗漏：先查项目内的 MD，再让你确认。
7. **所有对话时所实现的功能一定要落实到 APP 里**（2026-09-08 补）。
8. **如遇使用不当的理论、公式、计算与逻辑，可以推翻之前**（2026-09-08 补）。
9. **APP 操作流程三步**：UI 或 3DM 输入 → 法兰优化 → 计算结果与出图（2026-09-08 补；登记 R21，并入 R17 界面整理）。
   我的执行方式：推翻**要说出来** —— 推翻了什么、为什么它不当、影响到哪些已交付的数／已登记的状态；
   不许静默替换（那会让你以为的和事实不一样）。

---

## 二、查过的 MD，与现状对照

查了：`HANDOVER.md` §0.0 总纲、§0.0.3 要求登记表 R1–R10、开发计划、待办队列；`README.md`；
`docs/Pt_理论模型.md`；`督导_2026-09-07.md`；`督导/信箱.md`（第 1–16 封）。

| 你的要求 | 现状 | 依据（可自己查） | 缺口 |
|---|---|---|---|
| 主旨：能用 → 能造 → 省铂 | ✅ 与总纲一致：目标 = **全判据过的前提下铂重最小**。「能用」= 判据表（管孔净流入、圆盘区最高温、法兰增量温降、管 J、热稳定、升温…）；「能造」= 焊接屈曲/烧穿板厚下界、图纸格 0.01 mm、圆盘盖得住管孔+焊脚、槽/孔留桥 | HANDOVER §0.0；`Core/Criteria.cs`；`DesignSpec.DiscFloorMm` | 补一条：孔径 < 1 mm 不考虑（R15） |
| 第一性原理、不凑答案 | ✅ 求解器**不搜索、不接受起点**：从约束盒下角求根，解与初值无关；判不了不算过；不收敛/不存在的场一个数不许引用（S1） | `Core/Solver.cs` 类注释；`SolverIsSeedFreeTests`；督导 S1 | 今天在修的回归正是这条的产物：下角漏了「不熔化」（见五） |
| 优化有理论依据 | ✅ 每次抬旋钮前**实测**「抬到上界判据变不变好」（单调 = 二分成立的前提）；候选之间按**每克铂买到的裕度**比价 | `Solver.RaiseUntil`／`ChooseKnob`；`--monotone`／`--sensmatrix` 实测表 | — |
| 两种输入 | ✅ UI 输入框；3DM 读几何后**逐级**翻成 基板 + r₁·t₁ + r₂·t₂ 交给解析路（09-08 晚，R8/R14），解析路走求根；3DM 模式自己的逐级定厚（旧搜索路 `SolveByLevel`）只剩「照图纸算基准」这一个用途 | HANDOVER R8/R14/R19；`Core/ShapeToAnalytic.cs` | 解析模型只有两级台阶：≥4 级的图纸拒翻并说明 |
| A：t1/t2 | ✅ 求解器旋钮（`Knob.Ring`=t1、`Knob.RingT2`=t2，逐片） | `Solver.Allocation`；R4 | — |
| A：r1/r2 | ✅ 旋钮（`Knob.RingR1/RingR2`，与 t1/t2 成对）；t=1（无台阶）时挪半径结构性无效，由每次实测淘汰 | 2026-09-05；R4 | 你确认这就是 A |
| A：**t0 舌片厚度** | ✅ 09-08 晚（R11）：**不是旋钮**，= I_设计/(10·舌片最窄有效宽) 闭式，与圆盘基板解耦；界面只读框显示 | `DesignSpec.SizeTongues`／`TongueThickMm`；`DesignSpecTongueTests` | 舌板开孔的 J 上界随之为 0（开孔就得整条加厚）—— 挖料该挖圆盘背侧 |
| B：挖洞的**大小与形状** | ◐ 圆盘背侧减重槽张角、舌板孔径、孔顺流拉长比 都是旋钮，由场比价决定；离散形状族量过、**未入候选** | R5/R6；`Knob.SlotSpan/TabHoleR/TabHoleAspect` | R13 |
| B：挖洞的**位置** | ◐ 槽的位置由一次场分析定死（移除优先级 = 导热贡献÷电流密度，槽心背对舌片）；舌孔孔心默认在舌片自由段中点（可手填），**不由场逐案定** | `DesignSpec.SlotsOf` 注释；`TabHoleXMm`；开发计划第 3 件 ✗ | R12 |

---

## 三、你的答复（2026-09-08）与登记

| 问 | 你的答复 | 登记（HANDOVER §0.0.3 要求登记表） |
|---|---|---|
| Q1 t0 拆成独立逐片旋钮？ | **可以** | **R11** ✗ |
| Q2 r1/r2 现状是不是 A？ | **是** | R4 维持 ✅ |
| Q3(a) 孔心位置由场逐案定？ | **是** | **R12** ✗ |
| Q3(b) 离散形状入候选？ | 圆角三角／方，**还有长椭圆与弯椭圆［仅用于法兰］** | **R13** ✗ （「仅用于法兰」我理解为圆盘区而非舌片；若指整片请纠正） |
| Q4 3DM 先做 R8？ | **是** | **R14** ✗（与 R8 同一件事） |
| Q5 制造约束还有吗？ | **孔径小于 1 mm 可以忽视** | **R15** ✗（最小孔径 1 mm；其余未再加） |
| Q6 MD 名字 | 再加我的名字（含型号） | 本档改名 `开发接手_Claude_Fable5-1_2026-09-08.md` |
| 09-09 下午：我解释欠账「熔化处置够不到舌片」，捋清 J=10 截面进下角后熔化不可达 | **可以，J 让工程师设定（实况风险工程师承担）；J 是设定值，J+1 是计算极限值；J 预设值为 10** | **R22** ✅（① 输入「设计电流密度 J」；熔化改判断工具，熔了就停「该解不存在」） |
| 09-09 下午：我报「R13 舌孔形状族到不了（舌片按 J 定厚后开不了孔）」 | **解法由法兰温场决定：接触处因导热偏冷 ⇒ 舌片挖孔（大小位置由温场定）⇒ J 超设计值 ⇒ 舌片重新搜形状 ⇒ 可能是倾斜 90° 的 Y 字形；叉口用「直椭圆 + 弯椭圆」组合或「圆角三角」（底朝盘）一个孔，不加新几何** | **R23** ✗（登记；做法见 HANDOVER R23 行：孔径探针里重定舌片厚、孔心与槽心照场落在舌根／舌侧、交界切口扣两种切口） |
| 09-09 傍晚：我说「要省铂不是把梯形舌端收窄」 | **不是把梯形舌端收窄（这并不是错，把它当成另一种解）** | 登记：搜形状里舌端收窄的形状（如 56/49/154、56/42/159）是**可行解**，按铂重排在后面、不淘汰；搜形状结果表列全部可行形状供工程师选 |
| 09-09 傍晚：我说「哪个轻由场和铂重说话」 | **也供工程师自行选择** | **R24** ✅：② 主行「搜形状结果 ▾」下拉，算过的每个形状都在，选一个写回页面再精算 |
| 09-09 晚：我按 Ø56 轨迹讲解法五步 | **跑的过程把①②③④⑤的状态显示出来，让工程师知道 APP 正在干啥** | **R25** ✅：② 页输出框顶上「APP 正在做：」五格状态条 + 命令行五步标题；认法在 Core.SolveStages 一份 |
| 09-09 晚：我说流水线加密不过后没有细网格重解 | （待答） | **R26** ✗ 登记：Flow 加 core.fineResolve |
| 09-10：用户要求 | **UI 必须有输入铂金管加热段数的输入框** | **R27** ✅：① 输入页段表正上方加 `NumericUpDown _segCount`（1–8，默认=段表行数），与段表行数双向同步（改框加/删行、表里加/删行框也跟着变），走同一条 `_segs` 出口，载入设计记录／图纸几何→参数天然接上 |
| 补 | **所有对话时所实现的功能一定要落实到 APP 里** | **R16** ◐（机制在：HANDOVER §0.-3 + `CliUiParityTests` 等三道门；覆盖率未知，因为「功能清单」没有分母）。每做一件，验收标准里加一行「界面上哪个按钮/哪句话」 |
| 补 | **UI 是否合理**（物件、排版、字体）；给工程师用，可改成更合理的操作，不必要的资讯或按钮可以不呈现；**放到最后做，改完截图确认** | **R17** ✗（现状我还没看过，不先下结论。到那一步：先 `--cli --uishot` 逐页截图看现状 → 提改法 → 改 → 再截图对照） |

### 工况（你 2026-09-08 给的，已核对代码）

- 铂金管升温 **20 °C/h** —— 代码默认 `RampRateKPerH = 20`，一致，不改。
- **J = 10 A/mm²** 是**升温期（空管）的依据**，功率可调；稳态硬判据仍是 **12**（你 08-25 厘清的三个数：15 一般／12 管壁 0.6 极限／10 实际使用最大）。
  你 09-08 选 **A**：不改代码。⚠ 不许把稳态判据改成 ≤10 —— 那会当场判掉已交付的 0.6 档（实测 10.961）。
- 你问「A 的工况下还会烧断吗」：HANDOVER §4.2s（2026-08-11 分析）温控 20 K/h 下**不熔**（起始电流 71 A、法兰峰值 1320 °C < 1768，共用片全程比管热 +215 K，端片安全）。
  ⚠ 但 APP 现在的判据 ① 只判「72 h 内到得了目标」，升温峰值 `TPeakC` 算了**没拿去和熔点比** ⇒ 并入 R20。

### ★★★★★ 设计因果链（你 2026-09-08 原话，全 APP 的主线；R18 按它重排）

> 「舌片截面 J 本来就 20–37」这句是我说错的：**如果横截面该加大就要加大，首先是可造可用，J 20–37 已完全不可用**
> （瞬间给这么大的电流也是不对的，电流使用的大小应该与铂金管升温速率有关）。

```
① 在 20 °C/h 下所需通过铂金管的电流（功率）是多少
② ⇒ 决定铂金法兰大小与尺寸（r1/t1/r2/t2）与舌片横截面积  [按 J = 10 为限制]
③ ⇒ 分析舌片与法兰的温场与电场（电流密度低的区域就是定位孔的形状与位置）
④ ⇒ 最后复核温场与电流场：核算电流通过截面的电流密度，**全体必须 < 11**
```

对照现状（2026-09-08 量的 0.8 档：舌根热点 +99 K、J_max 37）：现在的求解器只做 ③ 的一部分和 ②′②″③ 判据，
**① 的设计电流没有进尺寸链、② 的「按 J=10 定截面」没有、④ 的「全体 < 11」只有一行判不了**。这就是 R18 要做的。

你 09-08 补的口径：**烧断/熔化机制只是辅助验证** —— 按 J=10 设计，烧断不会出现；不再讨论熔断，那不是这个 APP 的重点。
（今天做的下角/探针熔化处置留着当安全网，不再往上加东西。）
我钉死的口径（你未反对）：设计电流取**升温全程的最大值**（空管、温控 20 °C/h），J 的复核在设计电流下做。

**推翻一条前任的口径（按第 8 条，说出来）**：前任把「法兰 J」定义成电流场的**逐点峰值**（凹角处 37），并要追网格到收敛才判 ⇒ 永远判不了。
你的第 ④ 步是「电流通过**截面**的电流密度」= I/A，闭式、与网格无关。我改成：舌片各截面（含开孔处）、舌盘交界弦、孔缘环、各级环，
全部 I_设计/A < 11 为硬判据，当场判得了；场的逐点峰值降为诊断量，只给第 ③ 步（定孔）和辅助验证（温度/熔化）用。

你 09-08 定的三条口径：① 设计电流的升温目标**全线 1150 °C**；② 圆盘绕管孔的截面**按整圈**算（A = 2πr·t，先按这个）；
③ （你随后纠正）第 ② 步「决定法兰大小与尺寸 r1/t1/r2/t2 与舌片横截面积 [按 J=10]」**整个就是「搜形状」**：舌片横截面（宽 × 厚）在那一步一起定，
**没有单独的「舌片宽度」概念**；UI/3DM 给的只是搜形状的起始几何。代码对应：搜形状逐个形状调求解器，J=10 截面下界在每个形状里定厚度，铂重在形状之间比。
另：**每次论证验算跑 2 段（3 片法兰）**，3 段的 3480.7 只当归档基准。

你 09-08 的提醒「对 Opus5 的计算逻辑与流程必须小心验证」：R18 只依赖前任四样东西，每样先独立核过再用 ——
① 升温电流模型 `RampTwoNode`（I²R_管 = C·dT/dt + 散热 + 耦合）：在 1150 °C 空管处手算对数；
② 共用片电流 √(I₁²+I₂²+I₁I₂)：120° 相位差的相量差，我推过，对；
③ 板面几何闭式（`HalfWidth`／面积）：用 W08 手算对；
④ 求解器只增不减框架：截面判据闭式单调，适用。核对结果记入「已验」表。

### 你问「遇烧毁条件 APP 的对策」（2026-09-08，按代码答）

| 烧毁条件 | 判 | 处置 | 缺口 |
|---|---|---|---|
| 法兰熔化 | `OverMelt` ⇒ `Ok=false`「该解不存在」 | UI 路：下角抬板厚到不熔 → 到顶交棒搜形状；3DM 路：加最热那一级 → 加不动交棒增宽 | 3DM 模式搜形状用不了（交棒无处去）；判据表看不到离熔点多远；细网格上没跑过 |
| 管子热失控 | 电流上界钉在 I_stab 内，越界拒算 | 改管壁/功率/保温（输入，不是旋钮） | — |
| 升温期烧断 | 判据 ①（硬安全） | 无旋钮，改升温速率/功率 | ① 只在终局复核算 |
| 电流密度上限 | 管 J 硬安全；法兰 J 一律判不了 | 管 J 无旋钮；法兰 J 判不了不算过 | C1：法兰 J 判不了挡住每一档 |
| 烧穿 | 板厚下界 max(焊接屈曲, 烧穿 0.6) | 起点在下界之上、只增不减 | — |

三个缺口你 09-08 拍板：**都排进计划，放在 R17 前面，紧要度我定** ⇒ 登记为 R18（法兰 J 判不了）、R19（3DM 熔化交棒无处去）、R20（升温 ① 进导航）。

### 顺序（紧要度由我定，2026-09-08）

1. **先收掉在手的回归**（0.8 档 NaN，第五节）—— 交不出东西的事优先。
2. **R18 法兰 J 判不了 → 判得了**：它挡住**每一档**的全绿交付，其它都做完也交不出来 ⇒ 最紧。 ✅ 09-09 收尾：第四趟搜形状 Ø58/舌58 2646 g，细网格复算全过 2648 g；截面修正（4da8835）后单点复算 2614.3 g／细网格 2615.8 g 全过；第五趟搜形状导航网格最轻 Ø56/舌56 2606 g，但细网格 ③ 11.76/10 不过 ⇒ 细网格重解（舌保温 5.2/2.9/8.8）全过 **2603.8 g** ⇒ APP 答案 Ø56/舌56/140 = 2603.8 g（细网格）。
3. **R14/R8 3DM 多级不压平均、走求根**（你说「先做」）。✅ 09-08 晚。
4. **R19 3DM 路要能进搜形状**（你纠正：不是熔化的事）：与 R14 同一条线（3DM 形状交给解析路 ⇒ 搜形状可用），紧接着做。✅ 09-08 晚。
5. **R11 舌片厚度与圆盘各级解耦**（你纠正：舌片厚度不是旋钮，= I/10 ÷ 舌宽；现在与圆盘基板同一根旋钮）。✅ 09-08 晚。
6. **R12 孔心位置由场定** → **R13 形状族**（同一条候选表机制，先位置后形状）。◐ 09-09 由独立工作树的开发 agent（Claude Fable 5.1，本人同型号）做了 Core／Geom／测试那一段，UI 由我接：
   槽心角按场（单舌 0°、双舌 107°＝±90° 一带，实测）已进几何链；舌孔孔心那条规则被 孔位对比.txt 推翻、只印不用；形状族（舌孔 圆/圆角三角/圆角方，圆盘 弯槽/长椭圆·切向/长椭圆·顺电流）等面积探针比价、选中的存进设计并出图。验法与数见 HANDOVER R12/R13 两行、deliverable/形状族_对比.txt。
7. **R15 最小孔径 1 mm**（小，夹在中间做）。✅ 09-09（同上那位）：取值域 {0}∪[1,上界]，四道门 `MinHoleRadiusTests`。
8. **R20 判据 ① 改闭式进导航**（你纠正：J<11 就不会熔，不判峰值；① = 设计电流没被管 J 上限截住）。✅ 09-09 凌晨。
9. **R17 UI 检视与整理**（你说放最后；改前改后逐页截图对照）。✅ 09-09 凌晨（与 R21 一起）：三步页签、分步按钮收起、③ 页判据表+场图；截图见 deliverable/界面截图_改前／改后。

每一件的验收标准我做之前先写进 HANDOVER 开发计划，做完附证据。

---

## 三·五、我依赖的结论：已验 ／ 继承未验（2026-09-08 起，逐条维护）

> 你 09-08 问「现在进行的每一步都要以不一定是对的状态去对待？」—— 是。没验过的一律当不一定对。
> **已验** = 我自己跑出来的（门／真解／轨迹）；**继承未验** = 前任写的、我还没重验；**已推翻** = 验了发现不对（写清被什么推翻）。

| 结论 | 状态 | 证据／出处 |
|---|---|---|
| 0.8 档起点 0.60 mm 全片必熔（入口／HC1\|HC2／HC2\|HC3 熔，出口不熔） | **已验** | `deliverable/对帐超时_轨迹.txt` 留痕；`MeltIsNotASolutionTests.逐片熔化位与整线位一致` |
| 熔化区里 LineRunner 第一次 RunOnce 就返回，各片熔/不熔只由各自板厚决定 | **已验**（代码读 + 实测一致） | `LineRunner.Run` `if (!res.Ok) return res;`；第 1 遍 2.8 分钟 22 次解 |
| W08 下角 = 0.68／1.70／1.50／0.60 mm（导航网格） | **已验** | `deliverable/下角因熔化上抬_轨迹.txt`，v2/v3 两次逐位相同 |
| 耦合展开后抽热↑ ⇒ 电流↑ ⇒ 片更热（第 2 遍片2 又熔） | **已验**（现象）；机理是我的解释，**未单独量** | 同上，第 2 遍 1.45→1.50 |
| 候选探针到上界让邻片熔 ⇒ 第 1 轮收场（bba08c7） | **已验** | 对帐轨迹第 1 轮 |
| 6f191b2 能到 3480.7 是因为当时熔化只记 Note | **继承 + 代码读**，未重跑 6f191b2 | HANDOVER／LineRunner 熔化支的历史注释 |
| 熔化两层修好后 0.8 档回到 3480.7 g（板厚 0.83/2.32/2.16/0.72，三条逐片判据全过） | **已验** | `deliverable/对帐超时_轨迹.txt`（75 分钟、129 次场解） |
| 0.8 档现在唯一不过的是法兰 J 判不了（C1／R18） | **已验**（bba08c7～a982814 口径） | 同上：停因「没有旋钮能治：· 法兰 J_max」；R18 第一阶段后此行由「法兰截面 J」接手 |
| 升温电流模型 `RampTwoNode`（温控 20 K/h）：I²R_管 = C·dT/dt + 散热 + 耦合 | **已验**（部分独立）：升温末端峰值 / 准静态电流 ∈ [0.85, 1.40]，管电阻 R = ρ(1150)·L/A 由测试自算，I²R 落在千瓦级 | `DesignCurrentTests.独立核数_升温末端电流约等于准静态电流` |
| 共用片电流 = √(I₁²+I₂²+I₁I₂)（120° 相位差的相量差） | **已验**（推导）：\|I₁∠0 − I₂∠120°\|² = I₁²+I₂²−2I₁I₂cos120° | `DesignCurrentTests.两段三片_…` 用同一式复算 |
| 板面几何闭式（`HalfWidth`、孔弦、环弧）用于截面 J | **已验**（手算）：宽 60×厚 1、600 A ⇒ J=10；R10 孔 ⇒ 40 mm² ⇒ 15；环 2π·25.8 | `SectionSizingTests` 5 条 |
| 法兰更重 ⇒ 设计电流不降 | **已验** | `DesignCurrentTests.法兰更重_设计电流不降` |
| 管保温加厚 ⇒ 设计电流下降（杠杆 1） | **已验**（修过一次哑的：传错输入） | `DesignCurrentTests.管保温加厚_设计电流下降`；`deliverable/杠杆扫描_盘70舌70.txt` 里 5→80 mm 纹丝不动那行是修前 |
| J=10 截面 + 舌保温 能同时满足 ②′>0 与 ③≤10（Ø70/舌70，2 段） | **已验** | `deliverable/形状_盘70舌70_轨迹.txt`：可行 2997.8 g |
| 舌保温 0.3→20 mm 把抽热从 +118 压到 −20 W（③ 286→5.9 K，②′ 转负） | **已验** | `deliverable/杠杆扫描_盘70舌70.txt` |
| 「Φ=1 与 J 上限互斥／C2 与 ② 通过 J 对立，只剩舌长一个出口」（HANDOVER §4.2w/§4.3n） | **部分推翻**：J=10 下用舌保温就能进窗口（上面两行），舌长不是唯一出口 | 同上 |
| 温控 20 K/h 下不熔（法兰峰值 1320 °C） | **继承未验** | HANDOVER §4.2s（2026-08-11，当时的几何） |
| 管 J 三个数：15 一般／12 管壁 0.6 极限／10 实际使用最大 | **用户给定**（08-25、09-08 A） | HANDOVER 判据表 2614 行；本档「工况」 |
| 板厚下界 = max(焊接屈曲, 烧穿 0.6) | **继承未验**：屈曲支在 Ø120 盘上给 **4.7 mm**（自由边长条板 k=0.43 × 安全系数 2），模型自己注释承认「长条板套到环上是没有论证的替换」。它只在大盘上咬（Ø60–70 时 0.55–0.65 < 0.6 不起作用）。**你 09-09 答**：图纸（Pt_Heater1/3 在 Ø120 上 2.0／3.0 mm）是工程师拍脑袋画的，不构成「没翘」的证据 ⇒ 不凭图纸推翻，**靠理论算**：09-09 起用工作流三路独立推导「内缘环焊到管上的环板受焊缝收缩的屈曲下界」并互相反驳，综合后决定是留、是改 | `DesignSpec.DiscFloorMm`；`Core/WeldDistortion.cs`；走查 Pt_Heater3 交接后核算 7996 g（就是被它顶到 4.74 mm） |
| 图纸各级 → 基板 + r₁·t₁ + r₂·t₂ 的翻译**可逆**（翻出的参数造板、栅格化、反推，级数厚度半径逐位回来） | **已验** | `ShapeToAnalyticTests.三级图_翻过去再翻回来_逐位相同`；真图纸 Pt_Heater3 翻出 3.00／孔+10×0.333／孔+20×0.667 |
| 舌片厚 = I/(10·舌宽) 与圆盘解耦后，舌片各截面 J ≤ 10、圆盘基板下界只由圆盘侧截面定 | **已验**（闭式，用 DesignCurrent 独立复算） | `DesignSpecTongueTests`；`SectionSizingTests.舌片解耦后_基板下界只看圆盘侧截面` |
| 判据 ① = 升温所需电流没被管 J 许用截住（0.8 档 2 段：所需 1214 A ⇒ 管 J 9.5 < 12，没截住；许用压到 2 必截住而所需电流不变） | **已验**（闭式） | `DesignCurrentTests.管J许用压小_升温电流被截住_判据一不过` |
| 圆盘各圈截面厚度：R11 之前取 (−r,0) 在盘上；解耦后那点在舌片上（盘径 = 舌宽时全部圈读成舌片厚）—— **已推翻**，改取 (0,r) | **已验**（手算 2π·25.8×1.0） | `SectionSizingTests.圆盘各圈厚度取的是盘上的点_不是舌片厚` |
| 舌盘交界切口：整条弦 2·hwT·t 没扣管孔弦、也把基板抬到舌片厚 —— **已推翻**，改为 边条 + 焊弧×孔边厚（盘径 = 舌宽：8.4 + π·25.8 = 89.4 mm²/mm；\|xT\| ≥ 孔半径时回原式） | **已验**（手算） | `SectionSizingTests.交界切口_盘径等于舌宽时扣管孔弦并加焊弧` |
| 出图舌片侧（R11 分舌片）盘径 = 舌宽时盖住管腔左半圆、且不跑管腔自检 —— 已修（减管孔 + 自检 + 切不开算失败） | **已验**（真写图读回：管腔空、舌片层 2.5 mm） | `Export3dmMatchesSolvedTests.盘径等于舌宽且舌片另有厚度_舌片侧不许伸进管腔` |
| 截面修正后 Ø58/舌58（2 段、管保温 10）仍可行且更轻：导航网格 2614.3 g、细网格 0.25 mm 收敛全判据过 2615.8 g（原 2646/2648 由错截面算出） | **已验**（30 分钟） | `BestShapeFineMeshTests`（慢）、`deliverable/细网格复算_盘58舌58.txt` |
| 设定 J 从 ① 输入一路到判据限值：J=20 ⇒ 舌片厚 2.03→1.02、板厚下界减半、极限值 21、算例带 J；判据行与求解器不再读常数 10/11 | **已验**（闭式 + 接线走查） | `DesignJTests`、`UiWiring` 16″ |
| 熔化只判不抬：W08 闭式下角 0.60 mm（不套 J 截面）必熔 ⇒ `MeltFloor` 1 次场解就停、讯息「该解不存在」、板厚与舌片厚一位不动、不交棒 | **已验**（3 s） | `BranchMarksAreReachedTests.下角熔化就停_不抬厚度_这条分支走到了`、`MeltFloorTests` |
| 审查工作流（95 个 agent、630 万 tokens）在额度撞顶前确认 14 条；未修的记在 HANDOVER 登记表下方「审查修正与欠账」 | **记录** | HANDOVER |
| 搜形状走查此前**没吃 `--segs`**：三趟搜形状全是 3 段 4 片的数（单点轨迹是 2 段 3 片） | **已验**（代码读 + 第三趟日志印「片3」） | `Walk.SearchShape` 现已套 `SetSegments`；第三趟（3 段）Ø70/舌70 16 轮筛 4061 g 不可行 vs 2 段单点 2997.8 g 可行 —— 两者不可比 |
| 舌保温不花铂（∂铂重/∂舌保温 ≡ 0）、板厚/环倍率对铂重单调递增 | **继承未验** | HANDOVER `--monotone` 2026-08-28 实测表 |
| ③ 在导航网格与细网格上差 2.03 倍（根的位置随网格移动） | **继承未验** | HANDOVER A⑬（2026-08-28） |
| 法兰 J 一律判不了（孔那一带网格未细化，值是下界） | **继承 + 代码读** | HANDOVER C1；`LineRunner` 法兰 J_max 判据 |
| 求解器解与初值无关（两组入参逐位相同） | **继承未验** | HANDOVER `--solve --seedprobe`；`SolverIsSeedFreeTests` |
| 移除优先级（导热贡献 ÷ 电流密度）定**圆盘槽的槽心角**：单舌片 0°（背对舌片）、双舌片对称进电 107°（±90° 一带） | **已验**（09-09 开发 agent 跑的门，我读了数）：Pt_Heater1 构型，`FieldPlacementTests.单舌片_槽心落在背对舌片的零度一带`／`双舌片对称进电_槽心离开零度`；deliverable/形状族_对比.txt 末段 | 算法与 2026-09-05 的 移除优先级.txt 同一份（`RemovalPriorityTests` 逐单元自证相同）；⚠ 双舌那一条顺带修了 `TwoTabs`：此前第二条舌片不在域里 |
| 移除优先级定**舌孔孔心**（自由段内优先级最高处） | **已推翻**（09-09，被 2026-09-05 的 孔位对比.txt 逐点实测推翻）：优先级朝舌根单调涨（Pt_Heater1 最高处 x=−14.5），而实测最优 x=−50、−20 最坏（峰值 J +23.9 %、抽热反升） | deliverable/形状族_对比.txt「舌孔位置」段；处置：求解器只印不用（`Solver.FieldPlacement`），几何仍走自由段中点 |
| 等面积下圆盘挖料：弯椭圆槽 > 长椭圆·切向 > 长椭圆·顺当地电流（抽热 −30.6 / −22.6 / −4.7 %，Pt_Heater1、1045 mm²） | **已验**（09-09，单构型单点，样本量 1） | deliverable/形状族_对比.txt；背侧 \|∇V\| 只有全片最大的 1.8 % ⇒「顺电流」= 顺半径 = 顺热流。哪个形状对由求解器逐案探针比价定，不由这一组数定死 |
| 等面积下舌孔：圆角三角／圆角方 抽热与圆相当（−2.17／−2.44 % vs −2.22 %），峰值 J +8.2／+4.8 %（圆 −0.4 %、顺流椭圆 −7.6 %） | **已验**（09-09，样本量 1，与 2026-09-05 孔形对比.txt 一致） | deliverable/形状族_对比.txt |
| 舌片按 J=10 定厚后，舌孔孔径的 J 上界 = 0（开孔就得整条舌片加厚）⇒ 舌孔旋钮与舌孔形状族在求解器里到不了 | **已验**（闭式）：R_max = ½(w − I/(10·t_舌))，t_舌 = ⌈I/(10w)⌉₀.₀₁ ⇒ R_max ≈ w·0.01/(2t) ≈ 0.15 mm < 1 mm（R15 再砍掉）；要 R_max ≥ 1 mm 须 t_舌 被 0.6 mm 板料下限顶住，即 w > I/6 ≈ 200 mm（W08 电流 1214 A） | `Solver.HoleRadiusUpperMm`；`MinHoleRadiusTests`。要让舌孔活过来，得改 R11 的口径（例如孔处允许加厚而不是整条加厚，或 J 上限从 10 放到 11）—— 那是决定，不是我该拍的 |

**HANDOVER 里其余有用的资讯**：已按出处摘成 [HANDOVER_摘录_2026-09-08.md](HANDOVER_摘录_2026-09-08.md)（7 节 373 条：物理结论 93／判据限值 28／实测基准 28／已作废 75／坑 62／验证流程 34／前后矛盾 28）。
那份里每一条默认「继承未验」；我用到哪条、验到哪条，就搬到上表标状态。

**待核（摘录第 7 节里最要紧的，用到之前必须先弄清）**：

| 矛盾 | 两处说法 | 处置 |
|---|---|---|
| 闭式判据 TCR·ΔT 限值 | ≤2（§0/§1.84/§3）vs ≤4（§4.3e） | 用到 `--leadbound` 前重推一次 |
| 压接温度 | 300 °C 已是最优（§4.3m）vs 450 更好（§1.9）；设计记录钉 450 | 现在夹持温度已改为算出（热导边界），两句都成历史；R11 t0 时顺带核 |
| 0.8 档现役期望值 | 3547（记录）／3482（--solve）／3480.7（对帐）／3257.7（bba08c7 可行 False） | 以本次对帐跑出的数为准，写进第五节 |
| 0.8 档舌保温 | 18.7/1.6/1.4/3.9（08-14 --linefinal）vs 0.4/0.4/0.5/0.3（W08） | 前者是旧方案，不再引用 |
| 升温要求 | 3 h（§4.2 表）vs ≤3 天（用户）；代码 72 h、20 K/h | 以代码 72 h／20 K/h 为准（你 09-08 确认 20 °C/h） |
| 共用片／端片发热倍数 | 2.6×／2.7×／3× 三处 | 只当量级用，不当数用 |

## 四、我的工作方式（本仓既定，不新加）

- 四道门（编译 → 快门 → 界面接线 → 自检）一道不跳；慢门发版前跑。
- 提交讯息三段：**跑了什么／不覆盖／成本**。
- 最高准则：**可以做错，不可以让你以为的和事实不一样。**
- 要求登记状态只有 ✗／◐／✅，没有「不做」；「实测不划算」写进备注，撤不撤由你定。

---

## 五、我现在在做什么

**0.8 档回归**（慢门实测 3480.7 g → NaN，而跑它的门报「已通过」）：起点 = 约束盒下角 0.60 mm 全片**必熔**，
下角漏了「不熔化」这条约束。修法（督导第 16 封）：逐片、只按布尔「熔/不熔」二分抬下角，留痕，厚度到顶仍熔 ⇒ 交棒给增宽。

实测（W08，导航网格）：下角 0.60 → **0.68 / 1.70 / 1.50 / 0.60** mm，3 遍、22 次场解、9.7 分钟
（第一遍按熔化区的布尔二分，耦合展开后片2 又熔，第二遍再抬 1.45 → 1.50，第三遍验过）。

**第一层**（下角）提交在 `bba08c7`：0.8 档不再 NaN，但第 1 轮就收场（候选探到上界让邻片熔）、3257.7 g 可行 False。
**第二层**（熔化 = 随状态变的约束：探针/二分点上邻片熔了先在副本上抬再量，落地时持久化留痕；副本只求不熔、缓存迭代套）：
0.8 档 **75 分钟、129 次场解、3480.7 g**，板厚 0.83/2.32/2.16/0.72，三条逐片判据全过，**与 6f191b2 的 3480.7 对上**。
可行仍 False：唯一剩下的是法兰 J 判不了（C1，= R18）。已提交 `a982814`。

**R18 第一阶段**（你 09-08 下午给的设计因果链，晚上落地）：设计电流（管子准静态 20 °C/h 升温峰值，2 段：段 1473/1473 A ⇒ 片 1473/2552/1473 A）
→ 按 J=10 定截面成下角（W08 舌宽 60：2.46/4.26/2.46 mm）→ 新硬判据「法兰截面 J < 11」当场判得了。
**0.8 档固定形状（2 段）在新链下的答案**：共用片要 4.26 mm ⇒ 焊脚 4.26 ⇒ 盘 R30 盖不住管孔（要 30.06）⇒ 第一次场解前按 ⑥ 停、给处方。
这是对的：形状太小，该走第 ② 步搜形状。`ReconcileTraceTests` 改钉这个行为（零场解、处方数可闭式复算）。
**搜形状第一趟**（`UiWiring --segs 2 --searchshape`，99.6 分钟）：网格里没有可行形状，每点卡在 ③（法兰增量温降 ≤ 10 K）。
量了 Ø70/舌70（`deliverable/形状抽热_盘70舌70.txt`、`杠杆扫描_盘70舌70.txt`）：按 J=10 定完截面后抽热 100–118 W/片、③ 244–286 K；
舌保温 0.3→20 mm 把抽热压到 −20 W（③ 5.9 K，但 ②′ 转负）⇒ 可行窗口存在（抽热 0～4 W）。
**根因是杠杆 1 哑了**：设计电流算的时候传了开箱默认输入，没带设计记录的管保温层 ⇒ 1473 A（该是 1214 A）⇒ 截面撑大 ⇒ 抽热压不回。
修好后同一点单独求解：**可行，2997.8 g**（板厚 1.74/3.01/1.74、舌保温 6.3/3.1/8.9，2 轮 48 次场解 17 分钟）。
你 09-08 拍板「先做 1+3，做完再搜形状」：1 = 修好并加门（`管保温加厚_设计电流下降`）；3 = 圆盘保温 20 mm 本就在模型里、舌保温是旋钮 ⇒ 不用新加。
你说「J 还有空间」：`ApplySectionFloor` 已带 `jDesign` 参数，默认 10，要抬时不用改结构。
**搜形状第二趟**（修好杠杆 1 之后，`97cf201`）：走查 180 分钟预算跑满被掐，且走查超时不印输出框 ⇒ 一行结果没留下（我的仪器又没做好）。
改了走查：边跑边把输出框新增的行打进日志、超时也印已算完的、`--budget` 给分钟数。**第三趟**以 8 小时预算在跑。
观察：新链下一个可行形状点要跑满 16 轮筛，Ø70/舌70 单点 2 轮就 17 分钟 ⇒ 一趟搜形状几小时起跳，
「几十分钟」那句已过期；搜形状的成本（粗筛网格、候选数）是 R17/R21 之前要处理的事。
**第三趟实为 3 段**（走查没吃 `--segs`，已修）：Ø60/舌40 ⑥ 停 → Ø70/舌70 16 轮筛 65.8 分钟 4061 g **不可行**（片3 ③ 候选穷尽）→ 二分 Ø63.2…（跑着）。

**R14/R8 + R19 + R11**（09-08 晚，接着做的三件，都已落 APP、门全绿）：
- 3DM 多级逐级翻成 基板 + r₁·t₁ + r₂·t₂（`ShapeToAnalytic`，闭环可逆门）；「图纸几何 → 参数」把四个数写进控件，被控件夹住会列出来；
  控件范围放宽到装得下图纸（r₁ 0.5–30、r₂ 1–40、倍率 0.20 起）。走查 Pt_Heater3（2 段）：分析 → 照图纸核算 5721 g（③ 341 K）→ 交接（3.00／孔+10×0.333／孔+20×0.667 逐位进控件，盘径舌宽解禁）→ 解析核算 7996 g（被焊接屈曲下界顶到 4.74 mm，见已验表那条待你答的矛盾）→ 自动定厚…
- Flow：3DM 路照图纸解过一次就指交接（不管过没过），流水线自己做分析与交接，不再停在「要人决定」。
- 舌片厚 = I/(10·舌宽) 闭式（`SizeTongues`），求解器每轮先定舌片、基板下界只看圆盘侧截面；六环全接（Fit/Clone/存档/界面只读框/出图 tabT+Geom 切点分厚/说明书）。
  后果：舌板开孔的 J 上界变 0（开孔就得整条舌片加厚）⇒ 挖料在圆盘背侧；W08 固定形状不再被舌片截面抬到 4.26 mm。
- **对帐重跑（R11 下，W08 固定形状，2 段 3 片，导航网格）：可行 2911.4 g**，舌片 2.03/3.51/2.03 mm、2 轮 48 次场解 17.4 分钟，
  停因「第 2 轮全过；只往上走过 ⇒ 最小可行点」（`deliverable/对帐超时_轨迹.txt`）。`ReconcileTraceTests` 改钉这个行为。
  09-09 审查修正（交界切口扣管孔弦加焊弧、各圈厚度取盘上的点）之后重跑：**2859.5 g**，基板下角 0.73/1.26/0.73 mm 由圆盘各圈（r=26.6，J 12.1/21.0）定 —— R11 的解耦这才在数字上成立。
  ⚠ 与 3 段的 3480.7 不可比（段数不同）；这个数是导航网格上的，细网格复算没跑。
- 载入设计记录再核算要**复现记录**：页面代表一份完整设计（载入／解完）时舌片厚原样带着，改任何参数才回到规则（`_tongueFixed`）。

**R20 + R17/R21**（09-09 凌晨）：
- ① 改闭式：升温所需电流折成管 J 峰值 ≤ 许用（没被截住 = 升得到）；集总升温用时降为参考行。
- 主视图三步「① 输入 → ② 法兰优化 → ③ 结果与出图」+ 参考工具 + 使用说明；② 页只剩一颗「核算整线」，分步按钮收进「手动分步 ▾」；
  改前／改后截图在 deliverable/界面截图_改前_2026-09-08、界面截图_改后_2026-09-09（逐页 PNG，索引.txt）。
- 搜形状第四趟（2 段、管保温 10、R11 之后）跑完（6 小时预算到点）：最轻可行 **Ø58/舌58/舌长140 = 2646 g**，邻域外推无更好方向；细网格复算 0.125 mm **全判据过 2648 g**（deliverable/搜形状_第四趟_2段.txt、细网格复算_盘58舌58.txt）。
- R12/R13/R15 子代理做完、我接界面后合并提交 27ee603；搜形状并行化子代理在做；对 97cf201..27ee603 的多视角审查工作流在跑。
- 09-09 午：审查修正提交 4da8835（钩子 735/735）。W08 固定形状 2 段 2911.4 → **2859.5 g**（基板 0.73/1.26/0.73 由圆盘各圈定、舌片 2.03/3.51/2.03）。Ø58/舌58 也是盘径 = 舌宽，第四趟的 2646/2648 作废：修正后导航网格 2614.3 g、细网格 0.25 mm 收敛全过 **2615.8 g**（③ 9.14/10、法兰截面 J 9.99/11、⑥ 余 2.18 mm）。第五趟搜形状（deliverable/搜形状_第五趟_2段.txt）：导航网格最轻 **Ø56/舌56/140 = 2606 g**，Ø57 2610、Ø58 2615、Ø59 2617；舌宽单独收窄可行但更重（另一种解，R24 下拉里供选）；预算到点赢家精算没跑；Ø56 单点细网格复算 **③ 11.76/10 不过**（其余全过）⇒ 细网格重解 80 分钟：舌保温 5.2/2.9/8.8，③ 9.06/10，**全过 2603.8 g** ⇒ APP 答案 Ø56/舌56/140 = 2603.8 g；流水线缺这一步（R26 已派）。求解器返回的设计现在带解出来的重量（原带记录的 3547 g，Describe 印错）。
- 09-09 下午 **R22**：你答「J 让工程师设定（实况风险工程师承担）」「J 是设定值，J+1 是计算极限值」「J 预设值为 10」⇒ ① 输入新增「设计电流密度 J」，舌片厚／截面下界／孔槽上界／判据「法兰截面 J」限值（= J+1）全从它来；熔化改判断工具：`MeltFloor` 熔了就停「该解不存在」，不抬厚度（死支拆掉，「够不到舌片」欠账销）。快门 740 + 慢门 20 = 760；接线走查全过。
- 你 09-09：焊接屈曲下界靠搜形状避开，它是判断工具不是目标 ⇒ 推导先放下（工作流已停），回到主目标。
