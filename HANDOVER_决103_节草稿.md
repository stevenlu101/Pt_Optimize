## 0.-2x ★★★★★ 决 103 判据换向：稳态期「法兰最热处高出管接触处温度 ≤ 10 K」「管接触处流入法兰的净热流 ≤ 0」为硬判据、两条热稳定升硬（局部热稳定全格精算）、管 J 11；热偶读数基准两条与管孔净流入降参考；改回参数 `CriteriaRuleSet` —— 2026-09-24，云端 Linux 会话（wtCRIT，基于 cc49836）

> 依据：本文件 §0.-21 决 103 条（业主 2026-09-24「按今天的为准，改完方案再开跑」）；全局方案第 2 版 `deliverable/R48_全局解决方案_出解路线_2026-09-24.md` 1、3、4（L2）、7 节；决 99 报告（选项 A）。完整记录（机理、改动逐档、门表含覆盖／不覆盖、归因、分派核查、判据表应改成的行、红与重录、界面待办、待决定、成本）：`deliverable/R48_决103_判据换向_实施记录_2026-09-24.md`。
> 阈值只按决 103 改（新热侧 10 K、冷侧 0 W、管 J 11、两条热稳定 1.0 由参考升硬），其余一个没挪；新常数出处在 `Pt_Optimize/Core/CriteriaRules.cs`。全部 Linux 预跑、待 Windows 重录；耗时争用下量得（load 10～22）。

### 甲　起因

业主 2026-09-24 更正：稳态期（管内有玻璃）法兰温度场比管接触处**高**的方向是对的、可到 10 °C 以内；**拉低**管温的方向是错的；热偶读数基准 ± 5 作废；管 J < 11；最热铂高出热偶读数、管根低于热偶读数、管孔净流入须为正（下称三条旧判据）降参考。允许法兰偏热后「管孔净流入永远先红」的隐性保护消失 ⇒ 两条热稳定自己卡（HO:9075、9260），局部热稳定的报出值按决 99 修成全格。

### 乙　改了什么

- **新两条（带玻璃稳态硬判据，全名不带代号）**：第 j 片 `TMaxC − TRootC ≤ 10 K`（「法兰最热处高出管接触处温度」）；`QFromTubeW ≤ 0 W`（「管接触处流入法兰的净热流」，正 = 管 → 法兰；与「管孔净流入」同一个量、合格方向相反，不给预算）。接触处温度 = 模型算的该片管根接触温度（与旧判法「圆盘区最高温 − 管温」同一基准）：端片取相邻段端温，**共用片取两侧段端温度的较高者**（RunOnce 原式；对新热侧偏乐观，写明）；热偶设定值只印对照。空管到温稳态两条只作参考（全局方案 1.3 节）。构造 `LineRunner.ContactChecks`（公开，快门用合成片验）。
- **两条热稳定**带玻璃稳态升硬（≥ 1.0，空管态参考）；**局部热稳定全格精算**：热解留闭包，整线收尾 `ApplyLocalStabFullGrid` 对终局四片逐格精算（同一个 `LocalStability.Check`），片上落点换成全格 ⇒ C4′ 细区热点核对读到真落点（r ≈ 63～66）。整片热稳定逐片裕度带到片上（求解器用）。
- **管 J**：卡交付限值 = min(「管许用电流密度」12, 新参数「管 J 使用上限」11)，唯一读法 `DesignInputs.TubeJLimitAPerMm2`；① 升温、升温全程逐点、升温电流上限读同一个数；原 12 那行照印（`· 管电流密度对原许用值（对照）`）。法兰截面 J 不动。
- 三条旧判据降参考（照算照印，说明前加降级句）；三条旧判法参考行不动。
- **改回参数** `DesignInputs.CriteriaRuleSet`（`决103` 缺省／`决103前`），生产不传；分工况表 `RequiredByState`（生产）与 `RequiredByStatePre103`（改回）、`RequiredFor／StateKindOf／ApplyStateCriteria` 带口径，`LineResult.RuleSet`（RunOnce 写入；手造结果缺省改回，同 EmptyTube 约定）。
- **求解器分派**：现有分派 = 「判据键 → 候选表 + 当场实测挑选（ChooseKnob 比价、RaiseUntil 前提自检）」，键写死在三条旧判据上 ⇒ 加 `Solver.Allocation103`（冷侧 ← 舌保温／圆盘槽／舌孔；热侧 ← t₂／r₂／t₁／r₁／板厚；两条热稳定 ← 板厚），`AllocationFor(rs)`，改回 = 原表；逐片裕度、认证误差、回收保护名单、日志一并换。只抬不降的求解器用不到 3 节表的「舌保温↓」「板厚↓」「夹持温度↑」（写明，列待办）。
- **升温全程**：管 J 限 11；加两条热稳定 ≥ 1（原来不卡；改回不卡）；不卡温差、伸长表照印。终验三关判定原样。
- **拒答**：加密复算比对列仍是改前三条 ⇒ 决 103 带玻璃稳态拒答；保温搜索逐格点三项是改前口径 ⇒ 决 103 拒答。
- 界面图例（`Criteria.All`）与说明书按生产口径改；安装报告加逐片新判据表；作废档声明名单加新四键；转储测试把全格精算用时两个成员当挂钟量。

### 丙　跑了什么（Linux 镜像，重建三步 0 错、Compile Remove 29）

| 门 | 覆盖 | 不覆盖 | 结果 |
|---|---|---|---|
| 门 a 改回逐位（慢） | 决103前 下 W08 现役（盘 30）与 R = w = 45 导航整线一次解：判据表全行（R 格式、说明全文）、逐片、逐段、说明、没过名单对改前树 cc49836 逐行 | 判决网格、求根、升温全程逐点、图纸路径 | **过**：0 行不同（首跑差一行 = 说明里的挂钟「量雅可比用时」，加去挂钟后重跑） |
| 接线门 1 中性（探针） | 改回口径全文转储去掉 24 行新成员行 ⇒ SHA 与改前树实测 | — | **逐位相同** fd00d6d7…；另查出入库记录 07f96390… 在改前树上已不等（接线门 1 在 cc49836 上已红，非本改动） |
| 门 b 新判据只印 + 归因（慢） | 两例 × 两口径判词并列 | 判决网格、求根 | 过（只核级、限值与全格做没做） |
| 门 c 局部热稳定全格（慢） | 决 99 案例 W08 判决 h 1、细区 59：报出值 = 逐片重建全格最小（逐位） | 导航、加密阶梯、放大 | **过**：入口 1.8816 @ r 66.412（= 决 99 记录）；前 12 名乐观 4.38～4.91 倍；14736 格约 12 s（整线 6.6 %） |
| 快门 8 条（`R48CriteriaSwapGateTests`） | 管 J 11、新两条构造、分工况表两口径、求解器分派、升温全程热稳定、全格精算合成（含 C4′ 热点接线）、拒答、源码门（Core 引用旧三条的 13 档名单） | 界面与命令行 | 8／8 过（终跑 11 条连慢门 11／11 过，9 min，`…_194230` 四份证据） |
| 点名快门 | RequirementRegister、NMesh 快、F4ZoneShare 快、UInsulWindow、SolverIsSeedFree、HeatBalance、EmptyTube、PropsWiring、CriteriaGlossary | 慢门 | 过，除 CriteriaTableTests 14 条（§1.83 表没改，预期）与接线门 1（改前已红 + 本改动变因） |
| `R48MStopTolTwoPointTests`（整类慢门） | 两点导航各 ≤ 120 轮、认证误差 ≤ 0.05、注射 ⇒ 13.074 超 300 轮 | 判决网格 | **红，改前就红**（HO:592、HO:773 同一红）：生产支 27／30 轮过；注射支 37 轮收敛。诊断跑把注射改回判据口径，37 轮逐项同 ⇒ 与判据口径无关；测试档没改 |

### 丁　开 − 关归因（关 = 决103前，开 = 决103；导航网格）

| | W08 现役 关 | W08 现役 开 | R = w = 45 关 | R = w = 45 开 |
|---|---|---|---|---|
| 法兰最热处高出管接触处温度 ≤ 10 | — | 6.581 过 | — | 17.665 不过 |
| 管接触处流入法兰的净热流 ≤ 0 | — | −0.364 过 | — | +25.333 不过 |
| 局部热稳定 ≥ 1 | 6.692 参考（前 12 名 r 33.1） | 1.918 过（全格 r 63.5） | 2.675 参考 | 1.971 过（r 63.0） |
| 整片热稳定 ≥ 1 | 14.433 参考 | 14.433 过 | 10.080 参考 | 10.080 过 |
| 最热铂高出热偶读数 ≤ 5 | 7.755 不过 | 参考 | −12.254 过 | 参考 |
| 管根低于热偶读数 ≤ 5 | 1.085 过 | 参考 | 63.815 不过 | 参考 |
| 管孔净流入 > 0 | −1.087 不过 | 参考 | 11.870 过 | 参考 |
| 管 J（12 → 11） | 9.505 过 | 9.505 过 | 9.514 过 | 9.514 过 |
| 交付 AllOk | 不全过 | **全过** | 不全过 | 不全过 |

场量逐位不变（外层耦合停机容差两口径都是绝对目标 0.025 K）。W08 冷侧最紧一片只离 0 有 0.36 W。

### 戊　重录与待办

- **§1.83 判据表**要照实施记录 6 节改 10 行、插 3 行（`CriteriaTableTests` 现 14 条红）；改前先看对照行名字不含「管 J」的理由。
- 全量快套件（测试未改前）1131 条：过 1080、红 51，逐条归类见实施记录 7 节：本改动引起的都已按改回口径或新语义改测试（改后重跑受影响类全过，红的只剩改前就红的 `SyncProgressOnlyTests`、`R48F7AdaptiveRadiusTests.门f` 与接线门 1）；改前就红的 11 条（改前树 cc49836 加一个转储档实跑同红）不动；接线门 1 改前也红、没重录（入库记录在改前树上就不等，变因未查到；决 103 后本树 Linux 实数 aced3039…）。
- 求解器走通探针（W08 导航遍，70 min 到闸取消）：下角冷侧四片 +50～+73 W 红 ⇒ 实测淘汰槽／舌孔 ⇒ 抬舌保温，片 0 → 5.5 mm、片 1 → 2.5 mm；每片每轮约 30 min（争用下）。热侧与热稳定红时的分派没走到。
- 界面（Windows）：`ManualPage.cs` 管 J 限值改读 `TubeJLimitAPerMm2`；判据表页核图。`ShapeSearchDriver`（wtSHAPE2）报告「三条硬判据」换成新两条与两条热稳定。
- 加密复算换比对列（热侧容差可沿用限值 10 %、冷侧 0.5 W；两条热稳定容差没有出处）与保温搜索接新判据；做之前两件都拒答。

### 己　待决定

- 【待决定】加密复算里两条热稳定：(A) 不进比对列、只印（零代价，收敛复核不覆盖热稳定）；(B) 定选定容差进列（要看过数才定、写「选定」）。拒答期间全局方案 L4「三档收敛复核」做不了。
- 【待决定】共用片接触处温度：现取两侧段端较高者（与旧判法「圆盘区最高温 − 管温」同一基准，对新热侧偏乐观）；(A) 保持；(B) 取较低者（保守，热侧数变大）；(C) 两侧分判。改一行、改回参数照办。
- 【待决定】空管到温稳态两条热稳定：现按全局方案 1.3 节只作参考；若要卡，改表一格。
- 【已按选项 A 做，待追认】决 99 选项 A「修成全格精算」：只在整线收尾对终局那一份场算（不是每次热解都算），代价判决网格 +6.6 %；回退代价 = 改回参数一个（`CriteriaRuleSet = 决103前` 即回到前 12 名口径）。

### 庚　成本

人时约 7 h；机时（争用下）门 a／b 两跑各约 10 min、门 c 3.2 min、点名快门 15.8 min、全量快套件 31.4 min、两点门两跑约 40 min、求解器走通探针 70 min（到闸）。代码 Core 约 +700／−80 行，测试新档 2 个（约 730 行）＋ 旧档改 10 余处（钉子与改回口径）。「应为」数由编排者合并时改（新测试 11 条：快 8、慢 3）。
