# ---- .NET 构建产物 ----
[Bb]in/
[Oo]bj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/

# ---- NuGet ----
*.nupkg
packages/
!packages/build/

# ---- Office 临时锁文件 ----
~$*
*.tmp

# ---- 本地测试结果 ----
TestResults/
*.trx
*.coverage

# ---- 说明 ----
# docs/fig/*.png 与 docs/*.docx 刻意纳入版本管理：
# 论文中的全部图表均由程序生成，一并提交以便追溯「哪一版结论配哪一版图」。
# 源数据（*.3dm、*.xlsx）同样纳入，它们是模型的唯一事实来源。

# 场图/网格图输出目录（可由 --mesh / --shell / --run 重新生成）
figs/

# Rhino 打开文件时的锁文件
*.rhl

# 编辑器/系统临时文件
新建 文字文件.txt

# 会话里临时的构建输出（-o 到仓内是为了让测试的「往上找仓根」还能找到）
.tmpb/
.tmpcg/
.tmprun/
.tmprun2/
.tmpverify/

# ★ Rhino 自动备份：按定义是**上一次存档的旧副本**，不是事实来源（2026-09-07 督导 ③）。
#   本行上面写着「源数据同样纳入，它们是模型的唯一事实来源」—— .3dmbak 恰恰不是。
#   入库之后同一份几何在仓库里有两份、其中一份是陈的，正是本项目栽得最多的那种。
*.3dmbak

# ★ Python 字节码：证据线的 .py 与 .md 是事实来源，.pyc 是它们的派生物。
#   2026-09-18，Opus 5：合并复核查出 deliverable/A_解析交叉验证/scripts/__pycache__/ 下
#   9 个 .pyc 被暂存进来。字节码随解释器版本变、看不出改了什么、也无法据以复算，
#   入库只会让「这一跑用的是哪份脚本」多一个陈的答案 —— 正是本项目最怕的那种。
__pycache__/
*.pyc
*.pyo

# ★ 合并期的跑前备份与 md5 快照（deliverable/_bak_merge/）：M11 那条开放事项的做法是
#   「快套件跑前备份、跑后按 md5 还原」，备份本身是**过程物**，不是交付物，也不是证据
#   （证据是还原后逐份核对相同这件事，写在 HANDOVER 合并节里）。
#   2026-09-18，Opus 5：`git add -A` 会把它连同一个 .tar 一起吸进 stream.patch —— 09-18 12:58
#   那一份 stream.patch 里没有它，本次重建时当场撞到。造门在下游，省得下次又悄悄进来。
deliverable/_bak_merge/
logs/
