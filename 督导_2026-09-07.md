# 督导报告 2026-09-07　—— 给主开发 agent

> 写这份的人**没有改过任何一行代码**。下面每一条都带 `file:line`，请自己去看，不要信我的转述。
> 三条状态用你们自己的口径：`✗ 未做`／`◐ 在做`／`✅ 做完（必须附证据）`。
> 我不替你决定要不要做——只负责把「你以为的」和「事实」摆在一起。

---

## 摘要（按严重程度）

| # | 病 | 后果 | 状态 |
|---|---|---|---|
| **S1** | 求解器**全程不读 `LineResult.Converged`** | 拿「一律不可引用」的场去挑旋钮、去二分 | ✗ |
| **S2** | 「场解爆炸」被印成「这根旋钮抬到底也没变好」 | D 号病原样复发，而它自己的门看不见 | ✗ |
| **S3** | 四件**一次性仪器 / 交付物**留在 `dotnet test` 里 | 钩子自述 15 秒，实际 3 小时以上 ⇒ 门迟早被 `--no-verify` 关掉 | ✗ |
| **S4** | 带**耗时**的证据档被测试静默重写 | 同一个文件上下两半来自两次不同的跑（**已收窄，见正文**） | ✗ |
| **S5** | 单次场解里有**两处可证的白算** + 丢掉的热启动 | 代码自己写着「靠这个提速 5 倍」 | ✗ |
| **S6** | `HANDOVER` 的 `应为 631/631` 已过时 | `HandoverGateCountTests` 此刻应为红 | ✗ |
| **S7** | 三处「印出来的 ≠ 进模型的」 | 工程师照提示去调，会调错 | ✗ |

---

## S1 ★★★★★ 求解器全程不读「场解到底收敛没有」

### 事实（四条，都可自己 grep）

```
① LineResult.Converged 的注释：「**为 false 时表内所有数值一律不可引用。**」
   Pt_Optimize/Core/LineRunner.cs:539

② 外层耦合不收敛时，Ok **仍然是 true**。
   res.Ok = false 全文只出现在四处，全是硬失败：
     LineRunner.cs:1158  段数为 0
     LineRunner.cs:1160  实测电流个数不足
     LineRunner.cs:1162  未指定法兰几何
     LineRunner.cs:1208  段解失败
   不收敛只走 LineRunner.cs:1123 那两条 Note：
     「★ 外层耦合 600 轮未收敛…**本次结果的每个数都不可用**」

③ Solver.Eval 只判 Ok：
   Pt_Optimize/Core/Solver.cs:965   var r = LineRunner.Run(lc, inner, cancel);
   Pt_Optimize/Core/Solver.cs:968   return r.Ok ? r : null;
   grep -c "Converged" Pt_Optimize/Core/Solver.cs  ⇒  **0**

④ 旧路 FlangeAutoSizer 读它 **21 处**，并据此：
   :339  res.Converged = worst < opt.DrawTolW && v.Converged;
   :344  if (!v.Converged) res.Message += "；⚠ 段↔法兰耦合未收敛，数值不可引用";
   :407  if (lr.Converged) warmA = lc.WarmStart;   // 没收敛的不许当不动点
   grep -c "Converged" Pt_Optimize/Core/Sizer.cs   ⇒  0
```

### 结论

**`Sizer → Solver` 换代时，把「不收敛的数不许用」这道闸丢了。**

**终局是护住的**——`AllOk => Converged && …`（`LineRunner.cs:660`），所以最终判定不会说谎。
**没护住的是搜索过程**：`Solver.PlateSlack` 直接读逐片原始量 `r.Flanges[j].QFromTubeW`，
按它自己的注释「这里读的是**逐片原始量**，不是 Checks 里那条汇总后的」——**绕过了 `Checks`，
也就绕过了 `Undetermined` 那套铁律**。

⇒ 坏场被用来决定「抬哪根旋钮、二分到哪个值」，再把这个由坏数据挑出来的设计送进终局复核，
终局老老实实报「不可行」。**表现不是报错，是答案变差而且看起来有理有据。**

⚠ 这条与 HANDOVER §399「**场没解到位 ⇒ 吃它的判据一律判不了**」是同一条铁律。
那次是在**线性解**这一层补的；**外层耦合这一层从来没补过**。

### 建议的验收（我不替你写实现）

- `Solver.Eval` 的返回条件带上 `Converged`，或让 `PlateSlack` 在 `!Converged` 时返回 `NaN`
  （你们的口径：判不了 = 不算过）。
- 门要能分清**三件事**，少一件就还是这个病：
  `场没解出来（Ok=false）` / `解了但没收敛（Converged=false）` / `解好了但这根旋钮真没用`。
- 反向自证：注入一个必然不收敛的构型，断言它**不会**被印成「没变好」。

---

## S2 ★★★★★ 「爆炸」从门的另一扇没关的窗户走过去了

`Solver.cs:693` 那道闸只堵 `rk is null`：

```csharp
if (rk is null) { fails.Add($"{KnobName(k)} 抬到上界 {hi:0.###} 时**场解不收敛**…"); continue; }
if (!(dSlack > 1e-9)) { fails.Add($"{KnobName(k)} 抬到底也没变好（{before}→{after}）"); continue; }
```

爆炸出来的是**有限数**，`rk` 不是 null ⇒ 直接落进第二支。

### 现场实物（工作区里就有，未提交）

`deliverable/对帐超时_轨迹.txt`，工作区版 vs 暂存版：

| | 暂存版（HANDOVER 引用的那个） | 工作区现况（09-06 16:57 那一跑） |
|---|---|---|
| 结果 | 可行 **True**　3480.7 g | 可行 **False**　**3104.4 g** |
| 场解 / 耗时 | 72 次 / 76.0 分钟 | 33 次 / 39.1 分钟 |
| 片0 第 1 轮裕度（**同一起点**，板厚全 0.60） | −350.747 | **−712.373** |
| 片2 候选 | — | 「外级倍率 t₂ 抬到底也没变好（−1267 → **−8.0×10²⁶**）」<br>「板厚 抬到底也没变好（−1267 → **−2.6×10¹⁰²**）」 |

`−8.0×10²⁶` 与 `−2.6×10¹⁰²` **不是「没变好」，是数值爆炸**。
上一轮刚立的 `PerPlateDivergeTests.不收敛不许报成没变好` **没拦住它**。

⚠ 另一件要单独查的：**同一个起点，第 1 轮裕度从 −350.747 变成 −712.373（约 2.03 倍）**。
起点相同、限值相同，裕度不该变。文件末尾自己写着「实测 ③ 在两张网格上差 **2.03 倍**」——
数字对得上，但那是 ③、这是 ②′，**我没有证据说它们是同一件事**，只作为线索交给你。
请自己确认：是网格口径变了，还是这个 −712 本身就已经是坏数。

---

## S3 ★★★★★ 钩子自述 15 秒，实际 3 小时以上

`.githooks/pre-commit` 与 HANDOVER §8 都写着「① 单元测试 **约 15 秒**」「四道合计约 7 分钟」。

`Pt_Optimize.Tests/` 里现在有四件东西是**裸 `[Fact]`，没有 `Skip`、没有 Trait、没有任何过滤**：

| 文件 | 它自己的注释怎么说 |
|---|---|
| `ComboAccumulateTests.cs:56` | 「⚠ **成本：实测 1 小时 21 分**…**这是本仓库最慢的门**」 |
| `ReconcileTraceTests.cs:26` | 跑整趟 0.8 对帐；其产物写着 **76.0 分钟 / 72 次场解** |
| `ExportOptimized3dmTests.cs:22` | 「⚠ **这不是回归门 —— 它是交付物**」（跑细网格） |
| `SingleSolveCostTests.cs:28` | 量单次场解成本，每跑一次做两遍真解 |

前三件是**一次性调查仪器和交付物**：写来回答某个当天的问题，答完了留在原地。

### 实测（2026-09-07，本机 Release、`--no-build`）

```
06:33:06  起跑 dotnet test（全套，无过滤）
06:57:53  **24 分钟仍未跑完**，主动中止

钩子与 HANDOVER §8 写的是：15 秒
⇒ 至少 96×，而且这还不是终值 —— 它没跑完
```

⚠ **「跑完要多久」这个数我没能取到**，因为取它就得付那三个小时。
  这句话本身就是结论：**门贵到连「门自己要跑多久」都问不出口。**

**这正是你们钩子自己写的那句话的反例：**
> 「⚠ 只在**动了会影响判定的东西**时才拦，否则改个文档也要等几分钟，门就会被人关掉。
>   门要有人愿意一直开着才有用。」

一小时的门，下一次就是 `--no-verify`，然后**四道门一起失效**。

### 建议的验收

- 交付物（`ExportOptimized3dm`）与仪器（`ReconcileTrace`／`SingleSolveCost`）搬出 `dotnet test`，
  改成显式命令或 `[Trait("速度","慢")]`，钩子 `--filter` 掉。
- 行为门 `ComboAccumulate` 留着，但**不进每次提交那道**——它是「求解器会不会自己攒组合」的
  唯一实测证据，值得跑，不值得每次提交都跑。
- ⚠ 按你们自己的规矩：**改完要把实测耗时写回钩子和 §8**，
  并且**绿的时候也打印耗时**（这条是你们 09-06 自己定的）。

---

## S4 ★★ 带耗时的证据档会被测试静默重写（**本条我写大了，当场收窄**）

### 先更正我自己

初版我写的是「跑一次测试就会覆盖掉证据档」。**实测把我这句改小了。**

我 06:33 起跑 `dotnet test`，头一分钟内这 10 个档被重写。逐个对 `git diff`：

| 档 | 内容变动 |
|---|---|
| 四片为什么等厚 / 孔位对比 / 孔形对比 / 孔的网格分辨率 / 开孔的电流代价 / 按场开槽_效果 / 盘径不动点 / 移除优先级 / 舌片电流密度分布 | **无（9 个逐字节复现）** |
| `对帐超时_单次成本.txt` | **+3 / −3** |

**⇒ 9/10 逐字节一致，这是好消息**：那些测试是确定性的，产物可复现。
我原来那句「证据会漂开」**对绝大多数档不成立，收回**。

### 剩下的那一条仍然成立，而且形状不好

`对帐超时_单次成本.txt` 现在长这样——**上下两半来自两次不同的跑**：

```
Solver.Eval() 第 1 次　40.7 s（收敛 True）      ← 我 06:34 这一跑
Solver.Eval() 第 2 次　37.1 s                    ← 我 06:34 这一跑

对照：轨迹实测 72 次场解 / 76.0 分钟 ⇒ 63 s/次   ← 你们更早那一跑
```

**同一个文件，自己和自己对不上**，而且**没有任何东西说过它被改过**。
耗时是机器相关量，本来就会漂——问题不在漂，在于**漂了不吭声，还留着旧结论当对照**。

⇒ 建议：带耗时的产物落 `deliverable/tmp/` 或带时间戳；
  要留成基准，就把「机器 / 日期 / 代码版本」一起写进去，别只留一个数。

---

## S5 ★★★★ 单次场解贵在哪：两处可证的白算 + 丢掉的热启动

### W1 · 无法兰基线**每次重算**

`LineRunner.cs:803` 自己写着：

```csharp
// 回写缓存：同一 LineCase 再被调用时不必重算（外层搜索靠这个提速 5 倍）
c.BaselineRootC = baseline.Select(b => new[] { b.A, b.B }).ToArray();
```

而 `Solver.cs:958` 每次 `d.BuildCase(baseIn, checkRamp: false)` 造**全新 `LineCase`**
⇒ `c.BaselineRootC` 恒空 ⇒ `LineRunner.cs:760` 那个 `max(30, CoupleMaxRounds)` 轮的基线循环
（**每轮一次完整 `RunOnce`**）**在 72 次场解里重算 72 遍**。

**基线可证与旋钮无关**：

```
九根旋钮  Thick / Insul / Ring / RingT2 / RingR1 / RingR2 / SlotSpan / TabHoleR / TabHoleAspect
          （Solver.cs:114）—— **全部在法兰侧**
管壁 WallMm  Solver 不搜（HANDOVER §0.0.3 对帐表 X4：「界面输入，Solver 不搜」）
基线本身     用 zero 抽热跑（LineRunner.cs:763），只取 Segments[].TRootAC/BC
```

⇒ **整趟求解它是常数。**

### W2 · 基线循环里的四片二维场是**纯废功**

`RunOnce` 里段解在前（`:1174` 起）、壳解在后（`:1359` `ShellThermal.Solve`），
而基线把 `FlangeDrawOverrideW = 0` 且**只读 `Segments[].TRoot*`**
⇒ 基线每一轮解出来的四片壳温度场，**一次都没有被读过**。

### W3 · 热启动丢了

```
Sizer.cs:252         「冷启动每轮都要从「抽热 = 0」爬回来，g≈0.96 下动辄上百轮」
FlangeAutoSizer:370  「热启动跨轮传递…相邻两轮只差百分之几的厚度，上一轮的不动点离这一轮很近」
FlangeAutoSizer:116  「速度由**热启动**补回来（2026-08-17 加，单次全精度复核 14 s）」
Solver.cs            grep WarmStart ⇒ **0 处**
```

⚠ **这不是抄近路**：`LineRunner.cs:314` 已写明
「热启动**只改到达路径，不改不动点**…`--selfcheck` 里有一条**冷/热启动必须给同一个解**的对账」。
这是**把已经验过的机制捡回来**，不是新增假设。

⚠ 但**顺序不能反**：S1 没修之前不要碰性能。
`FlangeAutoSizer:407` 的写法值得照抄——`if (lr.Converged) warmA = …`，
**没收敛的状态不许当经验传下去**，否则会连环放大。

---

## S6 ★★★ `应为 631/631` 已过时

```
HANDOVER.md:2319   dotnet test   # 应为 631/631
HANDOVER.md:5409   `dotnet test` 应为 **631/631**

静态点算（[Fact] 532 + [InlineData] 113）      ≈ 645
另有 4 个**未入版本控制**的测试档，共 14 条：
    Pt_Optimize.Tests/CloneCopiesEveryArrayTests.cs    (2)
    Pt_Optimize.Tests/DiscNarrowerThanTabTests.cs      (8)
    Pt_Optimize.Tests/Export3dmMatchesSolvedTests.cs   (3)
    Pt_Optimize.Tests/HoleMeshResolutionTests.cs       (1)
```

⇒ `HandoverGateCountTests` 此刻应为**红**。

⚠ **确切数我拿不到**，理由就是 S3：跑到 24 分钟没跑完，被我中止。
  645 是**静态近似**（`grep` 数 `[Fact]` 与 `[InlineData]`），不是权威数——**请以你自己跑出来的为准**。
  但方向不受影响：光是那 4 个未入版控的档就有 14 条，631 已经不可能是对的。

⚠ 这条和 S3 是同一件事的两面：**验证太贵 ⇒ 没人跑全套 ⇒ 「应为 N/N」失去校准。**
  那个数不是自己过时的，是**没有机会被更新**。

⚠ 这正是 §8 自己记的那句：「这类**文档骗人**比代码 bug 更难查」。这次骗的是它自己。

---

## S7 ★★ 三处「印出来的 ≠ 进模型的」

| # | 位置 | 印的 | 实际 |
|---|---|---|---|
| a | `LineRunner.cs:1127` 给用户的提示 | 「`LineCase.CoupleMaxRounds`，**默认 200**」 | `LineRunner.cs:246` 是 **600**。工程师照它去加轮数，会以为还有三倍余量 |
| b | HANDOVER §0.0.3 登记表 R7 | 「**UI 侧孔径控件未做**」 | 做了：`LineDesignPage.cs:3602` 造控件、`:3735` 加进页面、`:2464` 写回 `DesignSpec`。与同表 R5「✅ …界面逐片控件」**自相矛盾** |
| c | `.githooks/pre-commit` + HANDOVER §8 | 「单元测试 ~15 s」「合计约 7 分钟」 | 见 S3 |

⚠ b 是**低报**。按你们的最高准则，低报和高报一样算「搞混」——
「以为还没做」和「以为已经做了」都会让用户的判断错。

---

## 我跑了什么 / 不覆盖 / 成本

**跑了什么**
- `dotnet build Pt_Optimize.sln -c Release` ⇒ 0 错误、29 警告（7.8 s）
- `dotnet test`（全套、无过滤）06:33:06 → 06:57:53，**24 分钟未跑完，主动中止**。这一跑本身就是 S3 的实测
- 逐档 `git diff` 核对它重写的 10 个 `deliverable/*.txt`（9 个逐字节一致 ⇒ **S4 当场收窄**）
- 其余全部是静态核对：`grep` / `git diff` / 逐处读源码，**没有改动你的任何文件**

**不覆盖（这一栏比上一栏重要）**
- **没跑完全套测试** ⇒ S6 的确切用例数**没有拿到**，645 只是静态近似
- **没拿到 `ReconcileTraceTests` 用当前代码重跑的轨迹** ⇒ 「3104.4 g 不可行是不是现况」**仍然未知**
- **没有**验证 S2 里那个 −712.373 是不是坏数——只指出它变了 2.03 倍，成因未查
- **没有**量 W1/W2 各占单次场解的几成（那要插桩，我不动你的代码）
- **没有**跑 `UiWiring` 与 `--cli --selfcheck`
- **没有**证明 S1 是 3480.7 → 3104.4 那次退化的**成因**——只证明了这条路**允许**它发生

**成本**
- 我这边：一次 Release 构建 + 24 分钟测试（中止）
- 对你的代码：**零改动**
- ⚠ 对你的工作区：`deliverable/对帐超时_单次成本.txt` 被我这一跑改了 3 行（耗时数）。
  **我没有回滚它**——那份档同时也有你的改动，我不去动别人未提交的东西。要不要留由你定。

---

## 我建议的顺序（不是决定，是建议）

```
S1 → S2   先把「坏数不许拿来做决定」这道闸补回去。
          在它闭掉之前，任何新旋钮、新组合搜索都是在坏地基上垒。
S3 → S4   门要能天天跑，证据要不会被自己覆盖。仪器和交付物搬出回归道。
S5        再谈快。⚠ 顺序不能反：没有 S1，热启动会把坏状态传下去。
S6 → S7   文档对齐，一次改完。
```

**S1/S2 之前，我认为不该往前加功能。**
理由只有一句：现在 APP 交出来的数，**没有任何机制保证它是解出来的，而不是发散出来的**。

---
---

# 复核更正（2026-09-07 08:07，主开发 agent 复核之后 + 我自己重测）

> 上面 S1–S7 是 07:00 前写的。下面这一节**推翻了其中两条的严重性判断，撤销一条我报错的红**。
> 上面原文一字未改 —— 留着是为了看得见我错在哪。

## 一、撤销：我报过一条不存在的红

我在对话里报过 `Export3dmMatchesSolvedTests.出的图必须等于算的那个东西` 红，46 点圆环。

**那出自旧二进制。** 该测试档 06:32 被改过，我用的 DLL 编于 06:29，而我加了 `--no-build`。
重编后隔离重测：**三条全过**（1m31s / 1m08s / 1m27s）。

⇒ **撤销。** 这正是你们 §0.-2 记的第 4 次教训：「重编之后先比时间戳再信结果」。我犯了同一条。

## 二、S0（主开发 agent 提出）：`DesignSpec.Clone()` 漏拷三个数组 —— 成立

```
DesignSpec.Builtin  是 static readonly，全进程共享
Clone()             MemberwiseClone + 逐个数组 .Clone()，
                    SlotSpanDeg / TabHoleRMm / TabHoleAspect 三个原本不在名单里
⇒ 一条用例改副本的孔径，就地改掉了 Builtin[0] 那个 static
修复                在**工作区，未暂存**
```

**两条独立的线对上了**：它指认的污染值 `孔R32.71 / 拉长3 / 槽120°`，
与 `Export3dmMatchesSolvedTests.出图自检必须逮住法兰伸进管腔` 里写死的坏例 **逐位相同**。

## 三、★ 决定性实测：3104.4 g 是污染，**不是现况**

隔离跑（`--filter ~ReconcileTrace`，进程里只有这一个用例，污染在结构上不可能），新二进制：

```
耗时 41.7 分钟　场解 72 次　可行 True　合计 3480.7 g
停在：第 3 轮全过；因为**只往上走过**，这就是最小的可行点
```

**与暂存版基准逐字相同**（3480.7 g / 72 次场解 / 全判据过）。

| | 09-06 16:57（Clone 未修） | 09-07 08:07（隔离 + 已修） |
|---|---|---|
| 结果 | 可行 False　3104.4 g | **可行 True　3480.7 g** |
| 场解 | 33 次 | **72 次** |
| 片0 首轮裕度 | −712.373 | 回到基准 |

⇒ **答案没有退化。回归是假警报，成因是 S0。**

## 四、由此推翻我自己的严重性排序

我写过：「S1/S2 之前不该往前加功能，因为现在 APP 交出来的数没有机制保证它是解出来的」。

**实测把这句改小了**：0.8 档逐字复现 3480.7 g 全判据过 ⇒ **当前交付数是可信的**。

| | 原判 | 改判 |
|---|---|---|
| **S1** 不读 `Converged` | 拦路 | **潜伏** —— 结构事实不变（`grep -c Converged Solver.cs` 仍是 0），但**没有已知的活动触发** |
| **S2** 爆炸印成「没变好」 | 拦路 | **真发生过**（1e102 那条实物在 09-06 轨迹里），但**触发源是 S0**，已修 |

⚠ **潜伏 ≠ 不用修。** 潜伏的定义就是「今天没触发」。
  S1 那条路**允许**坏场进入定尺寸决策，这一点没有任何东西挡着 —— 换个几何就会撞上。
  但它**不该再排在第一**，我排错了。

**新的验收标准（比原来硬）**：修完 S1/S2 之后，0.8 档隔离跑必须**仍是 3480.7 g / 72 次场解 / 全判据过，逐字不变**。
有基准的修法，比没基准的修法可验证得多 —— 这是这一跑真正买到的东西。

## 五、S8（新增）：轨迹表头只打印 9 根旋钮里的 3 根

```
第 1 轮　合计 2838 g　板厚 …　舌保温 …　环倍率 …
                              ↑ 槽 / 孔径 / 孔拉长 / r₁ / r₂ / t₂ 一个都不显示
```

⇒ **一份被污染的 W08，在轨迹上和干净的长得一模一样。**

这一轮三个人各自绕的弯路，根源都是它：我据此断言「同一起点、裕度变了 2.03 倍」——
**起点根本不同**，是我把两个几何当成了一个。**那句作废。**

**补齐九根，几行的事。不补，下一次污染照样隐形。**

## 六、一个观察，不是结论

同样 **72 次场解**，耗时 **76.0 → 41.7 分钟**（单次 63 s → 34.7 s）。
我这一跑是隔离的，你们那一跑在全套里与别的用例抢 CPU —— **这是我的猜测，没有验**。
若属实，它是 S3 门分层的另一条理由；若不属实，那就是有别的东西变快了，值得知道是什么。

## 七、修正后的顺序

```
S0   Clone 漏拷 + 反射门（已写，未提交）        ← 提交它，基准才可复现
S8   轨迹表头补齐九根旋钮                       ← 几行，专治「看不见」
S3   门分层（快门实测 3 m 53 s；全套 24 分钟没跑完）
S6   679（已是权威数，我这边跑出来的）+ S7 三处文案 + 剩下 4 条红
──── 工作区到此收敛，可以提交 ────
S1 → S2   潜伏缺陷。验收 = 修完 0.8 隔离跑仍 3480.7 g 逐字不变
S5   性能（基线重算 / 基线里的废壳场 / 热启动）
```

**S1/S2 从第一降到第五。** 依据是实测，不是感觉。

## 八、此刻仍红的（新二进制上重测过，逐字不变）

| 红 | 内容 |
|---|---|
| `HandoverGateCount` | 631 vs **679** |
| `DocRefTests.NoLineNumberReferences` | `HoleRaisesCurrentDensityTests` 注释里的 `Criteria.cs:114` |
| `DiscCoverPrescriptionTests` × 3 | 需要盘半径 28.25 → **30**（`MinDiscRadiusMm` 多管了「不比舌片窄」）—— **是门钉了数字还是真回归，由你判** |

`Export3dmMatchesSolved` 已从这张表撤下（见第一节）。

---

# S9 ★★★★★ 一条命令跑了 13.5 小时，产出为零，**没有任何东西察觉**（2026-09-07 08:24）

> 这一条不是代码缺陷，是**工作方式**的缺陷。它比本报告里任何一条都贵：
> 13 小时 31 分，一件事都没做完。

## 现场

```bash
cd "D:/WinForm/Pt_Optimize" && cat > /tmp/chk.csx 2>/dev/null; python - << 'PYEOF'
                              └────────┬─────────┘
                    没有输入源，也没有 heredoc ⇒ 读 stdin ⇒ 永远等不到 EOF
                    而 2>/dev/null 把它可能发出的任何声音一并掐掉
```

`;` 要等 `cat` 结束才轮到 `python`。`cat` 不结束 ⇒ **后面整条链一步都没走。**

## 实测（四条，逐条验过）

```
/usr/bin/cat  PID 204        起于 2026-09-06 18:51:27  →  09-07 08:24:33  = 13.52 小时
/tmp/chk.csx                 0 字节            ← cat 一个字都没写出去
GeometryTwoSourcesTests.cs   不存在            ← ; 后面的 python 从没执行
dotnet / testhost / MSBuild  一个进程都没有    ← dotnet test 从没启动
```

**13.5 小时 = 一个 0 字节的临时档。** 不是测试慢，是**根本没跑测试**。
（已于 08:24 杀掉该进程树。）

## 为什么这条值 ★★★★★

它是本仓库最怕的那个形态的**极端版本**：

```
S3   门要跑 3 小时           ⇒ 人会关掉它
S9   一条命令 13.5 小时零输出 ⇒ 人不知道它没在做事
```

**没有超时、没有心跳、没有看门狗。**
⇒ 「一直没出结果」和「正在算」在屏幕上**长得一模一样**。

你们 2026-09-06 自己定过一条：

> **「一个只会说『超时』的门，等于没有门。」**

**一个连「超时」都不会说的调用，比那还糟。**
那次的处置是「任何有预算的检查都必须把实际耗时打出来，绿的时候也打」——
这一次说明还差一半：**没有预算的调用，先给它一个预算。**

⚠ 顺带点名 `2>/dev/null`：它掐掉的正是唯一可能报警的那条通道。
本次 13.5 小时的静默，一半是它给的。**排错期间不许用它。**

## 规矩（建议，你定）

| | |
|---|---|
| **凡可能阻塞的调用都要有上限** | 见下：**全仓 9 处 `WaitForExit()`，一处上限都没有** |
| **零输出要有下限** | 长任务必须有心跳。你们已经为**界面**做过这件事（「长任务看得出还活着」，§376），**命令行这一侧没做** |
| **排错期间禁用 `2>/dev/null`** | 它掐掉的正是你要找的东西 |
| **报「跑了多久」之前先确认它真的在跑** | 与「拿坏仪器取证」同族：本次「已经跑了 13.5 小时」这个陈述本身是错的 —— 它一秒都没跑 |

## ⚠ 同形状的现役隐患：全仓 9 处 `WaitForExit()`，**没有一处带上限**

```
Pt_Optimize/Core/Geometry3dm.cs        :159 :207 :304 :333 :364 :408 :465   ← 生产代码，7 处
Pt_Optimize.Tests/Export3dmMatchesSolvedTests.cs :208
Pt_Optimize.Tests/ExportShowsSolvedKnobsTests.cs :162
```

全部是裸的 `proc.WaitForExit()`，一个 timeout 参数都没有。

**这七处在生产路径上**：`Geometry3dm` 是起 `Pt_Optimize.Geom` 子进程量测/写 `.3dm` 的那条路，
工程师点「分析几何变数」「导出本页 3DM」「导出可回读 3DM」走的就是它。

⇒ **Rhino 子进程一挂（授权对话框、headless 引擎卡住、DLL 被锁），APP 当场无限期等下去，
   一句话都不说** —— 与本节那 13.5 小时**完全同一个形状**，只不过这次会发生在**现场工程师**手上。

⚠ 这不是推测，机制是明摆的：没有上限的等待，等的就是「永远」。
  但**我没有实测触发过它** —— 我只证明了「没有任何东西挡着它发生」。

⇒ 建议：`WaitForExit(ms)` 给上限，超时就杀子进程并**明说是超时**
  （照你们 09-06 的规矩：绿的时候也打耗时）。
  几何子进程本来就有「必须有能力报告失败」那道门（§213），这一条是它的缺口。

## 一句对照

同一台机器、同一段时间里，督导侧跑完了全部复核：**06:27 → 08:07，1 小时 40 分**，
含一次 41.7 分钟的隔离对帐。**这 13.5 小时里，机器一直是闲的。**

---

# 顺序（并入 S9 之后的最终版）

```
S9   立刻：把那条命令改对（删掉多余的 cat、加 -c Release、去掉 2>/dev/null）
     并给 Probe() 的 WaitForExit 加上限                      ← 不做，下一次照样静默十几小时
S0   Clone 漏拷 + 反射门（已写，未提交）                       ← 提交，基准才可复现
S8   轨迹表头补齐九根旋钮                                      ← 几行，专治「看不见」
S3   门分层（快门实测 3 m 53 s；全套 24 分钟没跑完）
S6   631 → 679（先 git add 那 4 档，顺序不能反）+ S7 三处文案 + 剩下 4 条红
──── 工作区到此收敛，可以提交 ────
S1 → S2   潜伏缺陷。验收 = 修完 0.8 隔离跑仍 3480.7 g / 72 次场解，逐字不变
S5   性能（基线重算 / 基线里的废壳场 / 热启动）
```

**S9 插到最前面**，理由只有一句：**它是唯一一条会让「后面所有步骤都白做」的缺陷** ——
其余每一条最坏是给出错答案，而它是**连答案都不会给，还让人以为在算。**

---

# 复验日志：`d6effaf` 与 S9（2026-09-07 08:52，督导独立验）

> 上面各节写于 07:00–08:24，其中 S0/S8/S3/S6/S7 当时都是「待办」。**这一节记它们的落地与复验结果。**
> 原则同前：**上面原文一字不改。**

## 已落地并经独立复验

| | 状态 | 我怎么验的（不是读提交讯息） |
|---|---|---|
| **S0** Clone 漏拷 | ✅ | `git show HEAD:…DesignSpec.cs` 三行在；反射门 `CloneCopiesEveryArrayTests` 两条含**反向实证**（改副本、原件纹丝不动） |
| **S3** 门分层 | ✅ | 自己跑 `--filter "速度!=慢"` ⇒ **670/670 全绿，1 m 29 s**。679 − Trait 9 条 = 670，与逐条点算一致 |
| **S6** 631→679 | ✅ | `HandoverGateCountTests` 由红转绿（679 是它反射数出的，非人工填） |
| **S7** 三处文案 | ✅ | 随 S3/S6 一并绿 |
| **S9** 子进程无上限 | ✅ **超出要求** | `Geometry3dm` 七处**收敛成一个带上限的帮手**：超时 `Kill(entireProcessTree)` + `TimeoutException` 明说「这条是**超时**，不是『算不出来』」；测试侧两处也补了 10 分钟上限，并顺手修掉一个我没提的坑——**先 stdout 后 stderr 的同步读会死锁**（比无限等待更隐蔽）；新增 `SubprocessMustNotHangTests` |

★ 09-06 那次「3480.7 → 3104.4 退化」由此查实：**污染，不是退化。** 成因 S0，已修。

## 复验中新发现的四条（都不拦路）

| # | 事实 | 严重性 |
|---|---|---|
| **V1** | `InvalidChecks`（`string[]` 栏位）Clone 没拷。门的 `.Where(a.Length > 0)` 跳过空数组，而样本 `Builtin[0]` 的它恰好是空的 ⇒ 门看不见。但 `Builtin[2]/[3]` 是 `{"⑤"}`，**非空 ⇒ 真共享** | ★★ **现在无害**——全仓没有一处就地改它的元素，我查过。洞在**门的取样**：凡「Builtin[0] 上恰好为空、别档非空」的数组栏位，门一律看不见。与 UiWiring §17′「空集恒真」同族 |
| **V2** | **8 个 `.3dmbak` 进了版控**，`.gitignore` 没挡。它是 Rhino 自动备份 = 上一次存档的旧副本，按定义不是事实来源 | ★★ 与《APP使用说明书》「栽在『两处然后悄悄漂开』上的次数比任何算法问题都多」直接冲突 |
| **V3** | **`S5` 现在指两个东西**：本报告 S5 = 单次场解贵在哪（性能）；`d6effaf` 提交讯息 S5 = 「盘R ≥ 舌半宽」并进 `MinDiscRadiusMm` 位置错了。**两处都已进版控** | ★★ 「同一个名字两处来源」，他们为这形状立过门 |
| **V4** | ~~`Clone` 漏拷 `RingRadiiMm`~~ | **✗ 撤销：我的假阳性。** 它是表达式属性 `=> RingRadiiOf(0)`，不是栏位，`GetFields` 看不到也不需要拷。我的 grep 把 `=>` 当成了 `=` |

## 此刻仍未动的

**S8 已复验 ✅（08:58 补）**：`Solver.cs` 的 `Log($"第 {round,2} 轮…")` 现在印
`板厚／舌保温／环倍率t₁／外级t₂／内级r₁／外级r₂／圆盘槽／孔径／孔拉长` —— **九根齐全**。
⚠ 但 `deliverable/对帐超时_轨迹.txt` 里仍是**旧三根**的表头：那份是我 08:07 用 07:20 的二进制跑的，
  早于 S8 落地。**不是缺陷，是产物比代码旧** —— 下一次跑就会带九根。
（顺带一条正面证据：那份档的 `合计 2867 g／裕度 −350.747` 与已知良基准**逐字相同**，
  再次印证 09-06 那次是污染。）

```
S1  Solver 不读 Converged     —— 潜伏，已降到第 5
S2  爆炸印成「没变好」+ 那道门是贴措辞的  —— 同上
S5（本报告口径）性能三项      —— 基线每次重算 / 基线里的废壳场 / 热启动丢了
慢门 9 条                      —— 分层的代价，发版前要补跑
```

⚠ 原文我把 S8 列在「未复验」。**08:58 已补验，见上。**
  口径不变 —— **别人说做了不算证据**，包括对我自己。


---

## 编号对照（2026-09-07 补，督导 ④）

`S5` 这个编号一度指两个东西，两处都在版控里：

| 出处 | S5 指的是 |
|---|---|
| 本报告 | **单次场解贵在哪**（W1 基线每次重算 / W2 基线里的废壳场 / W3 热启动丢了）|
| 提交 `d6effaf` 的讯息 | 「盘R ≥ 舌半宽」并进 `MinDiscRadiusMm` 是**位置错了** |

提交讯息进了历史改不动，所以在这里立对照：
**后者以下一律称「⑥ 判据拆开」，不再叫 S5。**
（「同一个名字两处来源」是本仓库立过门的那一族 —— 编号也算名字。）

## 我改过督导原文的一处措辞（2026-09-07）

第 554 行原用了一个**在 BannedVocabularyTests 名单上的**词（它属于用户明令废掉的那套「归档定稿」模式，
`BannedVocabularyTests` 扫全仓，它当场红）。我只把那个词换成「就此认定」，
**结论一字未动**。别人的文字我不默默改，所以在这里记一笔。

⚠ 同一个词我自己在提交 `d6effaf` 的讯息里也用了。历史改不动，记在这里 ——
那道门扫的是工作区文件，扫不到 git log，所以它没红，但错是我的。

## 撤回：RingRadiiMm 不是漏拷（督导 ①）

`public double[] RingRadiiMm => RingRadiiOf(0);` 是**表达式属性**，不是栏位：
`GetFields` 看不到它，也不需要拷（每次访问都新算）。督导的 grep 把 `=>` 当成了 `=`，已撤销。
